> Nota geral: toda reconversão do corpus mencionada neste arquivo DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar.

> Nota de escopo: esta mudança corrige exclusivamente a proteção `native_label_pattern` de `recompose_native_paragraphs`. O early-return de linhas iniciadas por `:` (que desativa a recomposição geométrica da página inteira em `AINTARESP_1462304-PA.pdf` p.1/4/11 e `REsp_1704551-SP.pdf` p.1/3/4/6/7/14) está fora de escopo e não deve ser tocado — está registrado em `LOOPS.md` como achado pendente para mudança futura.

## 1. Restringir native_label_pattern à primeira linha física do bloco

- [ ] 1.1 Criar testes de regressão em `tests/test_cleaner.py` para `recompose_native_paragraphs` que reproduzam os dois formatos reais relatados e falhem antes da correção: (a) ementa fragmentada em palavras/fragmentos isolados dentro do mesmo bloco geométrico (formato equivalente a "PROVEITO" / "ECONÔMICO" / "DA" / "DEMANDA." de `AINTARESP_1462304-PA.pdf` p.7); (b) campo temático fragmentado no mesmo bloco do rótulo (formato equivalente a "RAMO DO DIREITO" seguido de "DIREITO" / "PROCESSUAL" / "PENAL," / "DIREITO" / "DA" / "PESSOA" / "COM" / "DEFICIÊNCIA" de `Inf0024E.pdf` p.4, onde rótulo e valor compartilham o mesmo bloco). Incluir também: sequência com linhas de uma palavra intercaladas com linhas de várias palavras; as ocorrências equivalentes de `AINTARESP_1462304-PA.pdf` p.9 e `REsp_1704551-SP.pdf` p.12. Incluir os testes negativos exigidos: rótulos de campo reais (`PROCESSO`, `RAMO DO DIREITO`, `TEMA`, `DESTAQUE`) como primeira linha física de seu próprio bloco permanecem separados do valor; títulos/cabeçalhos estruturais legítimos (incluindo os 8 casos de `SUBTÍTULO` do Código Civil e os demais marcadores formais/bare/qualified já cobertos); listas, incisos e enumerações; precedentes jurisprudenciais distintos; parágrafos realmente separados; os casos positivos de R01. Confirmar que os novos testes falham antes da implementação.
- [ ] 1.2 Implementar em `src/pipeline_juridico/cleaner.py`, dentro de `recompose_native_paragraphs`, a menor correção generalizável: ao montar a lista de linhas físicas a partir dos blocos geométricos, registrar para cada linha se ela é a primeira linha física (índice 0) do seu bloco de origem, e alterar a condição `not native_label_pattern.match(previous_text)` para só bloquear a junção quando essa marca for verdadeira para a linha anterior. Não alterar `current_line_pattern`, `formal_structure_pattern`, `bare_structure_pattern`, `qualified_structure_pattern`, o cálculo de `gap`/`line_height`, a proteção de fechamento jurisprudencial, o early-return de tabelas `|`, o early-return de linhas `:`, `mark_final_index`, o extrator, o roteamento, o OCR ou a arquitetura geral. Não criar regra específica a palavra, tribunal, processo, página ou arquivo.
- [ ] 1.3 Executar a suíte completa (`uv run pytest tests/`) e confirmar que todos os novos testes passam e que nenhum teste existente regrediu (incluindo os de R01, SUBTÍTULO, separação de precedentes e remoção de cabeçalhos/rodapés repetitivos).
- [ ] 1.4 Reconverter os 4 PDFs do corpus fixo com `converter-juridico --no-ocr` (`AINTARESP_1462304-PA.pdf`, `Inf0024E.pdf`, `REsp_1704551-SP.pdf`, `L10.406_CC_2002.pdf`), examinar o diff completo contra a saída atual em `output/` e confirmar: (a) as fragmentações relatadas em `AINTARESP_1462304-PA.pdf` p.7 e `Inf0024E.pdf` p.4 foram recompostas corretamente, sem alteração de tokens; (b) as ocorrências equivalentes de `AINTARESP_1462304-PA.pdf` p.9 e `REsp_1704551-SP.pdf` p.12 também foram corrigidas; (c) nenhum rótulo de campo real (`PROCESSO`, `RAMO DO DIREITO`, `TEMA`, `DESTAQUE`, etc.) foi fundido ao valor que o segue; (d) `L10.406_CC_2002.md` não sofreu nenhuma alteração indevida (os 8 casos de `SUBTÍTULO` e toda a hierarquia legislativa permanecem corretos); (e) cabeçalhos/rodapés repetitivos continuam removidos; (f) correções anteriores (R01, `fix-jurisprudence-precedent-fusion`, `fix-subtitulo-structural-boundaries`, `fix-repeated-header-cross-page-fusion`) permanecem intactas; (g) os marcadores `[[Pág. N]]` permanecem únicos, sequenciais e em quantidade idêntica à anterior em cada arquivo; (h) nenhuma página acionou OCR sob `--no-ocr`; (i) reconverter uma segunda vez e confirmar saída byte-idêntica à primeira reconversão (idempotência).
