> Nota geral: toda reconversão do corpus mencionada neste arquivo DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar.

## 1. Reconhecimento do marcador estrutural SUBTÍTULO

- [x] 1.1 Criar testes de regressão em `tests/test_cleaner.py` que reproduzam os 4 formatos positivos relatados (SUBTÍTULO anexado a artigo, a parágrafo, a inciso, e a cabeçalho TÍTULO já formado) para `recompose_native_paragraphs` e/ou `build_legislative_headings`, e confirmar que esses testes falham antes da correção. Incluir também os testes negativos exigidos: a palavra "subtítulo" em texto comum não vira cabeçalho; entradas do índice final (linha única "SUBTÍTULO N DENOMINAÇÃO") permanecem inalteradas; TÍTULO/CAPÍTULO/SEÇÃO/SUBSEÇÃO já corretos continuam corretos; artigos consecutivos continuam separados; os 4 casos positivos de R01 (Art. 44 §2º, Art. 593, Art. 1.458, Art. 1.368-F) continuam passando; a separação de precedentes jurisprudenciais consecutivos continua passando.
- [ ] 1.2 Implementar em `src/pipeline_juridico/cleaner.py` a menor correção generalizável: adicionar `SUBTÍTULO`/`SUBTITULO` a `_LEGISLATIVE_MARKER_PATTERN`, `formal_structure_pattern`, `bare_structure_pattern`, `qualified_structure_pattern` (dentro de `recompose_native_paragraphs`) e a `heading_levels` no nível `4` (mesmo nível de `capítulo`), dentro de `build_legislative_headings`. Não criar regra específica a números de artigo ou ao Código Civil. Não alterar `current_line_pattern`, a proteção de fechamento jurisprudencial, `mark_final_index`, o extrator, o roteamento, o OCR ou a arquitetura geral.
- [ ] 1.3 Executar a suíte completa (`uv run pytest tests/`) e confirmar que todos os novos testes passam e que nenhum teste existente (inclusive os de R01 e da separação de precedentes) regrediu.
- [ ] 1.4 Reconverter os 4 PDFs do corpus fixo com `converter-juridico --no-ocr` (`L10.406_CC_2002.pdf`, `AINTARESP_1462304-PA.pdf`, `REsp_1704551-SP.pdf`, `Inf0024E.pdf`), comparar antes/depois e confirmar: (a) as 8 ocorrências do corpo de `L10.406_CC_2002.md` foram corrigidas e nenhuma outra ocorrência de SUBTÍTULO ficou anexada; (b) o índice final continua íntegro, na mesma ordem e sem conteúdo removido; (c) os 4 casos positivos do R01 continuam corretos; (d) as separações entre precedentes jurisprudenciais continuam corretas; (e) os marcadores `[[Pág. N]]` permanecem sequenciais, únicos e em quantidade idêntica à anterior; (f) os outros 3 arquivos do corpus não sofreram nenhuma alteração indevida; (g) nenhuma página acionou OCR sob `--no-ocr`.
