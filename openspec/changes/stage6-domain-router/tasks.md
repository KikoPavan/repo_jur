## 1. C6.1 — Domain Router core: focused tests (TDD — red first)

- [ ] 1.1 Write failing tests for the input contract: the routing entry point accepts the Stage 3 `Phase1Artifacts` (literal Markdown + technical conversion report) read-only plus the Critical-Data Validation status and a validated routing context, and evaluates without requesting or performing further conversion, without resolving evidence, and without invoking OCR; a source-inspection test asserts `src/pipeline_juridico/domain_router.py` contains no evidence-resolution, converter, engines, inspector, or OCR invocation path (no import of the converter, engines, inspector, evidence, or OCR modules).
- [ ] 1.2 Write failing tests for the post-gate invariant: an artifact whose technical report records `result.quality_gate` as `PASS` is eligible for routing; an artifact recording `PASS_WITH_WARNINGS` is eligible for routing; an artifact recording `FAIL` stops routing (no decision produced — a deterministic routing-blocked error is raised); an artifact whose report is unparseable or lacks a recorded gate outcome is not routed (deterministic routing-blocked error); the gate outcome is read from the report, never from a caller-supplied override.
- [ ] 1.3 Write failing tests for the Critical-Data precedence: with recorded gate `PASS`/`PASS_WITH_WARNINGS` and critical status `REVIEW_REQUIRED`, the decision is `review_required` regardless of the routing context, and the critical status is evaluated before any routing signal; with critical status `OK` or `WARNING`, routing proceeds to the signal-evaluation step and the status alone never selects a domain or `review_required`.
- [ ] 1.4 Write failing tests for the approved initial routing signal `requested_domain`: with recorded gate `PASS`/`PASS_WITH_WARNINGS` and critical status `OK`/`WARNING`, a context carrying `requested_domain == "legal_knowledge"` yields the decision `legal_knowledge`; `requested_domain == "judicial_process"` yields `judicial_process`; **no routing context** yields `review_required`; an **empty context** yields `review_required`; the decision is never `legal_knowledge` or `judicial_process` through any signal other than `requested_domain` (the single permitted signal — `legal_hints`-derived keys and any other key are outside the vocabulary).
- [ ] 1.5 Write failing tests for the approved fixed precedence (human decision HR-2): (1) recorded gate `FAIL` stops routing with no decision even when `requested_domain` is present; (2) critical `REVIEW_REQUIRED` routes to `review_required` even when `requested_domain` selects a domain; (3) `requested_domain == legal_knowledge` → `legal_knowledge`; (4) `requested_domain == judicial_process` → `judicial_process`; (5) `requested_domain` absent → `review_required`; the order is fixed and steps are evaluated strictly sequentially.
- [ ] 1.6 Write failing tests for the configuration-error contract (HR-3): a routing context containing a signal key outside the permitted vocabulary (any key other than `requested_domain`) raises the dedicated `RoutingConfigurationError`; a context with `requested_domain` set to any value other than exactly `legal_knowledge` or `judicial_process` raises it; a structurally invalid value or malformed payload raises it; the error is never converted into a routing decision (including `review_required`) and never into a domain route — mirroring the Stage 4 configuration-error pattern (`CriticalValidationConfigurationError`).
- [ ] 1.7 Write failing tests for determinism and idempotency: evaluating identical `Phase1Artifacts`, critical status, and routing context twice yields identical decisions and identical reason codes; the decision contains no execution id, timestamp, or duration (pure function of the inputs).
- [ ] 1.8 Write failing tests for the non-mutation invariants: `SHA256(phase1_artifacts.markdown)` before calling the routing entry point equals the value after, and the serialized `phase1_artifacts.report_json` is byte-identical after routing, across `legal_knowledge`, `judicial_process`, `review_required` decisions and blocked (gate `FAIL`) outcomes; no routing metadata appears in the literal Markdown body or in the report.
- [ ] 1.9 Write failing tests that the signal is never derived from document content: with recorded gate `PASS` and critical status `OK`, a Markdown body containing strong legal-knowledge and judicial-process keywords but **no routing context** yields `review_required`; the same Markdown body routed with `requested_domain == "legal_knowledge"` yields `legal_knowledge` (decision tracks the validated signal, never the content); mutating only the Markdown body between two runs with identical context and status yields identical decisions.
- [ ] 1.10 Write failing domain-neutrality and independence source-inspection tests mirroring `tests/test_conversion_engine.py:299-306` and the Stage 4/5 patterns: reading `src/pipeline_juridico/domain_router.py` as text contains no semantic-classifier, LLM/semantic-model client, enrichment, Producer, domain-schema, or canonical publication reference; it contains no content-analysis path (no reading of the Markdown body for signal derivation); it contains no `guard_legal_bundle_write` invocation and no `bundle` write path; it references `RouteTarget` and `Phase1Artifacts` only as the shared contracts.
- [ ] 1.11 Write failing tests that the router never writes: executing routing for any decision performs no filesystem write to `repo_jur/bundle/`, no write to judicial-process storage, and no invocation of `guard_legal_bundle_write` (the guard's existing tests keep passing unchanged).
- [ ] 1.12 Write failing tests for the exact route-target outcomes: every decision serializes exactly as one of `legal_knowledge`, `judicial_process`, `review_required` (reusing `RouteTarget` from `contracts.py`); no other outcome is producible.
- [ ] 1.13 Write failing tests for the decision envelope: the routing decision carries the selected route target and a deterministic reason code from the fixed vocabulary — `critical_review_required`, `requested_domain_legal_knowledge`, `requested_domain_judicial_process`, `missing_routing_signal`, `signal_conflict` — and the reason code is derived solely from the inputs.
- [ ] 1.14 Write failing tests for observability-record safety and location: the record builder emits a payload that identifies the evidence by the report's provenance hash, records the gate outcome, the critical status, and routing-context signal **keys only** (never values — the `requested_domain` key presence is recorded, never its value), plus the decision and reason code; the record contains no document content, no full critical identifier value, and no routing-signal value; the record storage path is configurable (default `var/routing/state/`) and is rejected when it resolves inside the canonical bundle (reusing `config.py::ensure_outside_canonical_bundle`); a deterministic filename derived from the Phase 1 report (e.g. the execution id) is used.

## 2. C6.1 — Domain Router core: minimal implementation

- [ ] 2.1 Implement `src/pipeline_juridico/domain_router.py` with the frozen decision envelope (`RoutingDecision` carrying the route target and the deterministic reason code), reusing `RouteTarget` and `Phase1Artifacts` from the shared contracts without modification, and a `RoutingConfigurationError` dedicated configuration-error type mirroring the Stage 4 pattern.
- [ ] 2.2 Implement the pure, stateless routing entry point following the approved precedence (design.md Decision 6): parse the report read-only and read the recorded gate outcome from `result.quality_gate` (unparseable report or absent outcome → deterministic routing-blocked error; `FAIL` → routing-blocked error, no decision); `REVIEW_REQUIRED` critical status → `review_required` before any signal; `requested_domain == "legal_knowledge"` → `legal_knowledge`; `requested_domain == "judicial_process"` → `judicial_process`; `requested_domain` absent → `review_required`; unrecognized key or invalid value → `RoutingConfigurationError`; the Markdown body is never read for signal derivation.
- [ ] 2.3 Implement the routing-context validation with the fixed permitted signal vocabulary `{requested_domain}` (design.md Decision 4): the schema accepts the context carrier and validates the single key `requested_domain` against the two permitted values `legal_knowledge`/`judicial_process`, rejecting any other key and any other value via the dedicated configuration error; no contextual semantic analysis of values is performed.
- [ ] 2.4 Implement the observability record builder as a separate pure function over the decision inputs and outcome (design.md Decision 7/8): schema version, evidence provenance hash, gate outcome, critical status, routing-context signal keys (no values), decision, reason code, execution id when present in the report; the builder performs no I/O.
- [ ] 2.5 Ensure `domain_router.py` never mutates `Phase1Artifacts`, never imports or invokes the converter, engines, inspector, evidence, OCR, cleaner, validator, or report-generation modules, never invokes `guard_legal_bundle_write`, never writes to `repo_jur/bundle/` or process storage, never performs semantic classification, content inference, or LLM calls, and never generates YAML.

## 3. C6.2 — Operational route CLI: focused tests (TDD — red first)

- [ ] 3.1 Write failing tests for the `repo-jur route` command surface: invoking `repo-jur route <markdown.md> <report.json> --domain legal_knowledge` reads both artifact files, computes the deterministic decision `legal_knowledge`, writes the routing observability record under the configured state directory (default `var/routing/state/`, environment-overridable), and prints the decision; `--domain judicial_process` yields `judicial_process`; `--context <context.json>` supplies the same validated carrier (`{"requested_domain": "legal_knowledge"}` / `{"requested_domain": "judicial_process"}`); omitting both `--domain` and a `requested_domain`-carrying `--context` yields `review_required`; `--domain` and `--context` with **disagreeing** `requested_domain` values exit with a configuration error (exit 3); `--json` emits machine-readable output.
- [ ] 3.2 Write failing tests for route-command exit codes following the existing CLI convention: `0` when a decision is recorded; `3` on contract/configuration errors (invalid report contract surface, `RoutingConfigurationError` from unknown key/invalid value/disagreeing `--domain`/`--context`, unreadable context); `2` on unexpected errors; `1` on input errors; and a distinct non-zero code (`5`) when routing is blocked by a recorded `FAIL` gate outcome — no decision emitted and no routing decision record written on block.
- [ ] 3.3 Write failing tests that the route command never mutates the artifacts: after `repo-jur route` completes (success, blocked, or error), the literal Markdown file and the technical report file are byte-identical.
- [ ] 3.4 Write failing tests that the route command never invokes conversion or OCR and never touches the bundle: source-inspection of `src/pipeline_juridico/domain_router_cli.py` contains no converter, engines, inspector, OCR, evidence, or bundle-write path; behavioral tests confirm no file is written under `repo_jur/bundle/` and no process-storage write occurs.
- [ ] 3.5 Write failing observability tests at the CLI level: the record written by `repo-jur route` contains the decision, reason code, gate outcome, critical status, evidence provenance hash, and signal **keys only** — never document content, never full critical identifier values, and never the `requested_domain` value — and is stored outside the canonical bundle; a state directory resolving inside the bundle is rejected.
- [ ] 3.6 Write failing CLI non-regression tests: the existing `converter-juridico` surface (positional PDF argument, `--overwrite`, `--allow-partial`, `--no-ocr`, `--keep-temp`, `--log-level`, environment-driven `OUTPUT_DIR`/`LOGS_DIR`/`TEMP_DIR`, exit codes 0/1/2/3/4) remains unchanged; the full existing `tests/test_cli.py` suite passes without modification.

## 4. C6.2 — Operational route CLI: minimal implementation

- [ ] 4.1 Implement `src/pipeline_juridico/domain_router_cli.py` with argparse, following the existing CLI conventions (environment-driven directories, `--log-level`, sanitized/redacted logging helper reused or mirrored from `cli.py`), exposing the `route` subcommand with `<markdown.md>`, `<report.json>`, `--domain legal_knowledge|judicial_process`, `--context`, `--state-dir`, `--log-level`, and `--json`; the `--domain` flag maps onto the `requested_domain` signal, and `--domain`/`--context` disagreement on `requested_domain` is surfaced as a `RoutingConfigurationError` (exit 3).
- [ ] 4.2 Wire the command to the core routing entry point: load and validate the artifact files and the optional context (merging `--domain` and `--context` into the effective validated context), compute the decision deterministically, build the observability record, and persist it atomically under the state directory (reusing `validator.py::write_atomic`), printing the decision and returning the documented exit codes; blocked routing (recorded `FAIL`) exits non-zero without emitting a decision or writing a decision record.
- [ ] 4.3 Register the new console script `repo-jur = "pipeline_juridico.domain_router_cli:main"` in `pyproject.toml`; do not modify the existing `converter-juridico` entry point or any line of `cli.py`.

## 5. Validation

- [ ] 5.1 Run the focused Stage 6 tests (`uv run pytest tests/test_domain_router.py tests/test_domain_router_cli.py -v`) — all green.
- [ ] 5.2 Run the full regression suite (`uv run pytest tests/`, with the gitignored `input/*.pdf` corpus fixtures present in the worktree) — all existing tests plus new Stage 6 tests pass with no behavioral change to Stages 1–5, and `tests/test_cli.py` passes unmodified (CLI non-regression).
- [ ] 5.3 Run OpenSpec strict validation for this change (`openspec validate stage6-domain-router --type change --strict`) and the full strict validation (`openspec validate --all --strict`) — all passed, 0 failed.
- [ ] 5.4 Confirm the final diff: no FROZEN baseline modification, no `bundle/` write, no Stage 4/5 code or spec modification, no `router.py` change, no converter/OCR/routing-thresholds/cleaner/validator/report-generation change, no `contracts.py`/`config.py` change, no `cli.py` change, no dependency change, no Stage 7+ implementation, and no new normative rule invented without a FROZEN citation or the approved human review decisions (HR-1/HR-2/HR-3); the only production-code additions are `domain_router.py`, `domain_router_cli.py`, the two test modules, and the `repo-jur` console-script registration.
