> Nota geral: toda reconversão do corpus mencionada neste arquivo DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar.

## 1. Subordinação dos cabeçalhos do índice a `# ÍNDICE`

- [x] 1.1 Criar testes de regressão em `tests/test_cleaner.py` para `mark_final_index` que reproduzam o caso positivo: um documento sintético com corpo normativo, um marcador `PARTE`/palavra-chave equivalente já convertido em cabeçalho `#` ANTES do bloco denso do índice (corpo, não deve mudar), e dentro do bloco denso do índice (após o último artigo) um parágrafo bare `"ÍNDICE"` seguido por um cabeçalho `#` já formado (simulando o que `build_legislative_headings` já produz para PARTE GERAL/PARTE ESPECIAL) — confirmar que, após `mark_final_index`, esse cabeçalho vira `##` (nível+1) e `# ÍNDICE` permanece nível 1. Cobrir também o caminho em que `# ÍNDICE` é inserido como novo parágrafo (não promovido de um bare "ÍNDICE" existente). Incluir os negativos obrigatórios: cabeçalhos do corpo antes do índice permanecem inalterados; um documento sem índice detectável (`structural_count < 3`) não sofre nenhuma alteração; parágrafos de texto comum (não-cabeçalho) dentro do índice permanecem exatamente como estão, sem virar cabeçalho; marcadores `[[Pág. N]]` dentro e ao redor do índice permanecem inalterados; um cabeçalho hipotético já em `######` não ultrapassa o teto de 6 `#`. Confirmar que os testes falham antes da correção.
- [x] 1.2 Implementar em `src/pipeline_juridico/cleaner.py`, dentro de `mark_final_index`, a menor correção generalizável: após localizar (promovendo um parágrafo bare `"ÍNDICE"`) ou inserir o cabeçalho `# ÍNDICE`, demover em exatamente um nível (`#` a mais, com teto em `######`) todo parágrafo estritamente posterior que já seja um cabeçalho Markdown ATX (`^#{1,6}\s`). A decisão deve usar somente a posição relativa a `# ÍNDICE`, nunca o texto do cabeçalho (não criar regra específica para "PARTE GERAL"/"PARTE ESPECIAL"). Não alterar `build_legislative_headings`, `recompose_native_paragraphs`, o extrator, o roteamento, o OCR ou a arquitetura geral.
- [x] 1.3 Executar a suíte completa (`uv run pytest tests/`) e confirmar que todos os novos testes passam e que nenhum teste existente regrediu (inclusive os 8 casos de SUBTÍTULO do corpo e os testes de `mark_final_index` já existentes).
- [x] 1.4 Reconverter os 4 PDFs do corpus fixo com `converter-juridico --no-ocr` (`L10.406_CC_2002.pdf`, `AINTARESP_1462304-PA.pdf`, `REsp_1704551-SP.pdf`, `Inf0024E.pdf`), comparar antes/depois e confirmar: (a) em `L10.406_CC_2002.md`, `# ÍNDICE` permanece nível 1 e `# PARTE GERAL`/`# PARTE ESPECIAL` dentro do índice viram `## PARTE GERAL`/`## PARTE ESPECIAL`; (b) o corpo do documento, do início até imediatamente antes de `# ÍNDICE`, permanece byte-idêntico ao anterior (incluindo os 8 `#### SUBTÍTULO` e os dois cabeçalhos `# PARTE GERAL`/`# PARTE ESPECIAL` do corpo, que NÃO devem ser alterados); (c) nenhum texto ou token do índice foi alterado, apenas o nível dos cabeçalhos já existentes; (d) os 4 casos R01 continuam corretos; (e) os marcadores `[[Pág. N]]` permanecem sequenciais, únicos e em quantidade idêntica à anterior; (f) os outros 3 arquivos do corpus (`AINTARESP_1462304-PA.md`, `REsp_1704551-SP.md`, `Inf0024E.md`) ficam byte-idênticos aos anteriores; (g) nenhuma página acionou OCR sob `--no-ocr`; (h) uma segunda reconversão produz saída byte-idêntica à primeira (idempotência).
