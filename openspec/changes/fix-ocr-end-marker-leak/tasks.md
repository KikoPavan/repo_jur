> Mudança exclusivamente diagnóstica (instrução via `/goal`: "SOMENTE DIAGNÓSTICO"). Conclusão: **A) CRITÉRIO SEGURO ENCONTRADO** — o vazamento de `[End OCR]*` (artefato de formatação do plugin de terceiros `markitdown-ocr`) tem causa raiz isolada por leitura de código e reprodução local (sem OCR real); o ponto mínimo e seguro de remoção é imediatamente após a correção geométrica já arquivada (`fix-ocr-rotated-text-fragmentation`) consumir o marcador pela última vez. Nenhum código foi implementado. Ver `design.md` para a evidência completa e `proposal.md` para o resumo executivo.

## 0. Diagnóstico (concluído)

- [x] 0.1 Baseline: `git status --short`, `git log --oneline -10`, `openspec validate --all --strict`. Repo limpo, HEAD `b266ac6`, `fix-ocr-rotated-text-fragmentation` confirmada arquivada em `openspec/changes/archive/2026-08-14-fix-ocr-rotated-text-fragmentation/`. `design.md`, "Contexto".
- [x] 0.2 Rastrear a origem de `[End OCR]*` no código instalado de `markitdown_ocr` (`_pdf_converter_with_ocr.py`): função/arquivo, representação exata, possibilidade de múltiplas ocorrências por página, dependências internas deste pipeline, momento em que sua função estrutural termina. `design.md`, ETAPA 1.
- [x] 0.3 Investigar por que `## Page N`/`*[Image OCR]` (também emitidos pelo plugin) não aparecem no Markdown final hoje, enquanto `[End OCR]*` aparece — reprodução local determinística (sem OCR real) contra `remove_repetitive_margins`, confirmando a causa posicional (meio de conteúdo vs. início/fim de página). `design.md`, "Descoberta empírica adicional".
- [x] 0.4 Verificar interação com `_split_ocr_tail`, a reconstrução do residual vertical, `remove_repetitive_margins` e a composição dos marcadores `[[Pág. N]]`; identificar o ponto de não-retorno (última dependência real do marcador) e confirmar por reprodução local que remover o marcador antes desse ponto quebra `fix-ocr-rotated-text-fragmentation`. `design.md`, ETAPA 2.
- [x] 0.5 Comparar os três critérios candidatos (A: imediatamente após consumir a função interna; B: no fim da composição por página; C: pós-processar o documento final) quanto a acoplamento, risco de remover texto legítimo, impacto sobre o residual vertical, blast radius, idempotência e necessidade de reconhecimento por texto literal — sem criar limpeza global de strings arbitrárias. `design.md`, ETAPA 3.
- [x] 0.6 Reexecutar o roteamento (sem OCR) dos 8 PDFs de referência (270 páginas) de forma independente, confirmando que somente as 4 páginas `hibrido` do Testamento são elegíveis para qualquer candidato, e que marcadores `[[Pág. N]]`, conteúdo OCR substantivo e o resíduo vertical reconstruído permanecem intactos em todos os candidatos avaliados. `design.md`, ETAPA 4.
- [x] 0.7 Classificar a conclusão (A ou B) com origem, função exata, momento em que o marcador deixa de ser necessário, ponto mínimo de remoção, blast radius e proposta futura. `design.md`, "CONCLUSÃO".
- [x] 0.8 Confirmar não regressão: nenhum arquivo de `src/`, `tests/`, `output/`, `logs/` ou do corpus canônico alterado; nenhuma chamada de OCR/LLM; scripts de investigação mantidos fora do repositório (scratchpad da sessão).

## 1. Encerramento do ciclo de diagnóstico

- [x] 1.1 Claude (orquestrador) executou todo o diagnóstico diretamente (sem Codex/OpenCode), por se tratar de tarefa de auditoria/leitura de código, não de implementação em `src/`/`tests/` — consistente com `AGENTS.md`. `LOOPS.md` atualizado com o resultado deste diagnóstico ao encerrar este ciclo.
- [x] 1.2 Nenhum código, teste, dependência ou corpus canônico foi alterado. Commit local (sem push) desta mudança, sem arquivar (aguardando decisão humana de avançar à TDD/implementação, mesmo padrão de `fix-ocr-rotated-text-fragmentation`).

## 2. Implementação (TDD) — autorizada em 2026-08-14, ver `proposal.md` "Implementação"

> Codex é o implementador (cria testes + código); Claude (orquestrador) revisa o diff, executa os testes e valida antes de marcar concluído. Ponto de intervenção aprovado: `converter.py::convert_document`, imediatamente após `_replace_fragmented_vertical_residuals_in_document` e antes de `join_symbol_across_page_break`; correção restrita a páginas `hibrido`/`ocr_integral`. Nenhuma chamada real de OCR/LLM; todos os testes usam mocks/fixtures já existentes.

- [x] 2.1 Codex criou 6 testes RED em `tests/test_converter.py` para o helper ainda inexistente `_strip_internal_ocr_markers`: positivos (`test_strip_internal_ocr_markers_preserves_eligible_page_content`, cobrindo remoção de múltiplas ocorrências do marcador, preservação do texto substantivo anterior e do resíduo reconstruído posterior, preservação de `[[Pág. N]]`) e negativos (`test_strip_internal_ocr_markers_ignores_ineligible_pages` × 3 métodos, `test_strip_internal_ocr_markers_without_marker_is_noop` × 2 métodos). Também estendeu o teste de integração existente `test_convert_document_replaces_fragmented_vertical_residual_on_hybrid_pages` (de `fix-ocr-rotated-text-fragmentation`) para exigir `markdown.count(OCR_END_MARKER) == 0`, cobrindo diretamente a não-regressão daquela correção. `uv run pytest tests/test_converter.py -v`: RED inicial 42 passaram / 7 falharam como esperado (`AttributeError`).
- [x] 2.2 Claude revisou os testes RED: cobertura completa do escopo aprovado (remoção restrita a `hibrido`/`ocr_integral`, múltiplas ocorrências, texto literal semelhante mas não idêntico preservado, `[[Pág. N]]`/método preservados, não-regressão da correção geométrica já arquivada); nenhum sinal proibido; nenhuma alteração em `cleaner.py`/`router.py`/`engines.py`/prompt/modelo/provider/dependências (confirmado por `git status --short` + leitura do diff).
- [x] 2.3 Codex implementou `_strip_internal_ocr_markers(markdown, blocks, marker="[End OCR]*")` em `converter.py` (25 linhas: reaproveita `_PAGE_MARKER_SPLIT_PATTERN`/`_PAGE_MARKER_NUMBER_PATTERN` e a mesma lista de elegibilidade `hibrido`/`ocr_integral` já usada por `_replace_fragmented_vertical_residuals_in_document`; remove todas as ocorrências do literal via `segment.replace(marker, "")` apenas nos segmentos elegíveis) e o ponto de integração em `convert_document`, exatamente entre `_replace_fragmented_vertical_residuals_in_document` e `join_symbol_across_page_break`. `uv run pytest tests/test_converter.py -v`: 49/49. `uv run pytest tests/`: **391/391**.
- [x] 2.4 Claude revisou o diff completo linha a linha (`git diff src/pipeline_juridico/converter.py tests/test_converter.py`): escopo confinado exatamente ao ponto aprovado, nenhum sinal proibido, `cleaner.py`/`router.py`/`engines.py`/prompt/`pyproject.toml` intocados (confirmado por `git status --short`). Reexecução independente: `uv run pytest tests/test_converter.py -v` 49/49, `uv run pytest tests/` 391/391.
- [x] 2.5 Delta spec criado por Claude em `specs/juridical-pdf-conversion/spec.md` (`## ADDED Requirements`, requisito "Não vazamento de marcadores internos do mecanismo de OCR para o Markdown final"), formalizando genericamente: marcadores internos do mecanismo OCR não devem vazar para o Markdown final; remoção só ocorre depois de qualquer processamento dependente da posição do marcador (em particular, depois da substituição geométrica de resíduo vertical já formalizada); conteúdo OCR e residual nativo reconstruído preservados; caminho `texto_nativo`/`vazia`/`erro` não afetado; nenhuma limpeza genérica de strings arbitrárias. `openspec validate fix-ocr-end-marker-leak --strict`: válida. `openspec validate --all --strict`: **2 passed, 0 failed**.
- [x] 2.6 Validação final com dados 100% reais do Testamento (texto substantivo por página extraído do Markdown já auditado em `validate-supervised-ocr-testamento-publico`, resíduo fragmentado reproduzido localmente via pdfplumber sobre `input/processos_auditoria/012-015-Testamento Publico.pdf`, motor de OCR mockado — nenhuma chamada real): 4/4 páginas `hibrido`, status `sucesso`; `[End OCR]*` = 0 (baseline: 4); `[[Pág. N]]` = 4; texto substantivo de cada página presente byte a byte; resíduo vertical reconstruído (ex. página 1: URL + protocolo + assinante digital do carimbo e-SAJ) presente e intacto, imediatamente após o texto substantivo, sem o marcador entre eles. Idempotência: segunda execução do mesmo dry run produziu Markdown byte-idêntico. Regressão dos 4 PDFs canônicos com `--no-ocr`: byte-idênticos aos já commitados em `output/`. Regressão dos 3 PDFs de `processos_auditoria/` restantes com `--no-ocr`: sucesso, zero OCR; `100-106-DECISÃO.md` manteve 94 linhas / 0 linhas de 1 caractere (correção `fix-rotated-digital-signature-noise` intacta). Testamento sob `--no-ocr` continua corretamente bloqueado (`exit=3`, não afetado por esta mudança). Blast radius real: confinado às 4 páginas `hibrido` do Testamento; as 266 páginas `texto_nativo` do corpus permanecem sem alteração.
- [x] 2.7 `LOOPS.md` e `proposal.md`/`design.md`/esta `tasks.md` atualizados com o resultado da implementação. Commit local (sem push), sem arquivar.
