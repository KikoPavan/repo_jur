## 0. Gating note

- HR-1..HR-8 were **human-approved** on the planning card (2026-08-23) and are incorporated into this change. The values referenced below are the approved HR values: derived root `var/retrieval/` (HR-4); Chunking Profile v1 defaults `measurement_unit=characters`, `soft_limit=6000`, `hard_limit=12000`, `forced_split_overlap=200` (HR-2); chunk-set persistence during `retrieval sync` (HR-3); filter vocabulary (HR-5); stale-index degrade-to-filesystem-fallback policy (HR-6); CLI surface `repo-jur retrieval sync|rebuild` + `search|search-diagnose` (HR-7); result limits default 10 / maximum 50 with NO pagination in v1 (HR-8). No Stage 9 task is implemented inside this planning change; implementation is a separately authorized task.

## 1. C9.1 — Lexical index + sync/rebuild: focused tests (TDD — red first)

- [ ] 1.1 Write failing tests for the concept-level lexical index contract: the indexer consumes a canonical bundle root, enumerates only non-reserved `.md` concept documents under `legislacao/`, `jurisprudencia/`, `temas/`, `precedentes/`, derives `concept_id` from the relative path minus `.md`, and produces one derived record per concept; reserved files (`index.md`, `log.md`) and non-Markdown files produce no record; a source-inspection test asserts the index module never imports the converter, engines, inspector, OCR, producer, router, or any LLM/semantic client, and never resolves a process-storage path.
- [ ] 1.2 Write failing tests for Zero-Write during build: building the index from a bundle leaves every bundle file byte-identical (SHA-256 before == after); the derived index is written only under the configured derived root; a derived root resolving inside `bundle/` is rejected via the existing operational-path guard; no alternate write path exists.
- [ ] 1.3 Write failing tests for sync lifecycle operations: CREATE inserts a new `concept_id` record; CONTENT UPDATE reindexes the record; RENAME/MOVE removes the old positional id and inserts the new one; DELETE purges the record; a full sync run with no bundle change is a no-op (no spurious record churn).
- [ ] 1.4 Write failing tests for fingerprint stale detection: each record carries `concept_id`, `content_fingerprint`, `index_schema_version`, `indexer_logical_version`, and `index_config_fingerprint`; the content fingerprint covers the indexed body and retrieval-relevant canonical metadata; a content change flips the fingerprint and classifies the record stale; a schema/logical/config version change invalidates affected records; staleness is observable (fresh/stale/degraded state) and is never silently ignored at query time.
- [ ] 1.5 Write failing tests for full rebuild: rebuild discards derived state, traverses the current bundle, recreates records, never writes into `bundle/` (byte-identity asserted), and produces deterministically equivalent records for identical canonical inputs and indexer logical version; derived data inside the worktree is covered by `.gitignore` (a git-status assertion shows no derived file tracked).

## 2. C9.1 — Lexical index + sync/rebuild: minimal implementation

- [ ] 2.1 Implement `src/pipeline_juridico/retrieval/index.py` with the `LexicalIndexBackend` interface (`build(concepts, config)`, `sync(concepts, config)`, `search(query, filters, limit)`) per tech-spec §10.1, using SQLite FTS5 (stdlib `sqlite3`) as the reference implementation choice; the interface is backend-neutral so a compatible backend can be substituted without changing the bundle, schemas, `concept_id`, or result envelope.
- [ ] 2.2 Implement the deterministic bundle walker and `concept_id` derivation (relative path minus `.md`, no frontmatter duplication, no Stable IDs), restricted to the four Legal type trees; reserved and non-Markdown files are skipped; no process-storage path is ever resolved.
- [ ] 2.3 Implement the sync/rebuild engine: CREATE/CONTENT UPDATE/RENAME/MOVE/DELETE/CONFIG operations, per-record fingerprint computation (content fingerprint over body + retrieval-relevant metadata), and the full rebuild operation (discard derived state, traverse current bundle, recreate, never write into the bundle); derived root validated through `ensure_outside_canonical_bundle` and covered by an additive `.gitignore` entry when inside the worktree.

## 3. C9.2 — Search execution + materialization + fallback: focused tests (TDD — red first)

- [ ] 3.1 Write failing tests for candidate discovery: a query against the concept-level lexical index returns candidate `concept_id`s for literal legal terms, law numbers, CNJ-format identifiers, quoted expressions, and other exact tokens present in the corpus (Exact Retrieval Capability per search-execution memo §3.1); results are bounded by the requested limit; no candidate is derived from process storage.
- [ ] 3.2 Write failing tests for structured filters: filters on the exact public Stage-9 v1 vocabulary (`type`, `status`, `tags`, `lei_numero`, `lei_ano`, `lei_esfera`, `lei_tipo`, `processo_numero`, `tribunal`, `relator`, `data_julgamento`, `ramo_direito`, `precedente_numero`, `precedente_status`, `tema_numero`) are honored as exact filters and map deterministically to their canonical Legal OKF Profile frontmatter fields (public key = canonical field name with the `repo_jur_` prefix stripped for the 12 retrieval-relevant profile fields, e.g. `lei_numero` → `repo_jur_lei_numero`; `type`/`status`/`tags` map to the identically-named canonical keys); a filter on a non-retrieval-relevant field — including any `repo_jur_*` spelling supplied as a public filter key — is a configuration error (never silently ignored, no aliases); no automatic status boost/demote/exclusion occurs when no `status` filter is requested.
- [ ] 3.3 Write failing tests for canonical materialization: before delivery, the materializer confirms the candidate `concept_id` exists in the current bundle, reads the current canonical frontmatter/body, and validates applicable provenance; a stale or missing candidate is not delivered as grounded result and the stale/missing condition is recorded operationally; delivered `text_content` is always the current canonical literal.
- [ ] 3.4 Write failing tests for the result envelope: universal `concept_id` + `text_content` present; `source_refs` preserved when the concept declares sources (per-fragment attribution only when explicitly sustained); exactly 1 PDF → `source_pdf` + `repo_jur_pdf_hash`; 2+ PDFs → `repo_jur_pdf_hashes` mapping `sources[].id` → SHA-256; both fields never coexist; `page_refs` only when the physical association is explicit and unambiguous, all related pages preserved on cross-page fragments, never invented; no fragment-to-source or page-to-source association fabricated.
- [ ] 3.5 Write failing tests for the Direct Read-Only Filesystem Search fallback: fallback triggers when the index is absent, incompatible, corrupt, unavailable during rebuild, or failing initialization; fallback reads the bundle directly, derives `concept_id` from the current path, reads frontmatter from the concept, follows the Retrieval Contract provenance rules, keeps the bundle read-only, and records an observable degraded condition; fallback never promises index-level ranking/performance equivalence.
- [ ] 3.6 Write failing tests for result limits: the validated configuration enforces default limit 10 and maximum limit 50; a request exceeding the configured maximum limit is rejected or clamped; no unbounded result stream is exposed; the search interface and CLI expose no cursor, page token, offset-based paging, or snapshot-paging surface in v1 (pagination is out of scope per HR-8); limits are validated configuration, never silent unlimited.

## 4. C9.2 — Search execution + materialization + fallback: minimal implementation

- [ ] 4.1 Implement `src/pipeline_juridico/retrieval/search.py`: the Lexical-First, Hybrid-Ready orchestrator (query → candidate discovery → structured filters → canonical materialization → Retrieval Contract result envelope), consuming the backend interface and exposing a fusion seam placeholder that remains disabled (no fusion algorithm selected in v1).
- [ ] 4.2 Implement `src/pipeline_juridico/retrieval/materializer.py` per tech-spec §10.5: confirm path exists, read current canonical frontmatter/body, validate required provenance, reject stale candidate text as authority; stale/missing candidates are observable (operational record) and never silently served; re-query/sync or degrade to fallback when necessary.
- [ ] 4.3 Implement `src/pipeline_juridico/retrieval/fallback.py` (Direct Read-Only Filesystem Search) per tech-spec §10.3: Python read-only traversal/search over the bundle, canonical `concept_id` from current path, frontmatter read from the concept, Retrieval Contract provenance, observable degraded mode.
- [ ] 4.4 Implement the retrieval schema/config for structured filters (HR-5 vocabulary: the exact public keys `type`, `status`, `tags`, `lei_numero`, `lei_ano`, `lei_esfera`, `lei_tipo`, `processo_numero`, `tribunal`, `relator`, `data_julgamento`, `ramo_direito`, `precedente_numero`, `precedente_status`, `tema_numero`), including the deterministic public-key → canonical-frontmatter-field mapping (e.g. `lei_numero` → `repo_jur_lei_numero`, `tribunal` → `repo_jur_tribunal`) that is NOT a public alias, rejecting unknown filter keys — including `repo_jur_*` supplied as public filter names — as configuration errors, and the validated result-limit configuration (HR-8: default 10, maximum 50; no pagination surface in v1).

## 5. C9.3 — Chunking + Chunking Profile: focused tests (TDD — red first)

- [ ] 5.1 Write failing tests for the canonical-input contract: the chunker consumes only the current canonical concept body materialized from the bundle, frontmatter is parsed separately and never enters `text_content`; a Phase-1 intermediate artifact is never accepted as chunking source; no OCR, conversion, or evidence resolution occurs.
- [ ] 5.2 Write failing tests for Structural Block-First chunking: chunk boundaries prefer Markdown structural blocks (headings, paragraphs, lists, blockquotes, tables, fenced code blocks); chunks preserve literal contiguous spans; no text is rewritten, reordered, synthesized, or corrected; no heading or table header is artificially duplicated inside `text_content`; derived context (`section_path`, `table_header_context`) is exposed separately and never presented as contiguous text.
- [ ] 5.3 Write failing tests for Page-Aware mapping and size profile: `page_refs` is the ordered union of physical page intervals intersecting the chunk span; no page reference is invented before the first explicit marker; multi-source concepts never get invented page→source mapping; soft-limit behavior closes the chunk at the next appropriate structural boundary; a single unit exceeding the hard limit triggers a deterministic, lossless forced split (no character lost, order preserved, each fragment contiguous); Chunking Profile invariants hold (`soft_limit > 0`, `hard_limit > soft_limit`, deterministic measurement unit).
- [ ] 5.4 Write failing tests for chunk identity and invalidation: no Stable Chunk ID exists; each chunk set carries a derived `chunk_set_fingerprint` (canonical identity/content + Chunking Profile version + Chunking Profile configuration fingerprint + chunker logical version) and each chunk carries a deterministic `chunk_ordinal` and body range; `concept_id` remains the canonical join key; content change, `concept_id` change, Chunking Profile version change, Chunking Profile configuration fingerprint change, or chunker logical version change invalidates/rebuilds affected chunk sets; the canonical bundle is byte-identical after any chunking run.

## 6. C9.3 — Chunking + Chunking Profile: minimal implementation

- [ ] 6.1 Implement `src/pipeline_juridico/retrieval/chunking.py`: canonical-body-only input, structural block parsing (parser is Implementation Choice with a chunker logical version), contiguous grouping under the Chunking Profile, deterministic forced split with derived ranges, page-interval mapping for `page_refs`, and separate derived context fields (`section_path`, `table_header_context`).
- [ ] 6.2 Implement the versioned Chunking Profile model (profile_version, measurement_unit, soft_limit, hard_limit, forced_split_overlap) with validation invariants and the HR-2-approved v1 numeric defaults (`measurement_unit=characters`, `soft_limit=6000`, `hard_limit=12000`, `forced_split_overlap=200`); the values are versioned/configurable defaults — a future numeric change requires a new profile version and/or configuration fingerprint and invalidates/rebuilds affected derived chunk and index state; implement chunk-set fingerprinting/ordinal/body-range derivation and the invalidation rule (profile version/configuration fingerprint/content/concept_id/chunker logical version change invalidates affected chunk sets); persist derived chunk sets under the derived root during `retrieval sync` (HR-3 approved).

## 7. C9.4 — Reranking seam: focused tests (TDD — red first)

- [ ] 7.1 Write failing tests for the disabled default: with the Reranking Profile `enabled: false` or absent, the pipeline proceeds from candidate discovery through structured filtering to canonical materialization with no reranking; the seam exposes `should_rerank(query, candidates, profile)` and `rerank(query, candidates, profile)` interfaces per tech-spec §12.1; a source-inspection test asserts no reranker model/provider/API/GPU reference exists.
- [ ] 7.2 Write failing tests for fail-open behavior: a triggered reranker that times out, raises, or is unavailable discards partial/invalid output, preserves the previous valid order, records `failed_fallback`, and continues to canonical materialization; a fake adapter can be applied and produces the new order with `applied` state; bypass by trigger policy records `bypassed`; the four observability states `disabled|bypassed|applied|failed_fallback` are distinguishable.
- [ ] 7.3 Write failing tests for relevance/trust separation: no automatic relevance multipliers from `trust_tier`, `verified`, `status`, `repo_jur_verification_history`, or temporal decay; reranking never removes, deprecates, or deletes candidates from the index or bundle; reranking never writes to the bundle, frontmatter, or canonical content; Reranking Profile changes affect runtime only (bundle and lexical content byte-identical after profile change).

## 8. C9.4 — Reranking seam: minimal implementation

- [ ] 8.1 Implement `src/pipeline_juridico/retrieval/reranking.py`: the seam (`should_rerank`/`rerank`), the versioned Reranking Profile model (enabled default false; trigger/candidate/execution policy; implementation handle), fail-open orchestration (discard partial output → preserve valid order → record state → continue materialization), and observability state recording; no neural/LLM reranker, no provider, no mandatory intent classifier, no thresholds as architecture requirements.

## 9. Integration and validation

- [ ] 9.1 Add the additive `RetrievalConfig` to `config.py` (derived root, filter vocabulary schema reference, result-limit defaults default 10 / maximum 50 per HR-8 with no pagination surface in v1, env overrides) without changing any existing config field, and the additive `.gitignore` entry for the derived root when it resolves inside the worktree.
- [ ] 9.2 Implement the additive operational CLI: `repo-jur retrieval sync|rebuild` and `repo-jur search|search-diagnose` registered on the `repo-jur` entry (`domain_router_cli.py`), following the established conventions (argparse, environment-driven derived root, deterministic exit codes 0/1/2/3/5, sanitized logging, atomic observability writes); `converter-juridico`, `repo-jur route`, `repo-jur producer`, and `repo-jur process` surfaces remain byte-identical (CLI non-regression tests).
- [ ] 9.3 Implement retrieval observability records (index fresh/stale/degraded; sync/rebuild events; reranking states; materialization stale/missing) under the derived root, content-safe (no document content, no full critical identifier values, no secrets), outside `bundle/` and outside Phase-1 artifacts.
- [ ] 9.4 Run the focused retrieval test modules, the full repository suite (888 baseline must stay green), and `openspec validate --all --strict`; verify `git status` shows only the intended Stage-9 files (fixtures and derived data remain untracked); confirm zero production-code and zero test-code changes outside the Stage-9 implementation scope; confirm no Stage-10-or-later work is introduced.
