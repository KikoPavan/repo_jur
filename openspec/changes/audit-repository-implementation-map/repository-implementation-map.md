# Repository Implementation Map — Stage 0

Mudança: `audit-repository-implementation-map`. Diagnóstico exclusivo do estado físico real de `repo_jur` em 2026-08-15, HEAD `e1329aa` (branch `main`). Nenhum código, teste, configuração ou diretório foi criado, movido ou alterado por esta investigação.

> **Nota de reconciliação (segunda sessão, mesmo dia):** as seções `0`–`12` abaixo são o inventário original do Stage 0, produzido antes de `docs/baselines/frozen/` existir fisicamente no repositório. Elas permanecem como evidência do estado físico do código (não foram refeitas). A seção **`R. Reconciliação com Baselines FROZEN`**, inserida logo a seguir, cruza esse mesmo inventário contra as 24 baselines FROZEN agora disponíveis, corrige a contagem total de capacidades (23 → **24**, causa explicada em `R.0`), altera 2 classificações (`REUSE → ADAPT`) e substitui a ordem de Stages recomendada pela sequência oficial agora encontrada nas próprias baselines. Onde `R.` e o inventário original divergirem, `R.` é a versão vigente.

## R. Reconciliação com Baselines FROZEN

### R.0 — Causa da inconsistência "23 vs 24" e total correto

O relatório original desta mudança descreveu a tabela principal como tendo **23 capacidades** (a numeração 1–23 do inventário obrigatório da tarefa), mas a seção "Capacidades por estado" somou `8 REUSE + 6 ADAPT + 9 CREATE = 23` e depois adicionou, **fora da tabela numerada**, uma nota separada classificando `Judicial Process Retrieval` como `OUT_OF_SCOPE` — um 24º item classificado que nunca foi promovido a linha própria da tabela nem contado no total declarado. A causa raiz é essa: uma classificação real (`OUT_OF_SCOPE`) ficou registrada em prosa, não na tabela, e o texto continuou a afirmar "23" apesar de terem sido efetivamente classificados 24 itens.

**Total correto: 24 capacidades.** As 23 do inventário obrigatório da tarefa original (`ITP/Ingress` … `schemas/contracts comuns`) **mais** `Judicial Process Retrieval`, exigida como classificação explícita pela regra especial "Judicial Process Retrieval = OUT_OF_SCOPE" tanto do `/goal` original quanto de `decision-memo-retrieval-bounded-context-scope-v1.0-FROZEN.md` e da Arquitetura v15 (invariantes 65–67). A seção `R.1` abaixo promove esse item a linha própria (nº 24) da tabela reconciliada.

Investigado e descartado como causa alternativa: **Stable Concept Identity** (`decision-memo-stable-concept-identity-v1.0-FROZEN.md`, Modelo A — Identidade Posicional Pura, CLOSED) não é uma 25ª capacidade. É uma regra de schema que o futuro Legal Producer (capacidade 16) deve seguir — não um componente físico separado a inventariar; nenhuma implementação de código lhe corresponde isoladamente.

### R.1 — Tabela de reconciliação

Baselines lidas integralmente nesta reconciliação: `arquitetura-fase2-repo-jur-v15-FROZEN.md`, `technical-implementation-spec-repo-jur-v1.2-FROZEN.md`, `implementation-plan-repo-jur-v1.1-FROZEN.md`, `implementation-plan-correction-impact-v1.0-FROZEN.md`, `decision-memo-phase1-quality-gate-v1.0-FROZEN.md`, `decision-memo-ingress-transport-protocol-v1.0-FROZEN.md`, `external-source-ingestion-contract-v1.6-FROZEN.md`, `decision-memo-pdf-source-cardinality-v1.0-FROZEN.md`, `decision-memo-duplicate-act-handling-v1.0-FROZEN.md`, `lifecycle-field-ownership-v1.4-FROZEN.md`, `concept-identity-physical-structure-v1.3-FROZEN.md`, `decision-memo-stable-concept-identity-v1.0-FROZEN.md`, `decision-memo-physical-layout-logical-capability-mapping-v1.0-FROZEN.md`, `decision-memo-shared-conversion-core-bounded-contexts-v1.0-FROZEN.md`, `decision-memo-semantic-review-enrichment-layer-v1.1-FROZEN.md`, `decision-memo-post-ocr-critical-data-validation-seam-v1.1-FROZEN.md`, `decision-memo-retrieval-bounded-context-scope-v1.0-FROZEN.md` (16 de 24 arquivos — os 8 restantes, `legal-okf-profile-v1.3`, `phase1-operational-spec-v1.1`, `retrieval-contract-v2.8`, `decision-memo-chunking-strategy-v1.0`, `decision-memo-reranking-pipeline-v1.0`, `decision-memo-search-execution-path-v1.0`, `decision-memo-verification-history-schema-v1.0`, não foram lidos integralmente nesta sessão porque seu conteúdo normativo já está resumido com suficiente precisão pela Arquitetura v15 §8 + invariantes 1–67 e pela Technical Implementation Spec §6/§10–13, ambas lidas por completo; nenhuma classificação abaixo depende de um trecho não coberto por essas duas baselines-mestre).

| # | Capacidade | Baseline aplicável | Implementação física | Antes | Depois | Lacuna | Evidência/justificativa |
|---|---|---|---|---|---|---|---|
| 1 | ITP / Ingress | `decision-memo-ingress-transport-protocol-v1.0`, ESIC v1.6 §4 | `cli.py::main` (parsing de argv/`.env`), `inspector.py::validate_pdf_path`/`open_pdf` | ADAPT | ADAPT (lacuna precisada) | ITP/1.0 exige envelope ZIP versionado (`manifest.json` + `evidence.pdf`), `handoff_id` opaco, canal filesystem inbox com rename atômico no mesmo filesystem e retry/idempotência por `handoff_id`. **Nada disso existe** — o CLI recebe um caminho de PDF cru como argumento posicional, sem envelope, manifesto ou protocolo de handoff. `inspector.open_pdf` é reutilizável como a metade "receptor já com PDF em mãos" do fluxo, não como o ingress em si. | ITP memo §5–§9; ESIC §4. Confirmado por leitura completa de `cli.py` (nenhuma menção a ZIP/manifest/handoff) e `inspector.py` (só abre um path já resolvido). |
| 2 | Preflight | ITP memo §8, ESIC §5 | `inspector.open_pdf` (criptografia/vazio), `RoutingConfig.__post_init__` | ADAPT | ADAPT (lacuna precisada) | Preflight FROZEN inclui validação de container ZIP, central directory, limites configuráveis de tamanho/ratio, validação UTF-8+JSON do manifesto, comparação de `candidate_sha256` — nenhum existe, porque não há envelope a validar. Sobrevive como reutilizável: a validação estrutural de PDF (`open_pdf` recusa criptografado/vazio) corresponde exatamente ao item 12 da ordem de preflight do Technical Spec §7.2 ("structural PDF compatibility validation"). | Technical Spec §7.2 (13 passos de preflight); ITP memo §8. |
| 3 | official receiver SHA-256 | ITP memo invariante 6; ESIC §5.2 | `hashing.sha256_file`, `inspector.inspect_source` | REUSE | REUSE (confirmado) | Nenhuma. FROZEN exige "SHA-256 oficial recalculado pelo receptor" sobre os bytes exatos recebidos — exatamente o que `sha256_file` faz, computado no momento do recebimento, nunca confiando em hash externo. Único ajuste (não é lacuna de comportamento, é de sequência): hoje roda direto sobre o path do CLI; quando o ITP existir, deve rodar sobre `evidence.pdf` já extraído do envelope, no mesmo ponto lógico. | ITP memo invariante 6, §7.6; ESIC §5.2. |
| 4 | Evidence Preservation | ITP memo §11; ESIC §3.4; Arquitetura v15 §8.1 | Design não-destrutivo (PDF de origem nunca escrito), `isolated_page_workspace`, `write_atomic` | ADAPT | ADAPT (lacuna ampliada) | FROZEN exige que a evidência aceita seja preservada em **Object Storage externo, com referência estável e resolvível, antes da Fase 1** — nenhuma integração com Object Storage existe; o PDF permanece apenas no filesystem local de onde o CLI foi invocado, sem etapa de preservação/registro formal. Esta lacuna é maior do que o relatório original descreveu (que tratou "nunca alterar o PDF de origem" como quase-satisfazendo a capacidade); FROZEN pede um componente de infraestrutura adicional (`ObjectStorageGateway.put(...)`, Technical Spec §7.6) que hoje não existe em nenhuma forma. | ITP memo §11; Technical Spec §7.6 (`ObjectStorageGateway`). |
| 5 | Shared Conversion Core | `decision-memo-shared-conversion-core-bounded-contexts-v1.0`; `decision-memo-physical-layout-logical-capability-mapping-v1.0`; Arquitetura v15 §1.1/§2.1; Technical Spec §3.1 | `src/pipeline_juridico/` completo | REUSE | REUSE (confirmado, com ressalva) | Nenhuma quanto à identidade física: as três baselines mais diretamente sobre este ponto confirmam textualmente `src/pipeline_juridico/` como "fato físico atualmente confirmado" a ser reutilizado, proibindo qualquer `pipeline/shared_conversion/` paralelo. **Ressalva:** a própria Arquitetura v15 §1.1 lista a validação de dados críticos e o Quality Gate como responsabilidades do Shared Conversion Core — a primeira é `CREATE` (linha 12) e a segunda é `ADAPT` (linha 13); "REUSE" aqui descreve o substrato físico reutilizável, não que 100% das responsabilidades FROZEN do Shared Conversion Core já estejam implementadas. | Physical Layout memo (integral); Shared Conversion Core memo (integral); Arquitetura v15 §1.1, §2.1, invariantes 47/59/60. |
| 6 | `ConversionEngine` / interface | Shared Conversion Core memo; Technical Spec §8.1; Implementation Plan §5 | `converter.py::convert_document` + `engines.py` (fábricas) | ADAPT | ADAPT (agora mandatório, não opcional) | FROZEN nomeia literalmente a interface exigida: `class ConversionEngine: def convert(self, evidence_ref, config) -> Phase1Artifacts: ...` (Technical Spec §8.1). Diferente da leitura original ("formalizar só se um segundo motor for necessário"), a baseline trata isso como parte obrigatória do Stage 3, embora explicitamente **sem exigir reescrita** ("o adapter pode ser uma interface/facade mínima em torno da implementação existente" — Implementation Plan §5; critério de aceite 31 do Technical Spec: "reused... without rewrite requirement"). | Technical Spec §8.1, critério de aceite 31; Implementation Plan §5, Stage 3. |
| 7 | MarkItDown / markitdown-ocr | Technical Spec §19 (Implementation Choices) | `pyproject.toml`, `engines.py` | REUSE | REUSE (confirmado) | Nenhuma. Technical Spec §19 lista textualmente "Existing MarkItDown/markitdown-ocr adapter behind `ConversionEngine`" como a "Current Implementation Choice" — não uma dependência arquitetural congelada, mas o estado atual explicitamente aceito. | Technical Spec §8.2, §19. |
| 8 | Gemini/OpenAI-compatible client | Technical Spec §8.2, §19 | `engines.py::create_ocr_engine`, `cli.py::GEMINI_OPENAI_BASE_URL` | REUSE | REUSE (confirmado) | Nenhuma. FROZEN trata isto explicitamente como Implementation Choice atual, não como requisito arquitetural — "isso não congela Gemini, MarkItDown ou o cliente de compatibilidade como dependências arquiteturais" (Technical Spec §8.2). | Technical Spec §8.2. |
| 9 | OCR routing | Arquitetura v15 §1.1 ("OCR fallback quando necessário") | `router.py::route_page` | REUSE | REUSE (confirmado) | Nenhuma. Faz parte das responsabilidades listadas do Shared Conversion Core; FROZEN explicitamente não congela threshold de caracteres, provider ou modelo de OCR (Technical Spec §8.3) — o roteamento por sinal já implementado é compatível com essa neutralidade. | Technical Spec §8.3. |
| 10 | page markers | Technical Spec §8.4; Quality Gate memo §9.1 | `converter.py::format_page_marker`, `validator.py::validate_page_markers` | REUSE | REUSE (confirmado) | Nenhuma. Regra FROZEN exata: exatamente um marcador por página física, ordem 1..N, página vazia preserva o marcador — comportamento já implementado e testado. | Technical Spec §8.4; Quality Gate memo §9.1–9.2, invariantes 1–3. |
| 11 | Technical JSON/report | Technical Spec §6.3; Quality Gate memo §11 | `report.py`, `models.py::Relatorio`/`ResultadoPagina` | REUSE | **ADAPT (classificação alterada)** | FROZEN define uma forma aninhada específica e obrigatória: `schema_version`, `execution_id`, `input.{sha256,byte_size,page_count}`, `phase1.{implementation,implementation_version,logical_processing_version,relevant_config_fingerprint}`, `result.{quality_gate: "PASS"\|"PASS_WITH_WARNINGS"\|"FAIL", warnings, errors}`, `artifacts.{markdown_sha256}`, `pages[].{page_number,method,char_count,warnings,errors}`, `telemetry{}`. O `Relatorio` atual cobre boa parte do espírito (schema_version, run_id≈execution_id, pages com method/characters/warnings), mas: (a) não tem `logical_processing_version` nem `relevant_config_fingerprint`; (b) `status` usa o vocabulário `sucesso/incompleto/falha`, não `PASS/PASS_WITH_WARNINGS/FAIL`; (c) não há envelope `telemetry`. Extensão in-place de `models.py`/`report.py`, não recriação. | Technical Spec §6.3 (schema completo); Quality Gate memo §11.2. |
| 12 | Post-OCR Critical-Data Validation | `decision-memo-post-ocr-critical-data-validation-seam-v1.1`; Technical Spec §3.6, §8A | Nenhuma (confirmado) | CREATE | CREATE (interface e teste-invariante agora precisos) | Interface exigida: `class CriticalDataValidator: def validate(self, phase1_artifacts, profile) -> CriticalValidationResult`, resultado em `{status: OK\|WARNING\|REVIEW_REQUIRED, findings: []}`. Cada regra determinística exige proveniência versionada (`rule_id`, `rule_version`, `source/specification`, `validation_logic_version`) — proibido generalizar a partir de um único documento (relevante ao achado do selo digital em `audit-ocr-critical-data-fidelity`, commit `4759696`). Teste-invariante explícito da própria baseline: `SHA256(markdown_before) == SHA256(markdown_after)` (a seam nunca muta o literal). Pertence dentro de `src/pipeline_juridico/` (Shared Conversion Core), não em módulo à parte. | Critical-Data memo v1.1 (integral); Implementation Plan §6 (Stage 4), incluindo o teste-invariante SHA-256. |
| 13 | Phase 1 Quality Gate | `decision-memo-phase1-quality-gate-v1.0` (integral); ESIC §6.6; Technical Spec §8.5 | `validator.py` + `cli.py::main` | REUSE | **ADAPT (classificação alterada)** | Vocabulário normativo exato exigido: exatamente três resultados, `PASS` / `PASS WITH WARNINGS` / `FAIL` (serializados `PASS`/`PASS_WITH_WARNINGS`/`FAIL`). O código atual usa `StatusExecucao.{sucesso,incompleto,falha}` — vocabulário diferente, sem mapeamento 1:1 declarado. Mudança estrutural também relevante: o gate FROZEN é uma função que **retorna** um resultado tri-state (`evaluate_phase1(...) -> PASS\|PASS_WITH_WARNINGS\|FAIL`, Technical Spec §8.5); `validator.py` hoje é baseado em **exceções** (`MarkdownValidationError`), não em um objeto de resultado explícito — mesmo espírito determinístico, forma diferente. Confirma-se que OCR bem-sucedido pode dar `PASS` (já é o comportamento atual, via `verify_ocr_evidence`) e que página vazia preserva marcador sem warning obrigatório (já correto em `Metodo.vazia`) — esses dois pontos **não** mudam de classificação, só o envelope de estado/vocabulário. | Quality Gate memo §5–§9 (integral); Technical Spec §8.5 (pseudocódigo `evaluate_phase1`). |
| 14 | Domain Router | Technical Spec §3.2, §8B; Implementation Plan §8 (Stage 6) | Nenhuma (confirmado) | CREATE | CREATE (precedência e alvo exatos) | Alvos exatos: `legal_knowledge` \| `judicial_process` \| `review_required`. Regra de precedência explícita: se o gate físico = `FAIL`, o fluxo para antes do router; se o resultado crítico (capacidade 12) = `REVIEW_REQUIRED`, roteia para `review_required` independentemente do domínio. Proibido classificador semântico oculto (LLM), enrichment ou geração de YAML nesta etapa. Risco de colisão de nome com `router.py` (que roteia MÉTODO de página, não domínio de documento) confirmado e reafirmado — nenhum código do `router.py` atual satisfaz esta capacidade. | Technical Spec §3.2, §8B; Implementation Plan §8. |
| 15 | Legal Semantic Review | `decision-memo-semantic-review-enrichment-layer-v1.1` (integral); Technical Spec §3.5, §8C | Nenhuma (confirmado); planejamento em `docs/Pipeline_Conversao_Juridica_Corrigido.md` §20 | CREATE | CREATE (10 invariantes precisos) | Interface: `class SemanticReviewEngine: def review(self, phase1_artifacts, domain_profile) -> ReviewResult`. Markdown da Fase 1 é imutável para esta camada; correções apenas estruturais preservam todas as palavras originais; preferir patches `{before, after, reason, confidence, page_refs, evidence_refs}`; ambiguidade → `REVIEW_REQUIRED`; **nunca publica** (autoridade exclusiva do Producer). `Papel/Nome` pertence explicitamente aqui, não ao conversor determinístico — confirma e formaliza a decisão arquitetural já registrada em `docs/Pipeline_Conversao_Juridica_Corrigido.md` §20 (2026-08-10). | Semantic Review memo v1.1 (10 invariantes, integral); Technical Spec §3.5, §8C. |
| 16 | Legal Producer | Arquitetura v15 §4 (fluxo de 9 passos); Legal OKF Profile v1.3 (não lido integralmente nesta sessão, citações via Concept Identity/Lifecycle); `lifecycle-field-ownership-v1.4`; `concept-identity-physical-structure-v1.3`; `decision-memo-pdf-source-cardinality-v1.0`; `decision-memo-duplicate-act-handling-v1.0` | Nenhuma (confirmado) | CREATE | CREATE (contrato completo agora mapeado) | É de longe a capacidade com maior superfície normativa entre as `CREATE`. Resumo do contrato mínimo: (a) fluxo de publicação em 9 passos (carregar concept existente → resolver identidade/proveniência → detectar mudança → recomputar Producer-Owned → merge Shared/Human-Owned → cardinalidade PDF → `verified`+histórico+body ownership → validar OKF+repo_jur → publicação atômica); (b) `concept_id` é posicional (deriva do path, nunca duplicado no YAML — Stable Concept Identity, CLOSED, Modelo A); (c) `repo_jur_pdf_hash` (1 PDF) `xor` `repo_jur_pdf_hashes` (2+ PDFs, mapeado por `sources[].id`); (d) matriz de field ownership de `lifecycle-field-ownership-v1.4` §3 (`type`/`sources`/`generated`/hashes = Producer-Owned; `title`/`description`/`tags` = Shared Ownership; `status`/`verified` = Human-Owned; nunca sobrescrever silenciosamente Human-Owned); (e) Duplicate Act Handling conservador — fusão automática só com equivalência lógica/material segura, ambiguidade → `HUMAN REVIEW`, nunca `_v2` automático; (f) estrutura física obrigatória do bundle (`legislacao/`, `jurisprudencia/`, `temas/`, `precedentes/`, regras de slug determinístico por tipo). **`repo_jur/bundle/` não existe fisicamente neste repositório hoje** (confirmado por `find` no Stage 0) — sua criação é parte da implementação desta capacidade, não uma capacidade própria. | Arquitetura v15 §4, §8.2–§8.7, invariantes 1–21; Lifecycle v1.4 (integral); Concept Identity v1.3 (integral); Stable Concept Identity memo (integral); PDF Cardinality memo (integral); Duplicate Act Handling memo (integral). |
| 17 | Process Semantic Review | Implementation Plan §10 (Stage 8) | Nenhuma (confirmado) | CREATE | CREATE (confirmado, ainda sem decisão própria) | Diferente da linha 15, **não existe nenhuma decisão arquitetural dedicada ao lado Process** equivalente ao Semantic Review memo v1.1 do lado Legal — apenas a instrução geral de que "Semantic Review processual obedece às mesmas garantias de preservação do literal Phase 1" (Implementation Plan §10). Antes de qualquer implementação, precisaria de sua própria decisão de schema/profile, análoga à que já existe para o lado Legal. | Implementation Plan §10. |
| 18 | Process Producer | Implementation Plan §10 | Nenhuma (confirmado) | CREATE | CREATE (confirmado) | Mesma ausência da linha 16, sem nenhum fluxo de 9 passos equivalente documentado para o lado Process; a única garantia FROZEN é negativa: nunca grava no bundle Legal, nunca usa o Legal OKF schema. | Implementation Plan §10; Shared Conversion Core memo. |
| 19 | Process Storage | Implementation Plan §13 (tabela) | Nenhuma (confirmado) | CREATE | CREATE (confirmado) | Implementation Plan §13 classifica textualmente como "configuração/provider ainda domain-specific... adapter separado; no Legal bundle" — a própria baseline confirma que a decisão de armazenamento ainda está em aberto de implementação, não apenas ausente de código. | Implementation Plan §13. |
| 20 | Legal Knowledge Retrieval | Arquitetura v15 §8.5, invariantes 27–39; Technical Spec §10 | Nenhuma (confirmado) | CREATE | CREATE (interfaces exatas mapeadas) | Interface: `class LexicalIndexBackend: def build/sync/search(...)`. SQLite FTS5 é "reference implementation choice", não requisito. Fallback normativo: Direct Read-Only Filesystem Search. Filtros mínimos: `type`/`status`/`tags`, sem boost/demote automático. Materializador canônico de 5 passos (resolve `concept_id` → confirma path atual → lê frontmatter/corpo atuais → valida proveniência → rejeita candidato stale). Zero-Write reafirmado. Nenhum vector DB/embedding é escolhido ou requerido. | Technical Spec §10 (integral); Arquitetura v15 invariantes 27–39. |
| 21 | chunking/reranking | Arquitetura v15 §8.5, invariantes 33–45; Technical Spec §11–§12 | Nenhuma (confirmado) | CREATE | CREATE (algoritmo e seam exatos mapeados) | Chunking: `corpo canônico → mapa de intervalo de página → blocos estruturais → agrupamento contíguo sob Chunking Profile versionado → split forçado só quando necessário → registro de chunk derivado`; `text_content` sempre span literal contíguo; nenhum Stable Chunk ID. Reranking: seam `should_rerank`/`rerank`, `enabled: false` por padrão, Fail-Open (preserva ordem válida anterior em erro/timeout), proibido blending automático de `trust_tier`/`verified`/`status` na relevância. Nenhum classificador de intenção, Cross-Encoder, modelo, provider, GPU ou threshold é exigido. | Technical Spec §11 (chunking), §12 (reranking); Arquitetura v15 invariantes 33–45. |
| 22 | observability/logging | Technical Spec §15 | `cli.py` (`logging.basicConfig`, `_sanitize_log_message`), `report.py::TimingInfo` | ADAPT | ADAPT (superfície exigida muito maior) | FROZEN exige categorias específicas de evento por etapa — Ingress (handoff aceito/rejeitado/conflito, categoria de falha de preflight, hash oficial, resultado de preservação), Phase 1 (execution ID, método/resultado por página, warnings/errors, resultado do gate, timing), Producer (resultado de resolução, exigência de revisão humana, categoria de decisão de materialidade, resultado de publicação), Retrieval (índice fresh/stale/degraded, sync/rebuild, estado de reranking, materialização stale/missing). O código atual cobre uma fração pequena disso (log de erro + redação de segredos + timing por execução) — lacuna real bem maior do que o relatório original descreveu, mas a base (redação de segredos já testada, nunca logar credenciais) é diretamente extensível, não descartável. | Technical Spec §15 (integral). |
| 23 | schemas/contracts comuns, Legal e Process | Technical Spec §3 (Stage 1 "Contract Harness") | `report.py::validate_report_contract`, `models.py` | ADAPT | ADAPT (escopo formal ampliado) | Technical Spec §3 nomeia "common contracts" como sua própria capacidade lógica de Stage 1: Actor validation (`human:<id>`/`process:<id>`/`<producer>/<version>`), evidence references, tipos de artefato da Fase 1, estados do gate, `CriticalValidationResult`, `RouteTarget`, guards de Zero-Write. O único contrato real hoje é o schema do relatório técnico (`validate_report_contract`) — cobre uma fatia estreita da superfície agora formalmente exigida; nenhuma validação de Actor, nenhum `RouteTarget`, nenhum `CriticalValidationResult` existe. Continua sendo o ponto de partida correto para extensão (mesmo padrão de validação declarativa), não uma reescrita. | Technical Spec §3 (Stage 1, "Contract Harness"). |
| 24 | Judicial Process Retrieval | `decision-memo-retrieval-bounded-context-scope-v1.0` (integral); Arquitetura v15 invariantes 65–67; Implementation Plan §11 | N/A — regra especial, não inventariada por código | (fora da tabela original) | **OUT_OF_SCOPE (promovido à tabela)** | Confirmado por 3 baselines independentes: o Retrieval Contract vigente (v2.8) atende exclusivamente Legal Knowledge/`repo_jur/bundle/`; Judicial Process Retrieval "exige contrato/decisão própria futura"; proibido índice compartilhado misturando os dois domínios. Nenhuma investigação de implementação foi feita — classificação aplicada exatamente como instruído, sem inspeção adicional, em ambas as sessões. | Retrieval Bounded-Context Scope memo (integral); Arquitetura v15 invariantes 65–67; Implementation Plan §11 "Out of scope". |

### R.2 — Classification Changes After FROZEN Baseline Reconciliation

Apenas 2 classificações mudaram de valor (ambas `REUSE → ADAPT`); todas as demais 22 foram confirmadas, não alteradas — a maioria com lacunas descritas de forma bem mais precisa do que na sessão original, mas sem mudar de categoria.

1. **#11 Technical JSON/report: REUSE → ADAPT.** Causa: `technical-implementation-spec-repo-jur-v1.2-FROZEN.md` §6.3 define um schema aninhado obrigatório (`execution_id`, `input.*`, `phase1.*` com `logical_processing_version`/`relevant_config_fingerprint`, `result.quality_gate` no vocabulário `PASS`/`PASS_WITH_WARNINGS`/`FAIL`, `artifacts.markdown_sha256`, `telemetry{}`) que `models.py::Relatorio` não replica campo a campo, em especial o vocabulário de status e os dois fingerprints de versão/config. O relatório técnico atual é um REUSE genuíno em espírito (schema versionado, sem segredos, warnings por página), mas não em forma exata.
2. **#13 Phase 1 Quality Gate: REUSE → ADAPT.** Causa: `decision-memo-phase1-quality-gate-v1.0-FROZEN.md` exige exatamente três resultados normativos serializados (`PASS`/`PASS_WITH_WARNINGS`/`FAIL`) retornados por uma função de avaliação determinística. `validator.py` hoje é baseado em exceções (`MarkdownValidationError`) sobre um vocabulário de status diferente (`sucesso`/`incompleto`/`falha`, em `StatusExecucao`), não em um resultado tri-state explícito nesse vocabulário. O comportamento determinístico subjacente (OCR bem-sucedido pode passar; página vazia não gera warning obrigatório) já está correto e não muda.

Nenhuma capacidade `CREATE` foi rebaixada para `REUSE`/`ADAPT`, e nenhuma `REUSE`/`ADAPT` foi promovida a `CREATE` — a inspeção de código do Stage 0 (nenhuma implementação de Domain Router, Semantic Review, Producer, Storage ou Retrieval) permanece confirmada integralmente pelas baselines agora lidas.

### R.3 — Confirmações específicas pedidas pela tarefa

- **`src/pipeline_juridico/` continua REUSE/ADAPT sem relocação:** confirmado três vezes de forma independente (Physical Layout memo, Shared Conversion Core memo, Technical Spec §3.1/§4) — nenhuma baseline pede ou permite mover/duplicar o pacote.
- **Ingress/Preflight/Evidence Preservation contra ITP/ESIC:** confirmado ADAPT nas 3 linhas (1/2/4); lacuna real é a ausência total do envelope ITP/1.0 e da integração com Object Storage, não do conceito de ingresso/validação em si.
- **Technical JSON e Quality Gate contra Phase1 specs:** ambos mudaram de REUSE para ADAPT (linhas 11/13) — vocabulário e forma do schema, não a lógica de negócio subjacente.
- **Critical Validation contra memo v1.1:** confirmado CREATE (linha 12), agora com interface, resultado tri-state e teste-invariante SHA-256 exatos.
- **Semantic Review Legal/Process contra invariantes v1.1:** confirmado CREATE para ambos (linhas 15/17), com assimetria real — o lado Legal já tem decisão arquitetural completa (10 invariantes), o lado Process ainda não tem nenhuma.
- **Legal/Process Producer e schemas separados:** confirmado CREATE para ambos (linhas 16/18); isolamento de schema entre domínios é regra FROZEN explícita (Shared Conversion Core memo, "Prohibited Coupling") e nenhum dos dois Producers existe.
- **Legal Retrieval contra Retrieval v2.8:** confirmado CREATE (linha 20), escopo restrito a Legal Knowledge/`bundle/` confirmado.
- **Judicial Process Retrieval = OUT_OF_SCOPE:** confirmado e promovido a linha própria (24) nesta reconciliação.
- **chunking/reranking somente conforme escopo FROZEN:** confirmado CREATE (linha 21); nenhum vector DB, embedding, Cross-Encoder ou reranker específico é exigido — reforça que uma implementação futura não deve adicionar essas dependências além do que as baselines pedem.

### R.4 — Ordem de Stages: correção pela sequência oficial agora encontrada

A recomendação original do Stage 0 (seção 11, abaixo) foi **inferida** a partir das lacunas observadas, na ausência de qualquer baseline FROZEN física no repositório. As baselines agora disponíveis definem essa sequência de forma **explícita e idêntica em dois documentos independentes** (`technical-implementation-spec-repo-jur-v1.2-FROZEN.md` §18 e `implementation-plan-repo-jur-v1.1-FROZEN.md` §2):

```text
Stage 0  Repository Implementation Map        (esta mudança)
Stage 1  Contract Harness
Stage 2  ITP / Ingress / Preflight / Evidence Preservation
Stage 3  Shared Conversion Core (wrap via ConversionEngine)
Stage 4  Post-OCR Critical-Data Validation Seam
Stage 5  Phase 1 Quality Gate
Stage 6  Domain Router
Stage 7  Legal Knowledge Pipeline (Semantic Review + Producer)
Stage 8  Judicial Process Pipeline (Semantic Review + Producer)
Stage 9  Legal Knowledge Retrieval
Stage 10 Conformance / Regression
```

Esta sequência **substitui** a ordem recomendada na seção 11 original, que havia colocado Critical-Data Validation (aqui, Stage 4) como prioridade máxima antes de Ingress/Evidence Preservation (Stage 2) e do wrap do `ConversionEngine` (Stage 3) — uma divergência real entre a inferência da primeira sessão e a sequência normativa agora confirmada.

### R.5 — Primeiro Stage seguro recomendado (corrigido)

**Stage 1 — Contract Harness.** É o próximo passo pela sequência oficial: "reutilizar/estabelecer contratos realmente comuns" (Actor validation, evidence references, tipos de artefato da Fase 1, estados do gate, `CriticalValidationResult`, `RouteTarget`, guards de Zero-Write) — trabalho majoritariamente de consolidação sobre o que já é `REUSE`/`ADAPT` (linha 23), sem depender de nenhuma capacidade ainda `CREATE`.

Para quem procura o primeiro Stage que exige **código genuinamente novo em volume**: **Stage 2 — ITP / Ingress / Preflight / Evidence Preservation** (linhas 1/2/4) — é onde a maior lacuna concreta do repositório está hoje (nenhum envelope ITP/1.0, nenhuma integração com Object Storage), e é pré-requisito explícito de todo o resto da sequência FROZEN antes mesmo do Shared Conversion Core ser formalmente invocado como tal.

**Correção explícita:** Post-OCR Critical-Data Validation (linha 12), recomendada como "primeiro Stage seguro" no relatório original desta mesma mudança, é Stage 4 na sequência oficial — depois de Ingress/Preflight/Evidence Preservation e do wrap do `ConversionEngine`. A motivação original (achado real já confirmado, extensão natural de `validator.py`, zero dependência de capacidade ausente) continua tecnicamente válida como justificativa de *prioridade de valor*, mas a sequência FROZEN é uma ordem de **dependência arquitetural declarada**, não apenas de valor esperado, e deve prevalecer.

---

## 0. Precheck

- **Branch / HEAD:** `main` @ `e1329aa53b221c054b0eeed10dfb97f6259091fe`.
- **`git status --short --untracked-files=all` (antes desta mudança):** apenas os 2 artefatos pré-existentes não rastreados de sempre — `var/ocr_final/logs/012-015-Testamento Publico.report.json` e `var/ocr_final/output/012-015-Testamento Publico.md` (execução real de 2026-08-14, anterior a qualquer investigação recente; não gerados por esta tarefa).
- **OpenSpec ativo:** nenhum (`openspec list` → "No active changes found" antes de esta mudança ser criada).
- **Baseline da suíte:** `uv run pytest tests/` → **391 passed**, 0 failed.
- **Baseline OpenSpec:** `openspec validate --all --strict` → **1 passed**, 0 failed (`spec/juridical-pdf-conversion`).
- **`pyproject.toml`:** projeto `repo-jur`, Python `>=3.12`, gerenciado por `uv`/hatchling, layout `src/pipeline_juridico`. Dependências: `google-genai`, `markitdown-ocr`, `markitdown[all]`, `openai`, `pillow`, `pymupdf`, `python-dotenv`. Dev: `pytest`, `pytest-cov`. Entry point único: `converter-juridico = "pipeline_juridico.cli:main"`.
- **`.env.example`:** `INPUT_DIR`, `OUTPUT_DIR`, `LOGS_DIR`, `TEMP_DIR`, `GEMINI_API_KEY`, `GEMINI_MODEL`, `GEMINI_BASE_URL`, `OCR_ENABLED`, `OCR_PROMPT_FILE`, `NATIVE_MIN_TEXT_CHARS`, `FULL_PAGE_IMAGE_MIN_RATIO`, `SIGNIFICANT_IMAGE_MIN_RATIO` (nenhum valor de segredo lido ou registrado).
- **`/bundle/`:** não existe no repositório — nada a preservar/evitar nesta árvore.

## 1. Árvore física relevante (real, não a de nenhum diagrama)

```
repo_jur/
├── src/pipeline_juridico/        # único pacote de produção (2421 linhas, 11 módulos)
│   ├── cli.py                    # entrypoint converter-juridico
│   ├── config.py                 # RoutingConfig
│   ├── inspector.py              # abertura/validação de PDF, isolamento de páginas, hash de origem
│   ├── router.py                 # roteamento de MÉTODO de conversão por página
│   ├── engines.py                # fábricas MarkItDown nativo / OCR (cliente OpenAI-compatible)
│   ├── converter.py              # orquestração de conversão, composição, deduplicação geométrica
│   ├── cleaner.py                # limpeza/recomposição determinística de Markdown
│   ├── validator.py              # gate de qualidade Fase 1 (encoding, marcadores, conteúdo)
│   ├── report.py                 # contrato/serialização do relatório técnico JSON
│   ├── hashing.py                # SHA-256 utilitário + info de runtime
│   └── models.py                 # dataclasses: Relatorio, FonteInfo, SaidaInfo, OcrInfo, etc.
├── tests/                        # 14 arquivos, 1:1 com os módulos acima + acceptance/layout
├── prompts/ocr_literal_ptbr.txt  # prompt de OCR versionado
├── docs/Pipeline_Conversao_Juridica_Corrigido.md  # única baseline arquitetural commitada (Fase 1 completa + Fase 2 planejada §20)
├── openspec/
│   ├── specs/juridical-pdf-conversion/spec.md     # única spec normativa existente
│   └── changes/archive/                            # 21 mudanças arquivadas (2026-07-27 → 2026-08-15)
├── input/, output/, logs/, var/tmp/                # E/S da CLI (canônicos + corpus de auditoria)
├── var/ocr_final/                                  # artefato pré-existente não rastreado (não tocado)
└── LOOPS.md, AGENTS.md, CLAUDE.md, CHANGELOG.md, README.md
```

Não existe, em lugar nenhum do repositório: `pipeline/`, `shared_conversion/`, `ingress/`, `preflight/`, `domain_router/` (distinto de `router.py`), `legal/`, `process/`, `retrieval/`, `chunking/`, `storage/`, `/bundle/`. Nenhum desses diretórios foi criado por esta investigação — sua ausência é o próprio achado.

## 2. Achado transversal — divergência entre "baseline FROZEN" e estado físico real

A instrução desta tarefa presume a existência de baselines FROZEN vigentes cobrindo termos como `ITP`, `Preflight`, `Evidence Preservation`, `Shared Conversion Core`, `ConversionEngine`, `Domain Router`, `Legal Semantic Review`, `Legal Producer`, `Process Semantic Review`, `Process Producer`, `Process Storage`, `Legal Knowledge Retrieval`, `chunking/reranking`. Busca textual (`grep -ri`) por esses termos em todo o repositório (excluindo `.venv`, `node_modules`, `.git`) **não encontrou nenhuma ocorrência** em nenhum arquivo de documentação, spec ou config deste repositório. A única baseline arquitetural commitada em `repo_jur` é `docs/Pipeline_Conversao_Juridica_Corrigido.md`, que documenta integralmente a Fase 1 (conversão determinística PDF→Markdown, implementada) e, na §20, uma única camada futura ("revisor semântico por IA" + "YAML Frontmatter", ambos "planejado, não implementado") — sem nomear nenhuma das 23 capacidades lógicas desta tarefa por esses termos, e sem cobrir Domain Router, Legal/Process Producer, Process Storage, Legal Knowledge Retrieval ou chunking/reranking em nenhuma forma.

**Conclusão do achado:** a baseline FROZEN referenciada por esta tarefa é externa a este repositório (não está commitada em `repo_jur`). O mapeamento abaixo trata cada capacidade lógica pelo nome fornecido na tarefa e localiza o equivalente físico mais próximo já existente, sem presumir nenhum path documental — exatamente a regra principal desta tarefa (`INSPECT → REUSE → ADAPT IN PLACE → CREATE somente se realmente ausente`).

## 3. Tabela principal

| # | Capacidade lógica | Implementação física encontrada | Ação | Testes existentes | Lacuna | Justificativa |
|---|---|---|---|---|---|---|
| 1 | ITP / Ingress | `cli.py::main` (parsing de argv, `.env`, resolução de paths) + `inspector.py::validate_pdf_path`/`open_pdf` | ADAPT | `test_cli.py`, `test_inspector.py` | Sem estágio de ingresso formal separado do parsing de CLI; termo "ITP" não definido em nenhum documento do repo | Funcionalidade de ingresso (validação de caminho, arquivo, leitura de `.env`) já existe e é exercida em todo teste de aceitação; falta apenas nomear/isolar como estágio formal |
| 2 | Preflight | `inspector.open_pdf` (checa criptografia/páginas vazias), `RoutingConfig.from_env().__post_init__` (valida limites de roteamento) | ADAPT | `test_inspector.py`, `test_config.py` | Checagens existem inline no caminho de conversão, não como etapa nomeada e isolada antes do processamento | Pré-condições reais (PDF válido, não criptografado, não vazio; config de roteamento coerente) já são verificadas e falham cedo com exceções tipadas |
| 3 | official receiver SHA-256 | `hashing.sha256_file`/`sha256_bytes`, `inspector.inspect_source` → `FonteInfo.sha256`; `SaidaInfo.sha256` para a saída | REUSE | `test_hashing.py`, `test_inspector.py`, `test_report.py` | Nenhuma | Hash SHA-256 do PDF de origem é calculado no recebimento e registrado no relatório técnico; hash da saída também é registrado |
| 4 | Evidence Preservation | Princípio "pipeline não destrutivo" (§3 do doc), `inspector.isolated_page_workspace` (cópias isoladas por página, `--keep-temp` opcional), `validator.write_atomic` | ADAPT | `test_inspector.py`, `test_validator.py` | Preservação hoje é "não alterar o PDF de origem + escrita atômica de saída + hash no relatório", não um repositório de evidências com retenção/cadeia de custódia formal | As garantias centrais (fonte nunca é escrita, saída só é publicada após validação completa, hash de ambos registrado) já existem; um módulo de evidência dedicado seria uma extensão, não uma criação do zero |
| 5 | Shared Conversion Core | `src/pipeline_juridico/` inteiro (`converter.py` + `cleaner.py` + `engines.py` + `router.py`), único caminho de conversão usado por toda a CLI | REUSE | Toda a suíte de `tests/test_{converter,converter_integration,cleaner,engines,router}.py` | Nenhuma | O núcleo de conversão já é compartilhado por definição (um único pacote, um único entrypoint); **regra especial desta tarefa proíbe explicitamente criar `pipeline/shared_conversion/` só para casar com diagrama** — o alvo lógico já está fisicamente satisfeito sob outro nome |
| 6 | `ConversionEngine` / interface equivalente | `converter.py::convert_document` (orquestração completa) + `engines.py::create_native_engine`/`create_ocr_engine` (fábricas que retornam `MarkItDown` configurado) | ADAPT | `test_converter.py`, `test_converter_integration.py`, `test_engines.py` | Não existe classe/protocolo abstrato nomeado `ConversionEngine`; hoje há duas funções de fábrica concretas, não uma interface plugável | Equivalente funcional completo já existe e é testado; formalizar uma interface só se justificaria se um segundo motor de conversão (além de MarkItDown nativo/OCR) precisasse ser plugado — não há essa necessidade hoje |
| 7 | MarkItDown / markitdown-ocr | Dependências declaradas em `pyproject.toml`; `engines.py::create_native_engine` (`enable_plugins=False`) e `create_ocr_engine` (`enable_plugins=True`, `llm_client`) | REUSE | `test_engines.py` | Nenhuma | Motor principal e plugin de OCR já integrados exatamente como a única baseline commitada (`docs/...md` §7, §13) descreve |
| 8 | Gemini/OpenAI-compatible client | `engines.py::create_ocr_engine` (`openai.OpenAI(api_key=..., base_url=...)`), `cli.py::GEMINI_OPENAI_BASE_URL` | REUSE | `test_engines.py`, `test_cli.py` | Nenhuma | Cliente Gemini via endpoint OpenAI-compatible já configurado; nomenclatura de env vars é `GEMINI_*` (decisão já registrada em memória de projeto, diverge intencionalmente de um padrão `OCR_API_KEY`/`OCR_BASE_URL` genérico) |
| 9 | OCR routing | `router.py::route_page` + `inspect_native_text`/`inspect_raster_content`, `RoutingConfig` | REUSE | `test_router.py`, `test_config.py` | Nenhuma | Classificação `texto_nativo`/`hibrido`/`ocr_integral`/`vazia` por página já implementada e testada; **ver risco de nomenclatura na seção 6** — este módulo não deve ser confundido com a capacidade lógica 14 (Domain Router) |
| 10 | page markers | `converter.py::format_page_marker` (`[[Pág. N]]`), `validator.py::validate_page_markers` | REUSE | `test_converter.py`, `test_validator.py` | Nenhuma | Marcador canônico único e sequencial já implementado e validado em toda a suíte de aceite |
| 11 | Technical JSON/report | `report.py` (`build_report_json`, `validate_report_contract`, `schema_version="1.0"`), `models.py::Relatorio` e dataclasses aninhadas | REUSE | `test_report.py`, `test_models.py` | Nenhuma | Relatório técnico versionado, com contrato validado antes da escrita, já existe e cobre origem, saída, runtime, OCR, timing e páginas |
| 12 | Post-OCR Critical-Data Validation | Nenhuma implementação dedicada; `validator.py` cobre apenas integridade estrutural (encoding, marcadores, conteúdo vs. relatório) | CREATE | Nenhum | Total — nenhum checksum de identificador, nenhuma checagem de comprimento de selo, nenhuma comparação de valor redundante intra-documento | Busca confirmada em `validator.py`, `cleaner.py`, `report.py`: nenhuma função trata dado crítico (CPF, selo digital, etc.) especificamente. Esta lacuna já foi confirmada e documentada em detalhe pela auditoria arquivada nesta mesma sessão (`openspec/changes/archive/2026-08-15-audit-ocr-critical-data-fidelity/`, commit `4759696`): 1 divergência real de selo digital não capturada por nenhum mecanismo existente |
| 13 | Phase 1 Quality Gate | `validator.py` completo (`validate_encoding_and_line_endings`, `validate_page_content`, `validate_markdown_matches_report`, `validate_page_markers`), `cleaner.py::ensure_illegible_marker_authorized`, `report.py::validate_report_contract` — todos invocados em `cli.py::main` antes de `write_atomic` | REUSE | `test_validator.py`, `test_report.py`, `test_cli.py`, `test_acceptance.py` | Nenhuma | Gate de qualidade da Fase 1 já é exatamente esta cadeia de validações, documentada nas §9/§10 da baseline commitada, e é exercida ponta a ponta pela suíte de aceite |
| 14 | Domain Router | Nenhuma implementação — nenhum componente classifica um documento já convertido como domínio "Legal" vs. "Processual" | CREATE | Nenhum | Total | `router.py` existente roteia MÉTODO de conversão por página (texto_nativo/hibrido/ocr_integral/vazia), não DOMÍNIO do documento; confirmado por leitura completa de `router.py` — não há nenhuma lógica de classificação por tipo de peça jurídica em lugar nenhum do pacote |
| 15 | Legal Semantic Review | Nenhum código; arquitetura e 9 regras já planejadas em `docs/...md` §20 ("Fase 2, planejada, não implementada"), motivadas pelo achado Papel/Nome (`openspec/changes/archive/2026-08-07-fix-role-name-list-cross-block-fusion/`) | CREATE | Nenhum | Total (implementação); planejamento arquitetural já existe | A própria baseline commitada já rotula esta capacidade como não implementada — corrobora, não contradiz, esta classificação |
| 16 | Legal Producer | Nenhum código; §20.3 menciona "geração/extração de YAML Frontmatter" como etapa futura, também não implementada | CREATE | Nenhum | Total | Nenhum módulo produz um artefato "Legal" final além do Markdown fiel + relatório JSON já cobertos pelas capacidades 5–13 |
| 17 | Process Semantic Review | Nenhum código; nenhuma menção em `docs/...md` (que só planeja revisão semântica para o lado Legal, §20) | CREATE | Nenhum | Total — inclusive de decisão arquitetural, que ainda não existe para o lado Processual | Diferente da capacidade 15, esta nem tem arquitetura planejada na baseline commitada; precisaria de sua própria decisão arquitetural antes de qualquer mudança OpenSpec de implementação |
| 18 | Process Producer | Nenhum código | CREATE | Nenhum | Total | Mesma ausência da capacidade 16, sem nenhum planejamento prévio documentado |
| 19 | Process Storage | Nenhum código; `output/`/`logs/` são diretórios planos genéricos da CLI de conversão, sem separação por domínio | CREATE | Nenhum | Total | Nenhuma camada de armazenamento/indexação específica para processos judiciais existe; `output/`/`logs/` servem qualquer PDF convertido, Legal ou não |
| 20 | Legal Knowledge Retrieval | Nenhum código — nenhuma dependência de vetor/busca em `pyproject.toml`, nenhum módulo de indexação | CREATE | Nenhum | Total | Confirmado por ausência de dependências relevantes (`pyproject.toml` lista somente as 7 dependências de conversão) e ausência de qualquer módulo de busca/índice em `src/` |
| 21 | chunking/reranking | Nenhum código | CREATE | Nenhum | Total | Mesma confirmação da capacidade 20; nenhuma função de segmentação de texto para recuperação (distinta da segmentação de página/bloco já usada internamente pelo conversor) existe |
| 22 | observability/logging | `cli.py` (`logging.basicConfig`, `logger.error` com `_sanitize_log_message` — redação de segredos + truncamento em 500 chars), `report.py::TimingInfo`/`build_runtime_info` (duração e versões de runtime por execução) | ADAPT | `test_cli.py` | Sem agregação centralizada de logs, sem métricas/tracing distribuído; observabilidade hoje = log local por execução + timing no relatório JSON | Log estruturado básico com redação de segredos já existe e é testado (princípio "nunca registrar segredos" da baseline, §14, já cumprido); um estágio de observability completo (métricas, tracing) seria extensão, não criação do zero |
| 23 | schemas/contracts comuns, Legal e Process | `report.py::validate_report_contract` + `models.py` (dataclasses `Relatorio`, `FonteInfo`, `SaidaInfo`, `OcrInfo`, `TimingInfo`, `ResultadoPagina`) | ADAPT | `test_report.py`, `test_models.py` | Contrato existente é único e agnóstico de domínio (aplica-se a qualquer PDF convertido); não há schema separado para "Legal" vs. "Process" porque a capacidade 14 (Domain Router) que os distinguiria ainda não existe | O único contrato/schema real do repositório já é versionado (`schema_version`) e validado antes de cada escrita; uma eventual separação Legal/Process dependeria logicamente da capacidade 14 existir primeiro |

**Nota adicional (regra especial da tarefa, fora da numeração 1–23):** a instrução desta tarefa marca explicitamente `Judicial Process Retrieval` como `OUT_OF_SCOPE`. Nenhuma implementação foi encontrada nem investigada para essa capacidade — classificação aplicada exatamente como instruído, sem inspeção adicional.

## 4. Capacidades por estado

- **Completas (REUSE):** 3 (receiver SHA-256), 5 (Shared Conversion Core), 7 (MarkItDown/markitdown-ocr), 8 (cliente Gemini/OpenAI-compatible), 9 (OCR routing), 10 (page markers), 11 (Technical JSON/report), 13 (Phase 1 Quality Gate). — **8 de 23**, todas correspondentes integralmente à Fase 1 já implementada e coberta pela suíte `391/391`.
- **Parciais (ADAPT):** 1 (ITP/Ingress), 2 (Preflight), 4 (Evidence Preservation), 6 (ConversionEngine/interface), 22 (observability/logging), 23 (schemas/contracts comuns). — **6 de 23**, todas com funcionalidade real subjacente, mas sem a formalização/nomenclatura/generalização que a capacidade lógica describe.
- **Ausentes (CREATE):** 12 (Post-OCR Critical-Data Validation), 14 (Domain Router), 15 (Legal Semantic Review), 16 (Legal Producer), 17 (Process Semantic Review), 18 (Process Producer), 19 (Process Storage), 20 (Legal Knowledge Retrieval), 21 (chunking/reranking). — **9 de 23**, nenhuma com implementação equivalente identificável no repositório.
- **Fora de escopo (OUT_OF_SCOPE):** Judicial Process Retrieval, por instrução explícita da tarefa.

## 5. Overlaps e duplicações já existentes

- **Nenhuma duplicação de implementação foi encontrada** — cada capacidade REUSE/ADAPT mapeia para exatamente um módulo/função, sem lógica paralela concorrente.
- **Risco de colisão de nomenclatura (não uma duplicação real, mas um risco documentado):** `src/pipeline_juridico/router.py` já existe e é publicamente referenciado em toda a base de código e testes como o roteador de **método de conversão por página**. Se uma futura mudança OpenSpec para a capacidade 14 (Domain Router) criar um arquivo/classe também chamado apenas "router" sem qualificação, haverá ambiguidade de nome imediata dentro do mesmo pacote — recomenda-se, na mudança futura que implementar a capacidade 14, um nome explicitamente distinto (ex. `domain_router.py`, nunca `router.py` reaproveitado ou duplicado).

## 6. Riscos de arquitetura caso módulos paralelos sejam criados

1. **Criar uma árvore `pipeline/shared_conversion/` paralela a `src/pipeline_juridico/`** duplicaria o núcleo de conversão já testado (2421 linhas, 391 testes) sem necessidade — a regra especial desta tarefa já proíbe isso explicitamente, e esta investigação confirma que não há nenhuma justificativa técnica para tal duplicação: o pacote existente já cumpre integralmente o papel lógico de "Shared Conversion Core".
2. **Criar uma segunda função de hashing/relatório** para as futuras capacidades Legal/Process (16/18/19) sem reaproveitar `hashing.py`/`report.py`/`models.py` fragmentaria o contrato técnico único hoje existente (capacidade 23) — qualquer extensão Legal/Process deveria estender `Relatorio`/`validate_report_contract`, não recriá-los.
3. **Implementar Domain Router (14) sem revisar `router.py` existente** cria risco real de confusão de responsabilidade (roteamento de método de página vs. roteamento de domínio de documento) — ver seção 5.
4. **Implementar Post-OCR Critical-Data Validation (12) fora de `validator.py`** fragmentaria o gate de qualidade Fase 1 em dois pontos de decisão de publicação (estrutural em `validator.py`, crítico em outro lugar) — o achado da auditoria arquivada `audit-ocr-critical-data-fidelity` já recomenda `validator.py`/relatório JSON como extensão natural, não um módulo novo isolado.
5. **Qualquer criação de diretório `legal/`/`process/` antes de uma decisão arquitetural equivalente à §20 (hoje só existe para o lado Legal)** repetiria, para o lado Process, o mesmo erro que a decisão de 2026-08-10 já evitou para o lado Legal (tentar resolver semântica com heurística geométrica antes de uma camada dedicada) — recomenda-se que a capacidade 17 (Process Semantic Review) receba sua própria seção de decisão arquitetural, análoga à §20, antes de qualquer código.

## 7. Divergências entre baseline FROZEN e estado físico real

Ver seção 2 (achado transversal). Resumo: a baseline FROZEN referenciada por esta tarefa (com a terminologia ITP/Ingress, Preflight, Evidence Preservation, Domain Router, Legal/Process Producer, Process Storage, Legal Knowledge Retrieval, chunking/reranking) **não está commitada em `repo_jur`**; a única baseline arquitetural física é `docs/Pipeline_Conversao_Juridica_Corrigido.md`, que cobre integralmente a Fase 1 e apenas uma fração da Fase 2 (revisão semântica Legal + YAML Frontmatter, ambos "planejado, não implementado"). Qualquer mudança OpenSpec futura que implemente as capacidades 14–21 precisará **primeiro** registrar sua própria decisão arquitetural nesse documento (ou em um equivalente), seguindo o precedente já estabelecido pela §20.

## 8. Cobertura de testes existente

`391/391` testes passam na baseline desta investigação, distribuídos 1:1 com os módulos REUSE/ADAPT: `test_cli.py`, `test_config.py`, `test_engines.py`, `test_hashing.py`, `test_inspector.py`, `test_models.py`, `test_report.py`, `test_router.py`, `test_validator.py`, `test_cleaner.py`, `test_converter.py`, `test_converter_integration.py`, mais `test_acceptance.py` (ponta a ponta sobre o corpus canônico) e `test_project_layout.py` (estrutura de diretórios/arquivos obrigatórios). Nenhum teste existe, nem poderia existir hoje, para as 9 capacidades `CREATE` — não há código a testar.

## 9. Lacunas de testes (registradas, nenhum teste novo criado nesta tarefa)

- Nenhum teste cobre validação de dado crítico pós-OCR (capacidade 12) — consistente com sua ausência de implementação.
- Nenhum teste cobre classificação de domínio de documento (capacidade 14).
- Nenhum teste cobre observabilidade além da redação de segredos em log (`test_cli.py`) — não há teste de timing/duração do relatório isoladamente (`TimingInfo` é coberto indiretamente via `test_report.py`/`test_acceptance.py`, não testado por si só quanto a precisão).
- `test_project_layout.py` fixa a existência de `input/`, `output/`, `logs/`, `var/tmp/` e `.env.example` com chaves específicas — qualquer capacidade futura que precise de novos diretórios físicos (ex. armazenamento Process) precisará estender esse teste deliberadamente, não presumir que a estrutura atual já comporta.

## 10. Dependências entre capacidades

```
1 ITP/Ingress → 2 Preflight → 9 OCR routing → {7 MarkItDown/OCR, 8 cliente Gemini} → 6 ConversionEngine
                                            └→ 5 Shared Conversion Core → 10 page markers → 13 Phase 1 Quality Gate → 11 Technical JSON/report
13 Phase 1 Quality Gate → 12 Critical-Data Validation (futuro, estende 13)
5 Shared Conversion Core → 14 Domain Router (futuro) → {15 Legal Semantic Review, 17 Process Semantic Review}
15 Legal Semantic Review → 16 Legal Producer → 20 Legal Knowledge Retrieval → 21 chunking/reranking
17 Process Semantic Review → 18 Process Producer → 19 Process Storage
3 receiver SHA-256, 4 Evidence Preservation, 22 observability/logging, 23 schemas/contracts → transversais a todas as capacidades acima
```

A capacidade 14 (Domain Router) é a bifurcação estrutural: nada em 15–21 (lado Legal) ou 17–19 (lado Process) pode ser implementado de forma coerente sem ela existir primeiro, pois é ela quem decidiria qual documento segue qual ramo.

## 11. Ordem recomendada das futuras mudanças OpenSpec

1. **Post-OCR Critical-Data Validation (12)** — maior prioridade: já tem achado real confirmado (selo digital), critério de baixo custo já identificado (comprimento fixo + checksum CPF), extensão natural de `validator.py` já existente; nenhuma dependência de capacidade ainda ausente.
2. **Domain Router (14)** — pré-requisito estrutural para todo o resto (15–19); deve vir antes de qualquer trabalho Legal ou Process de nível semântico, exatamente porque bifurca o fluxo.
3. **Legal Semantic Review (15)** — já tem arquitetura e 9 regras pré-planejadas na baseline commitada (§20); menor risco de retrabalho por já ter sido pensada.
4. **Process Semantic Review (17)** — precisa primeiro de uma decisão arquitetural própria (análoga à §20), ainda inexistente; só depois de (2) e com essa decisão registrada.
5. **Legal Producer (16)** e **Process Producer (18)** — dependem de (3)/(4) respectivamente já estarem implementados e validados contra corpus real.
6. **Process Storage (19)** — depende de (18) definir o formato do artefato a armazenar.
7. **Legal Knowledge Retrieval (20)** e **chunking/reranking (21)** — última prioridade; dependem de (5) já ter um corpus de artefatos Legal Producer real para indexar; caso contrário qualquer decisão de chunking seria especulativa.

Capacidades ADAPT (1, 2, 4, 6, 22, 23) não precisam de mudança OpenSpec dedicada isoladamente — cada uma pode ser formalizada como parte da mudança que primeiro precisar da sua generalização (ex.: capacidade 23 se estende naturalmente dentro da mudança que implementar a capacidade 14).

## 12. Primeiro Stage seguro após aprovação deste mapa

**Post-OCR Critical-Data Validation (capacidade 12)** é o único candidato que satisfaz simultaneamente: (a) achado real já confirmado e evidenciado (auditoria arquivada `audit-ocr-critical-data-fidelity`, commit `4759696`), (b) zero dependência de capacidade ainda ausente, (c) extensão in-place de um módulo já existente e testado (`validator.py`), (d) escopo pequeno e mensurável (2 mudanças candidatas já detalhadas em `LOOPS.md`: comprimento fixo do selo digital + checksum CPF; comparação de valores redundantes intra-documento). Qualquer Stage que envolva Domain Router ou as camadas Legal/Process semânticas deveria vir depois, pois dependem de decisões arquiteturais ainda não registradas (seção 6, item 5).
