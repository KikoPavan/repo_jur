## 1. Focused Stage 5 tests (TDD — red first)

- [x] 1.1 Write failing tests for the boundary input contract: `evaluate(phase1_artifacts)` accepts `Phase1Artifacts` produced by the Shared Conversion Core (literal Markdown + technical conversion report) and evaluates it without requesting or performing further conversion; a source-inspection test asserts `src/pipeline_juridico/quality_gate.py` contains no evidence-resolution, converter, or OCR invocation path (no import of the converter, engines, inspector, or evidence modules).
- [x] 1.2 Write failing tests for the three-state contract: a fully conformant artifact set yields `GateState.PASS`; an artifact set satisfying all structural/completeness conditions with at least one non-fatal warning recorded in the report yields `GateState.PASS_WITH_WARNINGS` (the serialized value; the human label "PASS WITH WARNINGS" is never a serialized value and `PASS_WITH_WARNINGS` is never used as a human label); an artifact set violating any fatal rule yields `GateState.FAIL`; the result state is always exactly one of the three `GateState` members.
- [x] 1.3 Write failing tests for page coverage: exactly `N` markers `[[Pág. 1]]..[[Pág. N]]` in order yields PASS (with `N >= 1`); a missing marker, a duplicated marker, an out-of-order sequence, a marker count different from the report page count, and a zero-page report (`N < 1`) each yield FAIL with the deficiency recorded in `errors`.
- [x] 1.4 Write failing tests for extraction outcomes using the deterministic page-state mapping (Decision 6): a page whose `pages[].status` is not `sucesso` (error state `falha`, or any not-completed state) yields FAIL regardless of `pages[].error` being null or set (the state carrier is `status`, `error` is informational); a page whose `pages[].method` is `erro` yields FAIL independently of its `pages[].status`, including an inconsistent record combining `method: erro` with `status: sucesso` (memo §3.3 `error` outcome, §6 criterion 4, §8.2); a page whose method is not blank but whose content is empty (`pages[].characters == 0` and `pages[].method != "vazia"`) yields FAIL (empty-return rule, its own deterministic fatal rule); a page determined blank (method `vazia`) with empty content keeps its marker, yields no warning, and does not prevent PASS; a page whose method is `erro` under a partial-mode artifact still yields FAIL (partial never converts FAIL into success); a report whose top-level status is `incompleto` while every page is `sucesso` does not FAIL because of the flag (allow_partial is never itself a FAIL condition).
- [x] 1.5 Write failing tests for structural/textual conformance: Markdown that cannot be encoded as UTF-8 yields FAIL; Markdown containing a NUL byte yields FAIL; Markdown containing a CR line ending (prohibited by the Phase 1 textual contract) yields FAIL; the literal body containing a canonical technical method comment (`<!-- método: ... -->`) yields FAIL (wrong-artifact/structural-conformance detection); a conformant boundary literal (markers only) passes this rule.
- [x] 1.6 Write failing tests for the report minimum contract: an unparseable technical report yields FAIL; a parseable report lacking the input evidence SHA-256 (`source.sha256`) yields FAIL; a report lacking the physical page count (`source.pages`) yields FAIL; a report whose per-page inventory does not cover every physical page (or lacks page number `number`, method `method`, or completed/error state `status` in a record) yields FAIL; a report whose inventory page numbers are not exactly the set `{1..N}` — missing, duplicated, out-of-range, or extra page numbers — yields FAIL; a complete inventory whose records are ordered differently from ascending `1..N` does NOT fail for record order (record order is not normative — no FROZEN baseline defines it; page-marker order in the Markdown remains governed by the marker rule); each deficiency is recorded in `errors`.
- [x] 1.7 Write failing tests for the warnings taxonomy: a non-fatal warning present in a completed page's `pages[].warnings` yields `PASS_WITH_WARNINGS` when all completeness conditions hold; warnings are never synthesized from page state or from marker patterns (there is no marker-based warning rule — no FROZEN baseline defines an illegible-text sentinel; memo §16.10, tech spec §8.4); OCR use alone (pages with method `ocr_integral`/`hibrido`, no warning) yields no warning and allows PASS; a legitimately blank page yields no warning; line-ending normalization, slow execution, large file size, and engine identity produce no warning.
- [x] 1.8 Write failing test for deterministic idempotency: evaluating the identical `Phase1Artifacts` twice yields identical state, warnings, and errors; the result contains no execution id, timestamp, or duration (pure function of the artifacts).
- [x] 1.9 Write failing test that diagnostics are non-authoritative: populating, mutating, or removing the optional diagnostics never changes the resulting state, warnings, or errors.
- [x] 1.10 Write failing tests for the non-mutation invariants: `SHA256(phase1_artifacts.markdown)` before calling `evaluate()` equals `SHA256(phase1_artifacts.markdown)` after, and the serialized `phase1_artifacts.report_json` is byte-identical after evaluation, across `PASS`, `PASS_WITH_WARNINGS`, and `FAIL` outcomes; no warning or error text produced by the gate appears inside the literal Markdown body.
- [x] 1.11 Write failing domain-neutrality and independence source-inspection tests mirroring `tests/test_conversion_engine.py:298-306` and the Stage 4 pattern: reading `src/pipeline_juridico/quality_gate.py` as text contains no `CriticalValidationResult`, `CriticalValidationStatus`, `CriticalFinding`, `CriticalDataValidator`, `RouteTarget`, and no `bundle` reference.
- [x] 1.12 Write failing behavioral independence test: with a test-only Critical-Data Validation rule returning `REVIEW_REQUIRED` for the same artifacts, the Quality Gate still reports its physical state (e.g. `PASS` on sound artifacts), and the gate result exposes no critical-data status or combined/review-required routing outcome.
- [x] 1.13 Write failing test that the gate never writes: executing `evaluate()` for any outcome performs no filesystem write to `repo_jur/bundle/` and creates no `verified` or OKF `status` value (the result exposes only state, warnings, errors, and optional diagnostics).
- [x] 1.14 Write failing test for the truncation residual: the gate never infers or invents a truncation signal — a technical report that contains no truncation signal (the current contract defines none) never produces `FAIL` from truncation, no other observable (e.g. empty return) is treated as truncation, and the empty-return rule remains its own deterministic fatal rule. The test documents the residual (proposal.md Residual Risk 2) and asserts the FROZEN rule that an authoritative truncation signal is fatal (memo §8.2, §3.4; op-spec §7.1/§7.3; tech spec §17) stays normative as an already-normative deferred-evaluability requirement in the specification even though it is not evaluable on the current contract — no executable scenario requires consuming an undefined signal, and the report-schema synchronization (memo §14.3) must add the executable check; the test does not invent a signal.

## 2. Minimal implementation

- [x] 2.1 Implement `src/pipeline_juridico/quality_gate.py` with the frozen `QualityGateResult` dataclass (`state: GateState`, `warnings: tuple[str, ...]`, `errors: tuple[str, ...]`, optional non-authoritative `diagnostics: dict[str, object]`), reusing `GateState` from `src/pipeline_juridico/contracts.py` and `Phase1Artifacts` from `src/pipeline_juridico/conversion_engine.py` without modification.
- [x] 2.2 Implement the pure, stateless evaluation entry point `evaluate(phase1_artifacts) -> QualityGateResult` following the tech spec §8.5 conceptual sequence: validate the page inventory from the report, validate the `[[Pág. N]]` markers against the report page count (markers-only pattern for the boundary literal), validate no unresolved page errors (`pages[].status` not `sucesso`, and `pages[].method == "erro"` — fatal independently of `status`), validate the empty-return rule as its own deterministic fatal rule (`characters == 0` and `method != "vazia"`), validate the Markdown artifact (UTF-8, no NUL, no CR, no method comments in body), validate the technical report minimum fields, then return `FAIL` on any fatal error, `PASS_WITH_WARNINGS` when only non-fatal warnings exist, otherwise `PASS`. Implement NO known-truncation check and NO truncation inference (the current report contract defines no truncation signal — residual risk 2, not a blocker; the FROZEN rule that an authoritative truncation signal is fatal remains normative in the spec as an already-normative deferred-evaluability requirement that is not evaluable on the current contract, and the §14.3 report-schema synchronization must add the executable check) and no marker-based warning rule (residual risk 5).
- [x] 2.3 Implement the isolated read-only report-field mapping (Decision 8): read `source.sha256`, `source.pages`, and per-page `number`/`method`/`status`/`characters`/`warnings` from the parsed report using the existing `Metodo` values as the engine-neutral method vocabulary; validate the inventory is exactly complete (exactly `source.pages` records with page numbers exactly `{1..N}` — no missing, duplicated, out-of-range, or extra — without requiring any specific record order, which is not normative); never mutate, rename, or reorder any serialized report field (read-only semantic field mapping only); treat `pages[].status == "sucesso"` as the completed state and any other value as not-completed (fatal); treat `pages[].method == "erro"` as fatal independently of `status` (an inconsistent record with `method: erro` and `status: sucesso` is a FAIL); treat `pages[].error` as informational only (never the state carrier); treat non-empty `pages[].warnings` entries of completed pages as the warning representation; never read the top-level `status` (`StatusExecucao`) as a gate signal; treat missing required fields or unparseable JSON as report failures recorded in `errors`.
- [x] 2.4 Implement the warnings taxonomy (Decision 7): aggregate non-empty `pages[].warnings` entries of completed pages into `warnings`; never synthesize warnings from page state or marker patterns; never warn for OCR use alone, legitimately blank pages, line-ending normalization, slow execution, large file size, or engine identity.
- [x] 2.5 Ensure `quality_gate.py` never mutates `Phase1Artifacts`, never imports `CriticalValidationResult`/`CriticalValidationStatus`/`CriticalFinding`/`CriticalDataValidator`/`RouteTarget`, never resolves evidence or invokes conversion/OCR, never writes to `repo_jur/bundle/`, and never creates `verified`/OKF `status` values.

## 3. Validation

- [x] 3.1 Run focused Stage 5 tests (`uv run pytest tests/test_quality_gate.py -v`) — all green.
- [x] 3.2 Run the full regression suite (`uv run pytest tests/`, with the gitignored `input/*.pdf` corpus fixtures present in the worktree) — all existing tests plus new Stage 5 tests pass with no behavioral change to Stage 1–4.
- [x] 3.3 Run OpenSpec strict validation (`openspec validate stage5-phase1-quality-gate --type change --strict` and `openspec validate --all --strict`) — all passed, 0 failed.
- [x] 3.4 Confirm the final diff: no FROZEN baseline modification, no `bundle/` write, no Stage 4 code/spec modification, no Stage 6+ implementation, no converter/OCR/routing/cleaner/validator/report/CLI change, no dependency change, and no new normative rule invented without a FROZEN citation.
