## Why

A validação supervisionada arquivada (`validate-supervised-ocr-testamento-publico`, 2026-08-12) identificou, mas não isolou com evidência independente nem avaliou blast radius formal, um defeito sistemático na rota `hibrido`/OCR: cada uma das 4/4 páginas de `012-015-Testamento Publico.pdf` traz, após `[End OCR]*`, ~209 linhas de ruído de caracteres isolados — o mesmo carimbo de autenticação e-SAJ vertical já lido corretamente pelo PyMuPDF, mas fragmentado pelo pacote de terceiros `markitdown-ocr`. Esta mudança é **exclusivamente diagnóstica**: reproduz a causa raiz de forma independente (sem confiar apenas no relatório anterior), rastreia PyMuPDF → pdfplumber → `markitdown-ocr` → composição final, e avalia formalmente critérios de correção candidatos contra o corpus de controle completo (8 PDFs, 270 páginas). Não implementa código, não cria testes de produção, não executa OCR/LLM novo, não altera prompt/modelo/provider, não arquiva e não faz push.

## Pré-voo (evidência completa em `design.md`)

- `git status --short`: limpo. HEAD `b30771d`. `uv run pytest tests/`: 364/364 passando. `openspec validate --all --strict`: 1 passed (spec), 1 failed (esta própria mudança, esperado — diagnóstica, sem deltas, mesmo padrão dos precedentes arquivados).
- Nenhuma chamada de OCR/LLM feita nesta investigação. Toda evidência via PyMuPDF (`page.get_text("dict")`), pdfplumber (`page.chars`, `page.images`) e leitura direta do código-fonte instalado de `markitdown_ocr`, reproduzindo localmente o algoritmo de agrupamento de linhas.

## Conclusão (resumo — evidência completa em `design.md`)

**A) CRITÉRIO SEGURO ENCONTRADO.**

- **Estrutura do texto vertical:** 2 linhas verticais (`dir=(0,-1)`, Helvetica) por página, ~174 e ~202 caracteres, **cada uma ocorrendo uma única vez** (sem duplicação geométrica) — diferente do achado já corrigido `fix-rotated-digital-signature-noise` (que exigia 2 cópias sobrepostas na rota `texto_nativo`).
- **Primeiro estágio responsável:** o agrupamento de caracteres em "linhas" por proximidade de coordenada Y (limiar fixo de 2pt), interno a `markitdown_ocr/_pdf_converter_with_ocr.py::PdfConverterWithOCR.convert()` (linhas 199–227). `page.chars` (pdfplumber) já está correto; o defeito é exclusivamente deste agrupamento heurístico, que assume texto horizontal e nunca verifica direção de escrita.
- **Causa raiz confirmada por reprodução local** (sem OCR real): aplicando o algoritmo exato do plugin aos 383 caracteres nativos da página 1, obtêm-se 228 fragmentos (115 de 1 caractere, 105 de 2–9 caracteres) — concatenados e invertidos, reconstroem o mesmo texto das 2 linhas verticais já lidas corretamente pelo PyMuPDF.
- **Origem do ruído:** B) texto nativo residual — confirmado por leitura de código, o texto OCR (resposta LLM) e os fragmentos nativos são calculados por mecanismos totalmente independentes, apenas intercalados por posição Y na composição final.
- **Combinação OCR+nativo:** reproduzida e confirmada byte a byte — 1 imagem por página (`y_pos≈0`), 100% dos fragmentos de texto nativo ordenam-se depois dela, explicando o padrão observado (OCR legível, depois `[End OCR]*`, depois todo o ruído).
- **Critério recomendado (Candidato E, combinação de sinais):** fragmentos posicionados estritamente após `[End OCR]*` **e** cujo texto concatenado corresponde geometricamente (sobreposição de caracteres) a uma linha não horizontal já lida por `page.get_text("dict")` na mesma página. Nenhum sinal isolado (direção sozinha, posição sozinha, contagem de caracteres sozinha) é seguro — cada um foi avaliado e descartado como critério único, pelas mesmas razões já documentadas no precedente arquivado para a rota `texto_nativo`.
- **Blast radius:** no corpus de controle completo (8 PDFs, 270 páginas), a rota `hibrido`/`ocr_integral` — único ramo onde `markitdown-ocr`/`pdfplumber` processa a página — ocorre em **exatamente 4 páginas, todas já no escopo deste diagnóstico**. As 266 páginas `texto_nativo` restantes (incluindo os 78 blocos/linhas verticais legítimos de `100-106-DECISÃO.pdf`, `001-007-Petição Inicial.pdf` e `086-096-CONTESTAÇÃO...pdf`) nunca entram no ramo de código onde a correção atuaria — blast radius zero nelas, por construção do ponto de intervenção, não por ausência de teste.
- **Ponto de intervenção recomendado:** `converter.py::convert_document`, ramo `hibrido`/`ocr_integral`, imediatamente após obter `raw_content` do OCR — sem modificar o pacote de terceiros, sem tocar a rota `texto_nativo`.
- **Limitação registrada:** o corpus de controle não tem uma segunda página `hibrido`/`ocr_integral` real para validar a generalização do critério — mitigada pela exigência de corroboração geométrica (não apenas posicional) no critério recomendado; uma implementação futura deve cobrir isso com fixtures sintéticas dedicadas.
- **Decisão em aberto para a implementação futura:** descartar o texto residual corroborado ou substituí-lo pela reconstrução geométrica limpa do PyMuPDF (recomendado, por consistência com `fix-rotated-digital-signature-noise` e para não perder o valor probatório do carimbo e-SAJ).

## Implementação (2026-08-12, aprovada via `/goal` após o diagnóstico acima)

Usuário aprovou explicitamente avançar de diagnóstico para TDD + implementação mínima **nesta mesma mudança** (não criar mudança separada), com o Candidato E (posição estrutural + corroboração geométrica) confirmado como critério e o ponto de intervenção restrito a `converter.py::convert_document`, ramo `hibrido`/`ocr_integral`, ou helper privado associado.

**Refinamento descoberto durante a implementação, incorporado antes de considerar a mudança concluída:** o ponto de intervenção originalmente especificado no diagnóstico (aplicar a substituição diretamente sobre `raw_content`, por página, **antes** da composição do documento) causava um efeito colateral não previsto — a reconstrução geométrica, sendo idêntica nas 4 páginas (mesmo carimbo e-SAJ), tornava-se vulnerável ao cleaner já existente `remove_repetitive_margins` (`cleaner.py`, não alterado nesta mudança), que remove verbatim linhas finais de página idênticas entre ≥60% das páginas. Isso fazia a reconstrução ser removida e, em cascata (o cleaner itera até 10 vezes), o próprio marcador `[End OCR]*` — também idêntico nas 4 páginas — ser removido logo em seguida, violando a restrição explícita de que `[End OCR]*` deve permanecer exatamente como está hoje. Confirmado com dados 100% reais (texto substantivo já auditado + resíduo fragmentado reproduzido localmente via pdfplumber sobre o PDF real, sem nenhuma chamada de OCR nova) antes de considerar a implementação correta. **Correção arquitetural:** a geometria não horizontal de cada página (`_non_horizontal_line_texts`) passou a ser calculada e armazenada durante o loop por página já existente (mesmo ponto onde `_has_duplicated_rotated_block`/`page_has_large_text` já são calculados condicionalmente por método) — antes dos arquivos temporários de página serem descartados — e a substituição em si passou a ser aplicada **depois** de `remove_repetitive_margins`, sobre o documento já composto, por região de página. Isso preserva exatamente o comportamento atual de `remove_repetitive_margins` (incluindo sua não-remoção de `[End OCR]*`) e evita qualquer alteração em `cleaner.py`.

**Implementação final em `src/pipeline_juridico/converter.py`:** novas funções privadas `_split_ocr_tail`, `_tail_fragments`, `_is_degenerate_fragment_tail`, `_non_horizontal_line_texts`, `_char_multiset_overlap`, `_geometrically_corroborated_vertical_residual`, `_replace_fragmented_vertical_residual_in_text` (lógica pura, sem I/O), `_replace_fragmented_vertical_residual` (wrapper que abre a página via `page_path` — mantido para os testes unitários diretos) e `_replace_fragmented_vertical_residuals_in_document` (orquestradora, atua no markdown já composto, por região `[[Pág. N]]`, usando geometria pré-extraída). Critério: fragmentos posicionados estritamente após o último `[End OCR]*` de uma página, com ≥90% deles com até 2 caracteres, cujo conteúdo concatenado tem proporção de tamanho entre 0.5× e 1.2× e sobreposição de multiconjunto de caracteres ≥0.80 em relação ao texto de pelo menos uma linha não horizontal já lida por `page.get_text("dict")` na mesma página — limiares validados empiricamente contra o caso real (proporção medida 0.896, sobreposição medida 0.88). Quando corroborado, o resíduo é **substituído** (não apenas apagado) pela reconstrução coerente das linhas verticais. `convert_document` passou a computar `vertical_line_texts` por página (só quando `method in (hibrido, ocr_integral)`), armazená-las em `vertical_geometry_by_page`, e chamar a nova função orquestradora imediatamente após `remove_repetitive_margins` e antes de `join_symbol_across_page_break`. Nenhuma outra linha de `convert_document` foi alterada; `cleaner.py`, `router.py`, `engines.py`, `config.py`, prompt e dependências não foram tocados.

**Testes:** 21 testes novos em `tests/test_converter.py` (positivos: reconstrução do resíduo fragmentado, cobertura das 4 páginas via `convert_document` com OCR mockado, preservação do conteúdo substantivo, sobrevivência de `[End OCR]*` mesmo após `remove_repetitive_margins`; negativos: texto vertical legítimo não fragmentado preservado, resíduo horizontal legítimo preservado, fragmentos curtos sem corroboração geométrica preservados, ausência do marcador preservada, páginas `texto_nativo`/`vazia`/`erro` nunca tocadas pela função orquestradora, regressão explícita confirmando que `_replace_fragmented_vertical_residual` nunca é chamada na rota `texto_nativo`). `uv run pytest tests/test_converter.py -v`: 39/39 passando. `uv run pytest tests/`: **385/385 passando** (364 baseline + 21 novos).

**Validação executada:**
- `openspec validate --all --strict`: 2 passed, 0 failed (delta spec adicionado, ver `specs/juridical-pdf-conversion/spec.md`).
- **Dry run com dados 100% reais do Testamento** (texto substantivo por página extraído do Markdown já auditado em `validate-supervised-ocr-testamento-publico`, resíduo fragmentado reproduzido localmente via pdfplumber sobre `input/processos_auditoria/012-015-Testamento Publico.pdf`, motor de OCR mockado para reproduzir exatamente esses dois insumos por página — **nenhuma chamada real de OCR/LLM**): 4/4 páginas `hibrido`, `status: sucesso`; `[[Pág. N]]`: 4; `[End OCR]*`: 4 (idêntico ao baseline); linhas de 1 caractere: **0** (baseline: ~836 no total, ~209/página); resíduo reconstruído de forma legível em 4/4 páginas (ex. página 1: `"Para conferir o original, acesse o site https://esaj.tjsp.jus.br/pastadigital/pg/abrirConferenciaDocumento.do, informe o processo 1000386-85.2026.8.26.0136 e código ewt4TQYQ.\nEste documento é cópia do original, assinado digitalmente por JOAO PIRES GAVIAO NETO e Tribunal de Justica do Estado de Sao Paulo, protocolado em 05/04/2026 às 18:09 , sob o número 10003868520268260136."`); conteúdo substantivo (nomes, CPF, valores monetários) intacto em amostragem.
- **Reconversão dos 4 PDFs de `input/processos_auditoria/` com `--no-ocr`** (saída isolada, nunca em `output/`/`logs/` canônicos): `012-015-Testamento Publico.pdf` corretamente bloqueado em modo estrito (comportamento preexistente, não relacionado a esta mudança — sob `--no-ocr` a rota `hibrido` não pode ser satisfeita); os outros 3 processaram com sucesso, 100% `texto_nativo`, zero OCR — `100-106-DECISÃO.md` manteve exatamente 94 linhas, 0 linhas de 1 caractere (correção `fix-rotated-digital-signature-noise` intacta).
- **Regressão dos 4 PDFs canônicos com `--no-ocr`**: os 4 arquivos resultantes (`AINTARESP_1462304-PA.md`, `REsp_1704551-SP.md`, `Inf0024E.md`, `L10.406_CC_2002.md`) são **byte-idênticos** aos já commitados em `output/`.
- **`logs/*.report.json` das 7 reconversões bem-sucedidas:** 100% `method=texto_nativo`, `status=sucesso` — nenhuma chamada de OCR em nenhum dos 8 PDFs do corpus de controle (confirma blast radius zero fora do Testamento, exatamente como previsto no diagnóstico).
- **Idempotência:** segunda reconversão completa dos mesmos 7 PDFs bem-sucedidos, com `diff -rq` confirmando saída byte-idêntica à primeira execução.

**Blast radius real (medido, pós-implementação):** idêntico ao previsto no diagnóstico — apenas as 4 páginas `hibrido` de `012-015-Testamento Publico.pdf` são afetadas (e só quando OCR real é executado; sob `--no-ocr` a página é bloqueada como já era antes desta mudança); as 266 páginas `texto_nativo` dos outros 7 PDFs do corpus de controle (incluindo os 78 blocos/linhas verticais legítimos já mapeados no diagnóstico) permanecem byte-idênticas.

## Capabilities

### New Capabilities
(nenhuma)

### Modified Capabilities
- `juridical-pdf-conversion`: novo requisito "Substituição geométrica de resíduo de texto rotacionado fragmentado na rota híbrida/OCR" (ver `specs/juridical-pdf-conversion/spec.md`).

## Impact

- Código: `src/pipeline_juridico/converter.py` (novas funções privadas + 3 pontos de integração em `convert_document`, todos dentro do ramo `hibrido`/`ocr_integral` ou no cálculo de geometria por página já existente). `cleaner.py`, `router.py`, `engines.py`, `config.py`, prompt, modelo, provider e dependências não foram tocados.
- Testes: `tests/test_converter.py` (+21 testes).
- Nenhuma chamada real de OCR/LLM foi feita durante o diagnóstico nem durante a implementação/validação — toda validação do caso real do Testamento reutilizou evidência já obtida (Markdown auditado, PDF original, reprodução local de pdfplumber).
- Nenhum arquivo canônico (`output/`, `logs/`) foi alterado — as 4 saídas canônicas permanecem byte-idênticas; `100-106-DECISÃO.md` e as demais 2 saídas processuais de `input/processos_auditoria/` também permanecem sem alteração.
- Commits locais desta mudança (código + testes + delta spec + documentação), sem push, sem arquivamento — aguardando decisão humana sobre encerrar o ciclo.
