> Diagnóstico concluído (instrução original: `/goal` "SOMENTE DIAGNÓSTICO... Não implementar código..."), seguido de aprovação explícita do usuário via novo `/goal` para avançar a TDD + implementação mínima **nesta mesma mudança**. Conclusão do diagnóstico: **A) CRITÉRIO SEGURO ENCONTRADO** — o ruído de fragmentação da rota `hibrido`/OCR em `012-015-Testamento Publico.pdf` (4/4 páginas) tem causa raiz isolada e reproduzida localmente (sem OCR real) no agrupamento por coordenada Y interno a `markitdown_ocr/_pdf_converter_with_ocr.py`, não em `pdfplumber` nem na resposta do LLM. Critério combinado (posição estrutural após `[End OCR]*` + corroboração geométrica via PyMuPDF) implementado, com blast radius de 4 páginas em 270 avaliadas — as demais 266 páginas do corpus permanecem byte-idênticas. Ver `design.md` para a evidência completa (incluindo o refinamento do ponto de intervenção descoberto durante a implementação) e `proposal.md` para o relato completo de implementação/validação.

## 0. Diagnóstico (concluído)

- [x] 0.1 Baseline: `git status --short`, HEAD, `uv run pytest tests/`, `openspec validate --all --strict`. Resultado: repo limpo, HEAD `b30771d`, 364/364 testes passando, 1/1 spec válida (esta mudança, sem deltas, falha `validate --type change` por design — mudança diagnóstica sem capacidades novas, mesmo padrão dos precedentes arquivados). `design.md`, "Contexto".
- [x] 0.2 Inspecionar `012-015-Testamento Publico.pdf`, páginas 1–4, via `page.get_text("dict")` (PyMuPDF): blocos, bbox, direção de escrita, fonte, tamanho, texto — identificar estrutura real do texto vertical e confirmar ausência de duplicação geométrica (diferença em relação a `fix-rotated-digital-signature-noise`). `design.md`, ETAPA 1.
- [x] 0.3 Reproduzir localmente (sem OCR real) o algoritmo de agrupamento de `page.chars` (pdfplumber) usado por `markitdown_ocr/_pdf_converter_with_ocr.py`, lendo o código-fonte instalado do pacote; confirmar que `page.chars` está correto e que a fragmentação ocorre exclusivamente no agrupamento heurístico por coordenada Y; reconstruir deterministicamente o texto original a partir dos fragmentos. `design.md`, ETAPA 2.1–2.2.
- [x] 0.4 Classificar a origem do ruído (resposta OCR / texto nativo residual / combinação posterior) por leitura direta do código, não por inferência; reproduzir localmente a interleaving de `content_items` (texto + imagem, ordenados por `y_pos`) e confirmar contra o padrão real observado no Markdown já produzido pela validação arquivada. `design.md`, ETAPA 2.3–2.5.
- [x] 0.5 Avaliar critérios candidatos (direção isolada, sequência de linhas de 1 caractere, coordenada/eixo comum, continuidade espacial, posição relativa a `[End OCR]*`, origem em texto residual, combinações) sem usar nome de documento, número de página/processo, texto de assinatura ou nomes próprios. `design.md`, ETAPA 3.
- [x] 0.6 Medir blast radius dos critérios candidatos contra os 8 PDFs de referência (4 processuais + 4 canônicos, 270 páginas): roteamento completo por página, contagem de linhas verticais por PDF, identificação de todas as páginas `hibrido`/`ocr_integral` do corpus. Incluir explicitamente carimbos verticais legítimos da DECISÃO, a correção arquivada de blocos rotacionados duplicados, textos horizontais legítimos, páginas `texto_nativo` e híbridas. `design.md`, ETAPA 4.
- [x] 0.7 Comparar pontos de intervenção candidatos (antes do `markitdown-ocr`, composição interna do plugin, pós-processamento do residual geométrico comprovado) e recomendar o menor ponto que preserve integralmente o OCR substantivo, sem modificar o pacote de terceiros. `design.md`, ETAPA 5.
- [x] 0.8 Classificar a conclusão (A ou B) com causa raiz, estágio/função exatos, critério, blast radius, limitações e proposta mínima futura. `design.md`, "CONCLUSÃO".
- [x] 0.9 Confirmar não regressão: nenhum arquivo de `src/`, `tests/`, `output/`, `logs/` ou do corpus canônico alterado; nenhuma chamada de OCR/LLM; scripts de investigação mantidos fora do repositório (scratchpad da sessão). `design.md`, "Verificação de não regressão".

## 1. Encerramento do ciclo de diagnóstico

- [x] 1.1 Claude (orquestrador) executou todo o diagnóstico diretamente (sem Codex/OpenCode), por se tratar de tarefa de auditoria/análise geométrica e de código-fonte de terceiros, não de implementação de código em `src/`/`tests/` — consistente com `AGENTS.md`.
- [x] 1.2 Nenhum código, teste, dependência ou corpus canônico foi alterado; commit local (sem push) desta mudança, sem arquivar (instrução explícita do usuário).
- [x] 1.3 `LOOPS.md` atualizado com o resultado final deste diagnóstico ao encerrar este ciclo.

## 2. Implementação (TDD) — autorizada em 2026-08-12, ver `proposal.md` "Implementação"

> Codex é o implementador (cria testes + código); Claude (orquestrador) revisa o diff, executa os testes e valida antes de marcar concluído — nenhum agente marca sua própria subtarefa como concluída. Toda validação do caso real do Testamento reutiliza evidência já obtida (Markdown auditado + reprodução local de pdfplumber); nenhuma chamada real de OCR/LLM foi feita.

- [x] 2.1 Codex criou 21 testes RED em `tests/test_converter.py` para as funções ainda inexistentes (`_split_ocr_tail`, `_tail_fragments`, `_is_degenerate_fragment_tail`, `_non_horizontal_line_texts`, `_char_multiset_overlap`, `_geometrically_corroborated_vertical_residual`, `_replace_fragmented_vertical_residual`) e para a integração em `convert_document`: positivos (reconstrução do resíduo fragmentado, cobertura das 4 páginas do Testamento via mock de OCR, preservação do texto substantivo) e negativos obrigatórios (texto vertical legítimo não fragmentado, resíduo horizontal legítimo, fragmentos curtos sem corroboração geométrica, ausência do marcador, não-interferência na rota `texto_nativo`). `uv run pytest tests/test_converter.py -v`: 17 falharam (RED esperado, `AttributeError`), 22 preexistentes continuaram passando. Um bug de fixture foi identificado por mim antes da implementação (texto vertical do teste de integração abaixo do limiar de roteamento `hibrido`, roteava `ocr_integral`) e corrigido na etapa seguinte.
- [x] 2.2 Claude revisou os testes RED: cobertura completa do critério aprovado (posição + corroboração geométrica, nunca sequência de caracteres isolados isoladamente), nenhum texto `e-SAJ`/nome de arquivo/pessoa real. Bug de fixture comunicado ao Codex para correção junto da implementação.
- [x] 2.3 Codex implementou em `src/pipeline_juridico/converter.py` (primeira versão) e corrigiu o bug de fixture. `uv run pytest tests/`: 385/385 passando nessa primeira versão.
- [x] 2.4 Claude, revisando com dados 100% reais do Testamento (sem nenhuma chamada de OCR), descobriu que o ponto de intervenção da primeira versão (aplicado por página, antes da composição) causava `remove_repetitive_margins` remover em cascata a reconstrução E o próprio marcador `[End OCR]*` — violação da restrição do `/goal`. Instrução de correção enviada ao Codex com o refinamento arquitetural (cache de geometria durante o loop por página existente; aplicação da substituição depois de `remove_repetitive_margins`, sobre o documento composto).
- [x] 2.5 Codex refatorou: extraiu a lógica pura em `_replace_fragmented_vertical_residual_in_text`; manteve `_replace_fragmented_vertical_residual(raw_content, page_path)` como wrapper (testes existentes inalterados); adicionou `_replace_fragmented_vertical_residuals_in_document` (orquestradora pós-composição); moveu o ponto de integração em `convert_document` para depois de `remove_repetitive_margins`; corrigiu o fixture do teste de integração (texto substantivo variando por página, como no corpus real) e removeu o monkeypatch que mascarava `remove_repetitive_margins`; adicionou testes de regressão explícitos para a sobrevivência de `[End OCR]*` e para a não-interferência em páginas `texto_nativo`/`vazia`/`erro`. `uv run pytest tests/test_converter.py -v`: 39/39. `uv run pytest tests/`: **385/385 passando**.
- [x] 2.6 Claude revisou o diff completo linha a linha: escopo confinado a `converter.py` (novas funções privadas + 3 pontos de integração, todos no ramo `hibrido`/`ocr_integral` ou no cálculo de geometria por página já existente); nenhum sinal proibido usado; `cleaner.py`, `router.py`, `engines.py`, `config.py`, prompt e dependências intocados. Reexecução: 385/385.
- [x] 2.7 Dry run com dados 100% reais do Testamento (texto substantivo do Markdown já auditado + resíduo fragmentado reproduzido localmente via pdfplumber sobre o PDF real, OCR mockado para reproduzir exatamente esses insumos — nenhuma chamada real): 4/4 páginas `hibrido`, `[End OCR]*` = 4 (idêntico ao baseline), linhas de 1 caractere = 0 (baseline: ~836), resíduo reconstruído legível nas 4 páginas, conteúdo substantivo (nomes, CPF, valores) intacto. `proposal.md`, "Implementação".
- [x] 2.8 Reconversão isolada dos 4 PDFs de `input/processos_auditoria/` com `--no-ocr`: Testamento corretamente bloqueado (preexistente); os outros 3 com sucesso, 100% `texto_nativo`, zero OCR; `100-106-DECISÃO.md` manteve 94 linhas / 0 linhas de 1 caractere (correção `fix-rotated-digital-signature-noise` intacta).
- [x] 2.9 Regressão dos 4 PDFs canônicos com `--no-ocr`: os 4 arquivos resultantes são **byte-idênticos** aos já commitados em `output/`.
- [x] 2.10 Idempotência: segunda reconversão completa dos 7 PDFs bem-sucedidos, `diff -rq` byte-idêntica à primeira.
- [x] 2.11 Delta spec criado em `specs/juridical-pdf-conversion/spec.md` (`## ADDED Requirements`, requisito "Substituição geométrica de resíduo de texto rotacionado fragmentado na rota híbrida/OCR"), formalizando o comportamento implementado sem citar nome de documento, página/processo, pessoa ou texto literal de autenticação. `openspec validate --all --strict`: **2 passed, 0 failed**.
- [x] 2.12 `LOOPS.md` e `proposal.md`/`design.md`/esta `tasks.md` atualizados com o resultado da implementação. Commits locais (sem push), sem arquivar.
