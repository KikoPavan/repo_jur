## 0. Gating note

- C8.2 (process schema/profile + storage boundary) and C8.3 (Process Producer + storage adapter + CLI) were **gated on human approval of HR-3, HR-4, HR-5, HR-6 (and HR-7/HR-8)**; the approvals were recorded on planning card t_04603b78 on 2026-08-23 and are incorporated in this change (HR-1/HR-2 confirm the C8.1 scope). No Stage 8 task is implemented inside this planning change; implementation is a separately authorized task. The storage contract values referenced below (root `process/`, type vocabulary `Peticao | Contestacao | Decisao | Procuracao | Testamento | Anexo | OutraPeca`, profile fields) are the approved HR-3/HR-4/HR-5/HR-6 values.

## 1. C8.1 — Process Semantic Review seam: focused tests (TDD — red first)

- [ ] 1.1 Write failing tests for the input contract: the process review entry point accepts the Stage 3 `Phase1Artifacts` (literal Markdown + technical conversion report) read-only plus a review profile, and evaluates without requesting or performing further conversion, without resolving evidence, and without invoking OCR; a source-inspection test asserts `src/pipeline_juridico/process_semantic_review.py` contains no evidence-resolution, converter, engines, inspector, OCR, LLM, or semantic-model invocation path (no import of the converter, engines, inspector, evidence, OCR, or any external model client module).
- [ ] 1.2 Write failing tests for the post-gate invariant: an artifact whose technical report records `result.quality_gate` as `PASS` is eligible for review; `PASS_WITH_WARNINGS` is eligible; `FAIL` stops the review (deterministic blocked error, no review result); an unparseable report or a report without a recorded valid gate outcome is blocked; the gate outcome is read from the report, never from a caller-supplied override.
- [ ] 1.3 Write failing tests for the routing-decision contract: the review entry point accepts the existing Stage 6 routing decision and requires its target to be exactly `judicial_process`; a `legal_knowledge` or `review_required` decision raises the dedicated configuration error; an absent decision stops the run; the decision is never derived from the Markdown body, the report content, or any hint; a source-inspection test asserts the module contains no routing logic and no import of any content-classification path.
- [ ] 1.4 Write failing tests for Phase 1 immutability: `SHA256(phase1_artifacts.markdown)` before calling the review entry point equals the value after, and the serialized `phase1_artifacts.report_json` is byte-identical after review, across `OK`, `WARNING`, and `REVIEW_REQUIRED` review states; no review output (patches, warnings, state) appears in the literal Markdown body or in the report.
- [ ] 1.5 Write failing tests for the review result contract: the result exposes structured patches, extracted fields, classification suggestions, an immutable ordered tuple of warnings, and a review state that is exactly one of `OK`, `WARNING`, `REVIEW_REQUIRED`; the result is a pure function of the inputs (no execution id, timestamp, or duration in the decision-relevant output).
- [ ] 1.6 Write failing tests for word preservation on structural operations: a structural patch (separating/associating/reordering structural fields only) preserves every original word of the affected content — no word added, removed, summarized, paraphrased, translated, or invented, and no missing text completed; the preservation check is automatic and a patch that would violate it is rejected.
- [ ] 1.7 Write failing tests for structured patch provenance: every patch record carries `before`, `after`, `reason`, `confidence`, `page_refs`, and `evidence_refs` (page/evidence refs when supported); structured patches are preferred over full-document rewriting; a source-inspection test asserts the review module has no full-document overwrite path for the Phase 1 body.
- [ ] 1.8 Write failing tests for ambiguity semantics: a structural correction that cannot be made without inference yields review state `REVIEW_REQUIRED`, is not applied, and no text is invented or completed; a `REVIEW_REQUIRED` review result blocks any downstream Process Producer publication.
- [ ] 1.9 Write failing tests for the deterministic rule registry: rules require full provenance (`rule_id`, `rule_version`, `scope`, `source/specification`, `validation_logic_version`); a rule missing provenance is rejected at registration; duplicate rule identifiers are rejected; an empty registry is valid and review executes as a zero-rule run without structural modifications; a profile referencing an unregistered rule or duplicating an enabled rule identifier raises the dedicated configuration error; configuration errors are never converted into a successful review state.
- [ ] 1.10 Write failing tests for engine neutrality and no-LLM guarantees: the review executes through an engine seam with a deterministic default engine; a source-inspection test asserts `process_semantic_review.py` contains no LLM client, semantic-model client, external classification service, OCR provider, or conversion engine reference; review behavior does not change based on any engine/provider/model identity.
- [ ] 1.11 Write failing tests that review never writes: executing review for any review state performs no filesystem write to process-domain storage, no write to `repo_jur/bundle/`, and no invocation of any write guard; a source-inspection test asserts the review module contains no write path and no guard invocation.
- [ ] 1.12 Write failing tests for bounded-context isolation from Legal Knowledge: review never imports, constructs, or references a Legal Knowledge schema, Legal OKF Profile field, Legal classifier, or Legal enrichment model; a source-inspection test asserts no `legal_` module import, no Legal profile/type vocabulary reference, and no shared-index reference exists in the process review module.

## 2. C8.1 — Process Semantic Review seam: minimal implementation

- [ ] 2.1 Implement `src/pipeline_juridico/process_semantic_review.py` with the engine seam (`ProcessSemanticReviewEngine.review(phase1_artifacts, profile) -> ProcessReviewResult` per tech spec §3.5), reusing `Phase1Artifacts` from the shared contracts without modification, and the process review result model (patches, extracted fields, suggestions, warnings, state `OK`/`WARNING`/`REVIEW_REQUIRED`) with domain-isolated types.
- [ ] 2.2 Implement the deterministic default engine: a versioned rule registry mirroring the Stage 4/7 provenance discipline (rule registration validated at construction, immutable registry, empty-by-default), with structural corrections limited to boundary/field-association operations that preserve every original word; automatic word-preservation validation for every structural patch.
- [ ] 2.3 Implement the gate and routing-decision checks: parse the report read-only, read the recorded gate outcome from `result.quality_gate` (unparseable report or absent/invalid outcome → deterministic blocked error; `FAIL` → blocked error, no review result); require the consumed routing decision target to be exactly `judicial_process` (dedicated configuration error otherwise, absent decision stops); never accept caller-supplied overrides; never derive the route from content.
- [ ] 2.4 Implement ambiguity semantics: a structural operation that requires inference is not applied and yields review state `REVIEW_REQUIRED`; the review result never mutates `Phase1Artifacts`, never writes to process-domain storage or the bundle, and never invokes a write guard; no LLM/semantic-model reference and no Legal Knowledge reference exists anywhere in the module (source-inspection enforced).

## 3. C8.2 — Process schema/profile + storage boundary: focused tests (TDD — red first; gated on HR-3/HR-4/HR-5/HR-6)

- [ ] 3.1 Write failing tests for the validated process producer context: the context carries exactly the permitted keys — `type` (one of the approved process vocabulary) and `evidence_resource` (optional URI); an absent or invalid `type` raises the dedicated configuration error; an unknown key raises it; a structurally invalid context raises it; the error is never converted into a candidate, a publication, or a silent default.
- [ ] 3.2 Write failing tests for `type` authority and vocabulary: the process concept candidate's `type` equals exactly the explicit `type` from the producer context; no `type` is derived from the Markdown body, the technical report, or collector candidate hints; a value outside the approved vocabulary raises the dedicated configuration error; a non-authoritative classification suggestion conflicting with the explicit `type` routes the run to `REVIEW_REQUIRED` (no publication).
- [ ] 3.3 Write failing tests for the process frontmatter profile: the candidate has a valid YAML frontmatter block delimited by `---` with `type` as the first key, `generated.by` equal to the approved process producer actor (`repo_jur_process_producer/<version>`), `sources` present for identifiable sources, and the body following the frontmatter; `verified` is absent when no real verification event exists; process-profile fields are applied only for the applicable process type.
- [ ] 3.4 Write failing tests that the process profile never uses the Legal profile: a candidate or renderer that would apply a Legal OKF Profile field, a Legal type value, or a Legal schema key to a process concept fails validation; a source-inspection test asserts the process modules contain no Legal profile vocabulary and no Legal module import.
- [ ] 3.5 Write failing tests for PDF cardinality provenance: exactly 1 PDF → singular hash field with a lowercase 64-hex SHA-256 and no plural field; 2+ PDFs → plural mapping of each PDF `sources[].id` to its SHA-256 with no singular field; both fields together fail validation; every PDF source in a multi-PDF concept has an `id` present in the mapping; non-PDF sources stay in `sources` but never appear in the hash mapping.
- [ ] 3.6 Write failing tests for evidence-reference provenance: a PDF-derived process concept whose producer context supplies no evidence reference raises the dedicated configuration error (no invented resource, no publication); when supplied, `sources[].resource` records the reference and the report's `input.sha256` is preserved in the provenance fields; a reference that cannot be validated is a configuration error.
- [ ] 3.7 Write failing tests for the process-domain storage boundary: the process write guard authorizes a target only when the acting domain is `judicial_process` and the resolved target lies under the process root; any target under `repo_jur/bundle/` is denied regardless of acting domain; any non-process acting domain targeting the process root is denied; a source-inspection test asserts the process modules contain no bundle-write path and no invocation of the Legal bundle guard.
- [ ] 3.8 Write failing tests for process path resolution: the concept path is positional — `process/<type_dir>/<slug>.md` with deterministic slugs following the evidence-basename-or-hash discipline; no path escapes the process root; the type subdirectories are created only by Process Producer publication; `process/` is never created inside `bundle/`.
- [ ] 3.9 Write failing tests that process storage never intersects the bundle: after any Stage 8 capability executes against a repository containing a Legal bundle, the bundle is byte-identical; no file is ever written under `bundle/`; no shared Legal/Process index is created.

## 4. C8.2 — Process schema/profile + storage boundary: minimal implementation (gated on HR-3/HR-4/HR-5/HR-6)

- [ ] 4.1 Implement `src/pipeline_juridico/process_storage.py`: the approved process type vocabulary, the process frontmatter profile (OKF v0.2 base rules + process profile; no Legal fields), the positional path resolver for the process root, and the process write guard (acting domain `judicial_process` + process-root containment; bundle targets denied via the existing Legal guard semantics, never modified).
- [ ] 4.2 Implement the validated process producer context carrier with the permitted vocabulary `{type, evidence_resource}` and the approved process type vocabulary; dedicated configuration-error contract mirroring the Stage 4/6/7 patterns; never convert a malformed context into a silent default or publication.
- [ ] 4.3 Implement the process concept candidate render/parse (deterministic YAML subset) and candidate validation (YAML parse, process profile conformance, cardinality exclusivity, sources mapping, ownership rules, lossless round-trip), with the Legal-profile exclusion enforced.
- [ ] 4.4 Implement the process-domain storage root bootstrap (create `process/<type_dir>/` atomically at guarded publication only) and the state-directory enforcement (outside `bundle/` and outside process storage).

## 5. C8.3 — Process Producer core + storage adapter + CLI + observability: focused tests (TDD — red first; gated on HR-3/HR-4/HR-5/HR-6/HR-7/HR-8)

- [ ] 5.1 Write failing tests for the Producer input contract: the Producer entry point accepts the Phase 1 artifacts (read-only), the routing decision, the review result, and a validated process producer context; an artifact whose recorded gate outcome is `FAIL` or absent is blocked; a routing decision other than `judicial_process` (`legal_knowledge`, `review_required`, or absent) is a dedicated configuration error — Stage 8 never re-routes; the gate outcome and decision are never caller-overridden.
- [ ] 5.2 Write failing tests for Phase 1 immutability and candidate body preservation: `SHA256(markdown)` before the Producer run equals after; the serialized report is byte-identical; the rendered candidate body preserves the Phase 1 literal content including canonical `[[Pág. N]]` markers; no Stage 8 output enters the Markdown body or the report.
- [ ] 5.3 Write failing tests for lifecycle/ownership merge: regeneration loads the existing process concept first; Human-Owned fields and human-curated Shared Ownership values are preserved; Producer-Owned fields are recomputed deterministically; `status` is neither inserted nor mutated (absent stays absent, existing preserved); no `_v2`/UUID/stable-id artifact is created; unknown extension keys are preserved.
- [ ] 5.4 Write failing tests for `verified`/history policy: `verified` is never auto-created; a material change to the verified object archives only a real prior event into verification history preserving original `by`/`at` and recording `invalidated_by` and `reason`; `verified` is omitted when no active events remain; history never becomes active `verified`; a strictly technical change preserves active `verified`.
- [ ] 5.5 Write failing tests for `generated.at` semantics: a Producer run that does not meaningfully change content, ownership, or provenance does not update `generated.at`; a re-run with equivalent inputs produces no timestamp-only diff; a meaningful change updates `generated.at`.
- [ ] 5.6 Write failing tests for deterministic duplicate resolution: same physical evidence with equivalent canonical inputs/config/logical version and no meaningful change → NO-OP (no write, observability records the no-op); material change or unresolved ambiguity → HUMAN REVIEW (no write, observability records the requirement); a distinct/autonomous process document → distinct concept candidate under its own positional path; SHA-256 alone is never a no-op, never a rejection, and never legal identity; `status` is not altered and no `_v2` is created.
- [ ] 5.7 Write failing tests for candidate validation and atomic publication: an invalid candidate (bad YAML, missing `type`, cardinality violation, sources-mapping mismatch, ownership violation, Legal-profile contamination) is never published and leaves no partial file; publication validates first, writes a temp file on the same filesystem, flushes/fsyncs, and atomically renames; the write is authorized through the process write guard with acting domain `judicial_process`; a non-process acting domain is denied; a write targeting `bundle/` is denied; no automatic Git commit/push/merge occurs.
- [ ] 5.8 Write failing tests for determinism and idempotency: identical artifacts + review result + producer context yield identical candidate bytes on every evaluation; no execution id, timestamp, or duration influences the candidate; an equivalent re-run against an existing process concept produces no storage diff beyond the first run.
- [ ] 5.9 Write failing tests that the Process Producer never behaves as retrieval or Legal Knowledge: source-inspection asserts no retrieval index, no shared index, no chunking/reranking/search path, no Legal module/schema/profile import, no bundle-write path, and no invocation of the Legal bundle guard in `process_producer.py` and `process_storage.py`.
- [ ] 5.10 Write failing tests for the `repo-jur process build` command surface: invoking `process build <markdown.md> <report.json> --type <Tipo> [--evidence-resource <uri>]` reads both artifact files, computes the review and the rendered candidate, writes the observability record under the configured state directory (default `var/process/state/`, environment-overridable), and prints the candidate/outcome; `--context <process-context.json>` supplies the same validated carrier; invalid `--type`, unknown context keys, and structurally invalid contexts exit with a configuration error; `--json` emits machine-readable output.
- [ ] 5.11 Write failing tests that `process build` and `process validate` never write to process storage or the bundle: behavioral tests confirm no file is written under the process root or under `repo_jur/bundle/` for either command; a source-inspection test asserts the CLI has no direct write path outside the guarded publish path.
- [ ] 5.12 Write failing tests for the `repo-jur process publish` command surface: invoking `process publish <concept-candidate.md>` re-validates the candidate, publishes atomically through the process write guard into the process root, writes the Producer observability record (resolution outcome, materiality category, human-review requirement, publication result), and prints the publication result; a `REVIEW_REQUIRED` review state or a HUMAN REVIEW resolution exits non-zero and writes no process-storage file and no publish record.
- [ ] 5.13 Write failing tests for process CLI exit codes following the existing convention: `0` success; `1` input errors; `3` configuration/contract errors (invalid `--type`, unknown context key, missing evidence resource for PDF-derived concept, unreadable context/report, invalid report contract surface); `5` blocked (gate `FAIL`, non-process routing decision, `REVIEW_REQUIRED`, HUMAN REVIEW — no write); `2` unexpected errors; the `repo-jur route` and `repo-jur producer` codes remain unchanged.
- [ ] 5.14 Write failing tests that the process CLI never mutates the artifacts: after `process build`/`validate`/`publish` completes (success, blocked, or error), the literal Markdown file and the technical report file are byte-identical; the CLI never invokes conversion or OCR (source-inspection: no converter, engines, inspector, OCR, or evidence module imports beyond validation utilities).
- [ ] 5.15 Write failing observability tests at the CLI level: the records written by `process build` and `process publish` contain the resolution outcome, materiality category, human-review requirement, publication result, evidence provenance hash, gate outcome, routing decision, patch count, and `generated.at` action — and contain no document content, no full critical identifier value (process number, name, CPF/RG, selo), no patch body, no review patch content, and no secret or token; the state directory is rejected when it resolves inside the canonical bundle or inside process-domain storage.
- [ ] 5.16 Write failing CLI non-regression tests: the existing `converter-juridico` surface remains unchanged; the existing `repo-jur route` surface remains unchanged; the existing `repo-jur producer` surface remains unchanged; the full existing `tests/test_cli.py`, `tests/test_domain_router_cli.py`, and `tests/test_legal_producer_cli.py` suites pass without modification.

## 6. C8.3 — Process Producer core + storage adapter + CLI + observability: minimal implementation (gated on HR-3/HR-4/HR-5/HR-6/HR-7/HR-8)

- [ ] 6.1 Implement `src/pipeline_juridico/process_producer.py` with the Producer entry point: consume Phase 1 artifacts (read-only) + routing decision + review result + validated process producer context; re-read the recorded gate outcome from the report (blocked on `FAIL`/absent); enforce the routing decision is exactly `JUDICIAL_PROCESS` (dedicated configuration error otherwise); render the process concept candidate.
- [ ] 6.2 Implement the process candidate renderer per the approved profile: YAML frontmatter with `type` first, `generated.by = repo_jur_process_producer/<version>`, `sources` when identifiable, process-profile fields by type, PDF cardinality fields (singular/plural, mutually exclusive, sources-id mapping), preserved Phase 1 literal body with `[[Pág. N]]` markers; `verified` never fabricated; `status` never inserted/mutated; no `_v2`/UUID; no Legal profile field (validated).
- [ ] 6.3 Implement lifecycle/ownership merge per the conservative discipline (Decision 8): load existing concept, resolve identity/provenance, detect technical vs material change, recompute Producer-Owned, merge Shared/Human-Owned (preserving human curation), apply `verified`/history policy, apply body ownership, validate, publish.
- [ ] 6.4 Implement deterministic duplicate resolution per the conservative state machine (NO-OP / controlled update / NEW CONCEPT / HUMAN REVIEW), write-blocked on ambiguity, hash-as-physical-identity-only, no content-based safe-equivalence in v1.
- [ ] 6.5 Implement candidate validation (YAML parse, process profile conformance, cardinality exclusivity, sources mapping, ownership, Legal-profile exclusion) and atomic publication through the process write guard (acting domain `JUDICIAL_PROCESS`) into the process root, reusing `validator.py::write_atomic`; create the positional process tree only at publication; rely on the existing Legal guard semantics to deny any bundle target; no automatic Git commit/push.
- [ ] 6.6 Implement `src/pipeline_juridico/process_producer_cli.py` with argparse following the existing CLI conventions (environment-driven directories, `--log-level`, sanitized logging), exposing `repo-jur process build|validate|publish` with the documented flags; wire `--type`/`--evidence-resource`/`--context` into the effective validated process producer context with a dedicated configuration error on disagreement or unknown keys; wire `build` to the review seam + Producer render (never writing process storage or the bundle), `validate` to the candidate validator (never writing), and `publish` as the single write path through the process write guard; write the observability records under the state directory (default `var/process/state/`), enforced outside the bundle and outside process storage.
- [ ] 6.7 Register the new `repo-jur process` subcommands through the existing `repo-jur` entry point (`domain_router_cli.py`) mirroring the Stage 7 `producer` registration, without modifying `cli.py`, the `route` surface, or the `producer` surface; do not modify the `converter-juridico` entry point.

## 7. Validation

- [ ] 7.1 Run the focused Stage 8 tests (`uv run pytest tests/test_process_semantic_review.py tests/test_process_storage.py tests/test_process_producer.py tests/test_process_producer_cli.py -v`) — all green.
- [ ] 7.2 Run the full regression suite (`uv run pytest tests/`, with the gitignored `input/*.pdf` corpus fixtures present in the worktree) — all existing tests plus new Stage 8 tests pass with no behavioral change to Stages 1–7, and `tests/test_cli.py`, `tests/test_domain_router_cli.py`, and `tests/test_legal_producer_cli.py` pass unmodified (CLI non-regression).
- [ ] 7.3 Run OpenSpec strict validation for this change (`openspec validate 2026-08-22-stage8-judicial-process --type change --strict`) and the full strict validation (`openspec validate --all --strict`) — all passed, 0 failed.
- [ ] 7.4 Confirm the final diff: no FROZEN baseline modification, no `bundle/` content outside the Legal Producer's guarded publication, no process-domain content outside the Process Producer's guarded publication, no Stage 2–7 code or spec modification, no `router.py` change, no converter/OCR/thresholds/cleaner/validator/report-generation change, no `contracts.py`/`config.py` change, no `cli.py` change, no `route` or `producer` surface change, no dependency change, no LLM/semantic-model invocation, no retrieval/index/chunking/reranking implementation, no Stage 9/10 implementation, no automatic `verified`/`status`/`_v2`, and no new normative rule invented without a FROZEN citation or an approved human review decision (HR-1–HR-8); the only production-code additions are `process_semantic_review.py`, `process_storage.py`, `process_producer.py`, `process_producer_cli.py`, the four test modules, and the `process` subcommand registration.
