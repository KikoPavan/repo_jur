## 0. Baseline (orchestrator-recorded, before implementation)

- [x] 0.1 Record pre-change `uv run pytest -q` baseline: **1044 passed, 29 failed, 1 skipped, 2 warnings, 4 errors** (all pre-existing, caused by 4 missing gitignored real PDF fixtures: `L10.406_CC_2002.pdf`, `Inf0024E.pdf`, `AINTARESP_1462304-PA.pdf`, `REsp_1704551-SP.pdf`). Any count changing beyond newly added tests must be justified.

## 1. Segmentation module — core data model and pure function

- [x] 1.1 Create `tests/test_legal_source_segmentation.py` with synthetic fixtures (small in-line Markdown strings, no real PDFs) covering: (a) mono-concept document with no boundary rule match → `SINGLE`, one segment, body equals input verbatim; (b) two unambiguous "Edição n. N ... Brasília, DATA" boundaries → `SEGMENTS`, two segments, correct page ranges from `[[Pág. N]]` markers, no cross-segment content leakage, deterministic `segment_id`s (`segment-0001`, `segment-0002`); (c) two unambiguous "Tema Repetitivo N  Situação ...  Órgão ..." boundaries → `SEGMENTS`; (d) an internal `Tema n. N` / `Edição n. N` prose citation that does not match the exact boundary shape → does not create a boundary, document stays `SINGLE`; (e) a repeated running header (`EDIÇÃO N. N: TITLE`) appearing on every page of one unit → contributes no extra boundary beyond the once-per-unit dated line; (f) two different registered rules each matching once in the same document → `AMBIGUOUS` with reason identifying both rule ids; (g) [SUPERSEDED by Decision 7/Task 1ter — see below] page context is resolved from the document-wide positional page-map, not from a per-segment substring check; a segment body containing zero `[[Pág. N]]` markers of its own is an expected, valid shape (e.g. two records sharing one physical page) and does NOT make the outcome `AMBIGUOUS` by itself — only a boundary whose start position precedes every marker in the document-wide page-map (`page_at(start) is None`) is `AMBIGUOUS` with reason `unresolved_page_context`, and only a regressive/out-of-order page-map is `AMBIGUOUS` with reason `non_monotonic_page_markers`; (h) identical input segmented twice → identical `SegmentationResult` (determinism); (i) segmentation does not alter the SHA-256 of the input Markdown string.
- [x] 1.2 Implement `src/pipeline_juridico/legal_source_segmentation.py`: `SegmentationOutcome` enum (`single`/`segments`/`ambiguous`), `Segment` frozen dataclass (`segment_id`, `source_unit_label`, `body`, `page_start`, `page_end`, `boundary_rule_id`), `SegmentationResult` frozen dataclass (`outcome`, `segments`, `ambiguity_reason`), `SegmentationRule` frozen dataclass (`rule_id`, `rule_version`, `scope`, `source`, `validation_logic_version`, `detect: Callable[[str], list[BoundaryMatch]]`), `SegmentationRuleRegistry` (validates provenance at construction, defaults empty), and `segment_markdown(markdown: str, registry: SegmentationRuleRegistry) -> SegmentationResult` implementing Decisions 1-3 (page-context/ambiguity semantics superseded by Decision 7, Task 1ter — see below) from `design.md` exactly (no LLM, no `ProducerContext`, no report access). `segment_id` MUST be deterministic and position-derived (e.g. `f"segment-{position:04d}"`) and MUST NOT be treated anywhere as a legal identifier. Must not import `legal_producer.py` or `legal_semantic_review.py` (segmentation is a leaf module they both may depend on, not vice versa).
- [x] 1.3 Implement the two built-in rules (`jurisprudencia-em-teses-edicao-v1`, `precedentes-qualificados-tema-repetitivo-v1`) as a `DEFAULT_SEGMENTATION_REGISTRY` constant in the same module, with regexes anchored per `design.md` Decision 2 as corrected by Decision 2bis (asymmetric field separators: two ASCII spaces OR space+U+E205+space before `Situação`; two ASCII spaces OR one ASCII space before `Órgão`). Run `uv run pytest -q tests/test_legal_source_segmentation.py` until green.

## 1bis. Real-corpus correction — `precedentes-qualificados-tema-repetitivo-v1` field separator (post-implementation finding, same change)

- [x] 1bis.1 Orchestrator-recorded evidence (see `design.md` Decision 2bis): real-corpus validation of `output/STJ - Precedentes Qualificados.md` found the rule as specified in Decision 2/implemented in Task 1.3 matches **zero** times against the real document. Position-verified byte-level inspection of all 10 real header lines shows an asymmetric separator: between the record number and `Situação`, exactly the 3-character sequence space + U+E205 (Unicode Private Use Area) + space (` \uE205 `); between the status value and `Órgão`, an ordinary single ASCII space. Neither boundary uses two ASCII spaces as originally specified. Confirmed twice by direct byte-level/position inspection of the real file (not a synthetic fixture); the first transcription attempt was imprecise (incorrectly assumed U+E205 at both boundaries) and was corrected before this task was dispatched for implementation.
- [x] 1bis.2 Add RED tests in `tests/test_legal_source_segmentation.py` (or a clearly-named extension of it) covering, at minimum: (a) a header using the real corpus's exact separators (space+U+E205+space before `Situação`, single ASCII space before `Órgão`), with the complete structural shape → boundary recognized; (b) the equivalent header using two ASCII spaces at both boundaries (already covered by 1.1, re-asserted here to prove no regression) → still recognized; (c) two such real-shaped headers in one document → exactly two segments, correct page ranges, no leakage; (d) a bare U+E205 code point appearing inside unrelated body prose, with no surrounding structural shape → no boundary; (e) an internal running-prose citation `Tema Repetitivo N` (with or without a stray U+E205 nearby, in any spacing) that does not satisfy the full three-field/two-label shape on one physical line → no boundary; (f) an internal citation to a different Tema Repetitivo number inside a resolved segment's body → does not override or contribute to that segment's own `repo_jur_precedente_numero`; (g) a header missing a required separator at the correct boundary (e.g. a single space where space+U+E205+space is required, or vice versa), or missing a fixed label (`Situação`/`Órgão`) → does not match any accepted form, handled by existing fail-closed/no-boundary or `AMBIGUOUS` paths, never a silent partial split; (h) `[[Pág. N]]` markers and segment page ranges remain correctly derived per segment with the corrected regex; (i) `segment_id`/`source_unit_label` still never participate in `resolve_segment_identity()`'s returned fields for a segment produced from the real-shaped header. Run and confirm these fail (RED) against the pre-correction regex before implementing.
- [x] 1bis.3 Implement the minimal correction in `src/pipeline_juridico/legal_source_segmentation.py`: widen only the `precedentes-qualificados-tema-repetitivo-v1` rule's two field-separator sub-patterns independently to accept the corpus-evidenced literal forms at each boundary (number→`Situação`: two ASCII spaces OR space+U+E205+space; status→`Órgão`: two ASCII spaces OR one ASCII space) — do not loosen either boundary to `\s+`/`.*`/unrestricted whitespace, do not require symmetry between the two boundaries, do not touch the `jurisprudencia-em-teses-edicao-v1` rule, do not add any Phase 1 Markdown normalization/substitution step anywhere in the pipeline. Run the Task 1bis.2 tests until green, then re-run all of Section 1/2/3/4's existing tests to confirm zero regression.

## 1ter. Positional page-map correction — page context is a document-wide offset property, not a per-segment substring check (post real-corpus finding, same change)

- [x] 1ter.1 Orchestrator-recorded evidence (see `design.md` Decision 7): real-corpus validation of the corrected Task 1bis regex against `output/STJ - Precedentes Qualificados.md` surfaced a second, independent defect: `_page_range()` derives `page_start`/`page_end` only from `[[Pág. N]]` markers found inside each segment's own sliced body, never from the document's page-marker positions outside that slice. Position-verified inspection shows `Tema Repetitivo 978` and `Tema Repetitivo 999` both structurally begin between the same `[[Pág. 4]]` and `[[Pág. 5]]` markers (both belong to physical page 4), and `Tema Repetitivo 1039`/`Tema Repetitivo 1065` both begin between `[[Pág. 9]]` and the next marker (both belong to physical page 9) — but because Tema 999's body slice happens to contain the `[[Pág. 5]]` marker (its content continues onto page 5) and Tema 1065's slice likely behaves analogously at page 9/10, the old per-segment-substring `_page_range()` misattributes their `page_start` to the following page instead of the page their own header actually starts on. Confirmed by direct position inspection of the real file's marker offsets and boundary match offsets (not a synthetic fixture) before any code change.
- [x] 1ter.2 Add RED tests in `tests/test_legal_source_segmentation.py` (or a clearly-named extension) covering, at minimum, the ten scenarios listed in this task's dispatch instructions: (a) two segments sharing one page via a single `[[Pág. 4]]` marker → both resolve `page_start=page_end=4`; (b) three segments sharing one page → all three resolve the same page; (c) mixed same-page/different-page sequence (segment 1 after `[[Pág. 4]]`, segment 2 also on page 4, segment 3 after `[[Pág. 5]]`) → page spans `(4,4)`, `(4,4)`, `(5,5)`; (d) a segment crossing `[[Pág. 4]]`→`[[Pág. 5]]` → `page_start=4, page_end=5`; (e) every segment's `body` remains an exact, contiguous substring of the source Markdown, with no synthetic marker inserted; (f) a boundary occurring before any document-wide marker exists → `ambiguous` with reason `unresolved_page_context`; (g) a document-wide marker map with a regressive page number → `ambiguous` with reason `non_monotonic_page_markers`; (h) shared-page segments retain independent, correct `segment_id`/`source_unit_label`/resolved legal identity, unaffected by the page-sharing; (i) an internally-cited `Tema Repetitivo N` inside prose still does not create a boundary (regression guard, already covered by 1.1/1bis.2, re-asserted here); (j) all pre-existing 1/1bis tests remain green (no regression). Run and confirm these fail (RED) against the pre-correction per-segment-substring page logic before implementing.
- [x] 1ter.3 Implement the positional page-map model in `src/pipeline_juridico/legal_source_segmentation.py` per `design.md` Decision 7 exactly: build one document-wide `page_map` of `[[Pág. N]]` offsets per `segment_markdown()` call; validate monotonic page numbers (regression → `ambiguous`/`non_monotonic_page_markers`, checked before boundary processing); implement `page_at(position)` as a positional nearest-preceding-marker lookup; replace `_page_range()`'s per-segment-substring page resolution with `page_start = page_at(match.start)` / `page_end = page_at(next_match.start - 1 or len(markdown) - 1)`; remove the `empty_segment_body` ambiguity check entirely (superseded by `unresolved_page_context`, which fires only when `page_at(start)` is `None`); leave `Segment.body` slicing, the `SINGLE` outcome's whole-document min/max page resolution, and both boundary-detection regexes (Decision 2/2bis) completely untouched. Run the Task 1ter.2 tests until green, then re-run every test from Sections 1, 1bis, 2, 3, 4 to confirm zero regression.
- [x] 1ter.4 Re-run `uv run repo-jur producer analyze` against the real `output/STJ - Precedentes Qualificados.md` / its `logs/*.report.json` with `--type PrecedenteVinculante --json` and report, for all 10 Temas (692, 731, 954, 978, 999, 1014, 1016, 1031, 1039, 1065): `segment_id`, `page_start`/`page_end`, `identity_status`. Confirm exactly 10 boundaries/10 segments, Tema 978 and Tema 999 sharing the same `page_start` (physical page 4), Tema 1039 and Tema 1065 sharing the same `page_start` (physical page 9), and every segment's `identity_status: resolved` as `PrecedenteVinculante` only if its own body/header genuinely supports the canonical fields. Do not force the count or identity to match this expectation — if a real divergence is found, stop and report it explicitly instead of adjusting code to fit the expectation.



- [x] 2.1 Create/extend tests [tribunal resolution rule text superseded by Decision 11: `PrecedenteVinculante`'s `repo_jur_tribunal` is resolved from its own body only via classes A/B/C own-evidence (self-citation with matching own number, self-referential structural phrasing, or an institutional organ act naming this record's own number/no foreign Tema); cross-references (`Vide`, `Repercussão Geral Tema N/TRIB`), loose citations, and `Processo STJ|STF` mentions never resolve tribunal alone; textual/physical order is irrelevant] covering `resolve_segment_identity(concept_type, segment_body) -> IdentityResolution`: (a) a `PrecedenteVinculante`-shaped segment body whose own text is exactly one "Tema Repetitivo N Situação ... Órgão ..." record → `IdentityStatus.RESOLVED` with `repo_jur_precedente_numero` populated from that segment's own header, and `repo_jur_tribunal` populated only when the segment's own body independently carries class A/B/C own-evidence (not from the header alone); (b) that same segment body additionally containing an unrelated internal citation to a different Tema (e.g. "Tema em IRDR n. 11/TJSP") → identity is still `RESOLVED` from the segment's own record, unaffected by the internal citation; (c) a `TemaJuridico`-shaped segment body that is an editorial "Edição N" compilation citing 0, 1, or many distinct `Tema n. N` numbers purely as thesis grounding → `IdentityStatus.REQUIRES_OPERATOR_METADATA` (never `RESOLVED`, even when exactly one `Tema n. N` happens to be cited, because the segment itself is structurally an editorial compilation, not a single-theme record — implementer must encode this distinction explicitly, e.g. via the boundary rule id that produced the segment, not by re-guessing from citation count) with the identity reason naming the mismatch; (d) a segment body yielding two conflicting values for a field that must be singular (including genuine own-evidence for both STJ and STF for `PrecedenteVinculante`'s tribunal) → `IdentityStatus.AMBIGUOUS`; (e) `segment.segment_id`/`segment.source_unit_label` never appear in the returned `IdentityResolution.fields` mapping under any circumstance, even when a segment's label happens to contain a number that coincidentally matches an extracted field's value (assert the two are computed independently, not string-compared for "consistency"). Tests live in `tests/test_legal_source_segmentation.py`, not inside `legal_semantic_review.py`'s test file.
- [x] 2.2 Implement `IdentityStatus` enum and `IdentityResolution` frozen dataclass (`status`, `fields`, `reason`) and `resolve_segment_identity()` in `legal_segment_identity.py` (sibling module, importing `Segment`/`PRECEDENT_HEADER_PATTERN` from `legal_source_segmentation.py` per Decision 8), reusing the existing `_deterministic_extract()`/per-type field mapping from `legal_semantic_review.py`/`legal_producer.py` unchanged — no duplicate extraction logic. Explicitly encode Decision 6's finding: a segment produced by the `jurisprudencia-em-teses-edicao-v1` boundary rule is never eligible for `RESOLVED` `TemaJuridico` identity via the doctrinal/abstract absence allowance (its `boundary_rule_id` marks it as an editorial compilation, not a single doctrinal theme) — it always resolves to `REQUIRES_OPERATOR_METADATA` for `TemaJuridico`, regardless of how many/few `Tema n. N` citations happen to appear in its body. [SUPERSEDED by Decision 11 — see 2.1] A segment produced by `precedentes-qualificados-tema-repetitivo-v1` resolves its own `repo_jur_precedente_numero`/`repo_jur_precedente_status` from its own fixed-header content directly; `repo_jur_tribunal` is resolved only via the pure own-evidence classes A/B/C scan of the full segment body (not from the header, not by the generic first-occurrence scanner used for other concept types). Run targeted tests until green.

## 1quater. Shared structural parser — boundary detection and identity resolution converge on one canonical Tema Repetitivo header definition (post 1ter finding, same change)

- [x] 1quater.1 Orchestrator-recorded evidence (see `design.md` Decision 8): re-running `producer analyze` against the real `output/STJ - Precedentes Qualificados.md` after Task 1ter shows all 10 real `Tema Repetitivo` headers are correctly detected as boundaries (10 segments, correct shared/crossing page ranges), but every one of the 10 segments resolves `identity_status: requires_operator_metadata` via `resolve_segment_identity()`. Root cause, confirmed by direct inspection of `legal_segment_identity.py`'s `_OWN_PRECEDENT_HEADER` regex against `legal_source_segmentation.py`'s `_PRECEDENT` regex: the two modules independently define the same real-world header structure, and `_OWN_PRECEDENT_HEADER` was never updated for Decision 2bis's corpus-evidenced U+E205-flanked separator form, so it matches zero times against the real segments' own bodies even though the boundary regex matches all 10.
- [x] 1quater.2 Add RED tests (in `tests/test_legal_source_segmentation.py` and/or `tests/test_legal_producer.py`, implementer's choice, but must assert the shared-parser property directly, not just end-to-end resolution) covering, at minimum: (a) a segment whose own body starts with the real U+E205-flanked header → `identity_status=resolved`, `repo_jur_precedente_numero` equal to the header's own number; (b) the ASCII two-space structural variant → also `resolved`; (c) the correct number is extracted for each of two segments with distinct own headers → two distinct `repo_jur_precedente_numero` values, no cross-contamination; (d) an internal citation to a different `Tema Repetitivo` number inside a segment's body does not change that segment's own resolved number; (e) a segment containing only a bare in-prose `Tema Repetitivo N` citation and no own header → does not resolve (`requires_operator_metadata` or `ambiguous`, per existing missing-field rules); (f) a truncated/incomplete own header (missing a required label or separator) → does not resolve; (g) two segments with distinct own headers resolve two distinct identities in the same `segment_markdown()` call; (h) the same compiled structural pattern object (or an assertion that both call sites use one shared, single-source-of-truth pattern/constant) is used by both `segment_markdown()`'s boundary detection and `resolve_segment_identity()`'s field extraction — assert this structurally (e.g. importing the one exported pattern into the test and confirming both code paths reference it), not just via matching behavior; (i) all pre-existing Section 1/1bis/1ter tests remain green (regression guard). Run and confirm these fail (RED) against the pre-correction two-independent-regex state before implementing.
- [x] 1quater.3 Implement the minimal shared-parser correction per `design.md` Decision 8: define exactly one exported, capturing structural pattern for the `Tema Repetitivo` fixed tabular header in `legal_source_segmentation.py` (the module that already owns the corpus-evidenced structural anchor), reuse it unchanged for the existing boundary detector, and import it into `legal_segment_identity.py` to replace `_OWN_PRECEDENT_HEADER` entirely (delete the duplicate, do not keep both). Do not introduce any new normalization, `\s+`/`.*` widening, or symmetry requirement between the two field separators; do not change `jurisprudencia-em-teses-edicao-v1`; do not invert the module dependency direction (identity may import from segmentation, not vice versa); identity extraction continues to read only `Segment.body`, never `segment_id`/`source_unit_label`/boundary metadata as a shortcut. Run the Task 1quater.2 tests until green, then re-run every test from Sections 1, 1bis, 1ter, 2, 3, 4 to confirm zero regression.
- [x] 1quater.4 Re-run `uv run repo-jur producer analyze "output/STJ - Precedentes Qualificados.md" "logs/STJ - Precedentes Qualificados.report.json" --type PrecedenteVinculante --json` and report, for all 10 real Temas (692, 731, 954, 978, 999, 1014, 1016, 1031, 1039, 1065): `segment_id`, `source_unit_label`, `page_start`/`page_end`, `identity_status`, `repo_jur_precedente_numero`, and any other canonical fields resolved. Do not force the result — if any of the 10 still fails to resolve for a reason attributable to the shared-parser correction itself (as opposed to a genuinely distinct, newly-discovered corpus shape), report it explicitly rather than adjusting code to fit the expectation.
- [x] 1quater.5 Orchestrator-recorded evidence and no-code-change verification of the AIRESP trailing-newline finding (see `design.md` Decision 9): confirmed by direct byte comparison — the freshly rendered `Jurisprudencia`/`single` candidate for `AIRESP-1833684-2020-02-12` and the already-published `bundle/jurisprudencia/stj_agint_no_recurso_especial_no_1833684_sc.md` are byte-identical up to the final byte; the published file ends `...611\n\n` (two trailing newlines) where the freshly rendered candidate ends with one. This delta pre-exists this change and is not introduced/widened/narrowed by segmentation or the shared-parser correction; `write_atomic()` and `ConceptCandidate.render_text()` were not modified. Documented as pre-existing and out of scope, per instruction.

## 1quinquies. Segment prefix preservation — `_deterministic_extract()` must analyze content before its own first internal `[[Pág. N]]` marker (post 1quater finding, same change)

- [x] 1quinquies.1 Orchestrator-recorded evidence (see `design.md` Decision 10): re-running `producer analyze` against the real `output/STJ - Precedentes Qualificados.md` after Task 1quater shows 6 of 10 segments (978, 999, 1014, 1016, 1039, 1065) resolve `identity_status: resolved`, but 4 (692, 731, 954, 1031) remain `requires_operator_metadata`. Root cause, confirmed by direct offset inspection: `_deterministic_extract()`'s page-splitting step in `legal_semantic_review.py` derives page text only from content strictly after each `[[Pág. N]]` marker found inside the string it receives; when a `Segment.body` begins mid-page (its first internal marker belongs to the *next* physical page, because the segment's own content continues onto that page), the entire genuinely-owned prefix before that marker — confirmed to include the segment's own STJ/STF tribunal evidence for these 4 segments — is silently discarded before any field-extraction regex runs. This is invisible for whole-document calls (Phase 1 always starts at `[[Pág. 1]]`) and invisible for the 6 segments whose own tribunal evidence happens to also appear after their first internal marker.
- [x] 1quinquies.2 Add RED tests in `tests/test_legal_semantic_review.py` and/or `tests/test_legal_source_segmentation.py`/`tests/test_legal_producer.py` (implementer's choice per module boundary) covering, at minimum, the 9 TDD scenarios from this task's dispatch instructions: (a) a body `"Tema Repetitivo ... STJ ... [[Pág. 5]] restante"` → the prefix before the marker participates in extraction (e.g. `repo_jur_tribunal` resolves from the prefix); (b) a segment starting mid-page with no marker before its own header → fields present only in the prefix are not lost; (c) two segments sharing one page, second segment's extraction uses only its own prefix/body, no leakage from the first segment's text; (d) prefix + a following marker → the prefix's content is not duplicated into the post-marker page's text or double-counted; (e) a segment whose entire body has no internal marker but has valid page context → extraction still succeeds (regression guard for Decision 7's zero-marker segments, e.g. Tema 978/1039 shape); (f) a mono-concept document starting at `[[Pág. 1]]` → identical behavior to before this change (regression guard); (g) a prefix containing an internal citation to a different tribunal → does not override the segment's own unambiguous tribunal identity; (h) multiple genuinely conflicting tribunals with no structural rule to pick one → stays `ambiguous`/`requires_operator_metadata`, never picks the first occurrence; (i) all pre-existing Section 1/1bis/1ter/1quater tests remain green. Run and confirm these fail (RED) against the pre-correction page-splitting logic before implementing.
- [x] 1quinquies.3 Implement the minimal correction in `legal_semantic_review.py::_deterministic_extract()` per `design.md` Decision 10 exactly: when the received string contains at least one `[[Pág. N]]` marker and that marker's start offset is greater than 0, prepend one leading `(page_num, page_text)` tuple covering the genuine prefix before the first internal marker, using an optional keyword-only `leading_page: str | int | None = None` parameter (defaulting to the first internal marker's own page number as a label when not supplied) so every existing whole-document call site is unaffected (the prefix is always empty for whole-document calls, so this branch is inert there). Update `legal_segment_identity.py::resolve_segment_identity()` to pass `leading_page=segment.page_start` (already computed by `legal_source_segmentation.py`'s document-wide page map/`page_at()` from Decision 7 — reused by contract, not recomputed) when given a `Segment`. Do not insert any synthetic `[[Pág. N]]` marker into `body`; do not read outside `Segment.body`'s own offsets; do not duplicate `_deterministic_extract()`'s per-type field logic into a second parallel code path; do not change `_page_range()`/`page_at()` in `legal_source_segmentation.py` (unaffected, already correct per Decision 7). Run the Task 1quinquies.2 tests until green, then re-run every test from Sections 1, 1bis, 1ter, 1quater, 2, 3, 4 to confirm zero regression.
- [x] 1quinquies.4 Re-run `uv run repo-jur producer analyze "output/STJ - Precedentes Qualificados.md" "logs/STJ - Precedentes Qualificados.report.json" --type PrecedenteVinculante --json` and report, for all 10 real Temas (692, 731, 954, 978, 999, 1014, 1016, 1031, 1039, 1065): `segment_id`, `page_start`/`page_end`, `identity_status`, `repo_jur_precedente_numero`, `repo_jur_precedente_status` (if applicable), `repo_jur_tribunal`, and the segment-body evidence that supports each field. Give explicit, individual attention to Tema 1014 (previously resolved `repo_jur_tribunal: STF` while the majority resolved `STJ`): determine and report whether the segment's own body genuinely supports STF as its canonical tribunal or whether the value came from an internal citation captured indevidamente, distinct from any correction made by this task. Do not force any of the 10 to resolve — if one remains unresolved, report the exact missing-evidence category and stop.

## 1sexies. PrecedenteVinculante tribunal own-evidence resolution + document trailer boundary (final authorized subtask, same change)

- [x] 1sexies.1 Record the pre-code real-corpus `producer analyze` result and inspect the exact Tema 1065 trailer structure.
- [x] 1sexies.2 Add RED tests for all ten tribunal evidence/cross-reference/conflict/order cases and update PrecedenteVinculante fixtures that relied on a weak bare court mention.
- [x] 1sexies.3 Implement the pure PrecedenteVinculante-only own-evidence tribunal resolver without changing generic extraction for other concept types; run targeted tests to GREEN.
- [x] 1sexies.4 Add RED tests for the seven trailer requirements, including real Tema 1065 content/page preservation and absence of synthetic markers.
- [x] 1sexies.5 Implement the optional, rule-specific two-anchor trailer end boundary with fail-safe EOF behavior; run targeted and related suites to GREEN.
- [x] 1sexies.6 Update Decisions 6/11/12 and the legal-knowledge delta requirements/scenarios without changing canonical specs.
- [x] 1sexies.7 Re-run all three authorized real-corpus analyze cases, document evidence/cross-references for all ten Temas, and run every final validation command.

## 3. Producer integration — segment-scoped candidate rendering gated on resolved identity

- [x] 3.1 Add tests in `tests/test_legal_producer.py`: (a) `_base_candidate(..., segment=None)` behaves byte-identically to current behavior (regression guard) — mono-concept path, identity, and slug-fallback rules for the whole-document case are completely unchanged; (b) `_base_candidate(..., segment=resolved_segment)` renders a candidate whose body equals `segment.body`, not the whole `artifacts.markdown`, using only identity fields resolved from that segment; (c) two segments from the same synthetic multi-unit fixture, both PDF-derived and both `RESOLVED`, render two candidates sharing the same `repo_jur_pdf_hash` but resolving to two distinct `path`s (no collision, no `_find_migration_collision` false-positive); (d) attempting to build a candidate for a segment whose identity resolution is `REQUIRES_OPERATOR_METADATA` or `AMBIGUOUS` raises `LegalProducerBlockedError(reason="identity_unresolved")` and renders no candidate — no path is computed via `segment.source_unit_label`/`segment_id`/ordinal fallback; (e) the whole-document `_slug(evidence_resource, report)` fallback in `resolve_concept_path()` is exercised only for the `segment=None` (whole-document) path and is proven NOT reachable when a `segment` is provided and unresolved (i.e. the segment code path never calls `_slug` with segment data as a substitute).
- [x] 3.2 Implement: extend `_base_candidate()` with an optional `segment: Segment | None = None` parameter; when provided, call `resolve_segment_identity(context.type, segment.body)` first — if not `RESOLVED`, raise `LegalProducerBlockedError(reason="identity_unresolved")` immediately, before any path resolution; if `RESOLVED`, use `segment.body` in place of `artifacts.markdown` for both `_deterministic_extract()` input and the final `ConceptCandidate.body`, and resolve the path using only the resolved identity fields (existing `resolve_concept_path()`/`_resolve_legal_identity()` logic, unchanged — no new fallback parameter is added to those functions). Run targeted tests until green.

## 4. Producer CLI — `analyze` (with identity_status), `--segment`, `--all-segments`

- [x] 4.1 Add tests in `tests/test_legal_producer_cli.py`: (a) `producer analyze --type TIPO` on a synthetic `SINGLE` fixture reports outcome `single`, no state/bundle writes; (b) `producer analyze --type TIPO` on a synthetic `SEGMENTS` fixture (mixed: one segment `RESOLVED`, one `REQUIRES_OPERATOR_METADATA`) reports the correct segment count and, per segment, `segment_id`, `source_unit_label`, page range, boundary rule id, `identity_status`, and either `identity_fields` (when resolved) or an identity reason (when not) — no state/bundle writes; (c) `producer analyze` without `--type` fails with a configuration error (type is now required for analyze); (d) `producer analyze` on a synthetic `AMBIGUOUS` fixture reports outcome `ambiguous` and the reason, no segment list; (e) `producer build` on a `SINGLE` fixture without new flags is byte-identical to pre-change output (regression guard, exact JSON keys `candidate`/`concept_path`/`record_path`); (f) `producer build` on a `SEGMENTS` fixture without `--segment`/`--all-segments` blocks with `EXIT_BLOCKED` and reason `segmentation_multi_concept`, no candidate rendered; (g) `producer build --segment SEGMENT_ID` on a `RESOLVED` segment renders only that segment's candidate; (h) `producer build --segment SEGMENT_ID` on a `REQUIRES_OPERATOR_METADATA`/`AMBIGUOUS` segment blocks with `EXIT_BLOCKED` and reason `identity_unresolved`, and the response contains no candidate and no path derived from the segment's label; (i) `producer build --all-segments` on a mixed fixture (some resolved, some not) renders `candidates: [...]` only for resolved segments and `blocked_segments: [...]` (each with `segment_id` and reason) for the rest, in one response; (j) `producer build --segment ...`/`--all-segments` against a `SINGLE` fixture raises `LegalProducerConfigurationError`/`EXIT_CONFIG`; (k) `producer build` on an `AMBIGUOUS` fixture blocks with `EXIT_BLOCKED` and reason `segmentation_ambiguous` regardless of flags; (l) `_candidate_gate()` correctly locates a `PASS`/`PASS_WITH_WARNINGS` record when multiple segment-scoped `producer.build` records share one provenance sha256 (publish-side regression guard).
- [x] 4.2 Implement `analyze` subparser (with **required** `--type`) + `_run_analyze()` in `legal_producer_cli.py` (read-only: reads Markdown+report, calls `segment_markdown`, calls `resolve_segment_identity()` per segment, emits via existing `_emit()` helper, writes nothing).
- [x] 4.3 Implement `--segment`/`--all-segments` on the `build` subparser and extend `_run_build()`: call `segment_markdown()` first; branch on outcome (`single` unchanged; `segments` requires exactly one of the two flags or blocks with `segmentation_multi_concept`; `ambiguous` always blocks with `segmentation_ambiguous`); for a selected/each segment, call `resolve_segment_identity()` before building — render a candidate only when `RESOLVED`, otherwise record that segment as blocked with `identity_unresolved` (never substitute a label/ordinal path); for `--all-segments`, assemble `candidates: [...]` plus `blocked_segments: [...]` in the JSON/text output. Run targeted tests until green.

## 5. Full verification

- [x] 5.1 Run `uv run pytest -q tests/test_legal_source_segmentation.py tests/test_legal_producer.py tests/test_legal_producer_cli.py -v` and report the exact pass count. Result: 127 passed (with `tests/test_legal_semantic_review.py` added, 162 passed total across the four focal files).
- [x] 5.2 Run `uv run pytest -q` (full suite) and diff the pass/fail/skip/error counts against the Section 0 baseline; any delta beyond the newly added tests passing must be explained. Result: 1115 passed, 29 failed, 1 skipped, 4 errors. The 29 failed + 4 errors are the same pre-existing real-PDF-fixture-dependent tests as the Section 0 baseline (`L10.406_CC_2002.pdf`, `Inf0024E.pdf`, `AINTARESP_1462304-PA.pdf`, `REsp_1704551-SP.pdf` gitignored/missing); passed count rose from 1044 to 1115 (net +71 from newly added segmentation/identity/producer/CLI tests), no new failures.
- [x] 5.3 Run `uv run repo-jur test conformance`, `uv run pytest -m conformance`, `uv run pytest -m regression` and compare against baseline (same 4 pre-existing missing-fixture errors expected, no new failures). Result: `repo-jur test conformance` exits 2 with category `ENVIRONMENT_CONFIGURATION_ERROR` (known pre-existing gate, not a code regression); `pytest -m conformance` → 10 passed; `pytest -m regression` → 4 errors, same missing-fixture `UsageError` as baseline (identical 4 filenames), no new failures.
- [x] 5.4 Run `openspec validate --all --strict` and `openspec validate multi-concept-legal-source-segmentation --strict`. Result: both pass — `multi-concept-legal-source-segmentation` valid; all-strict Totals: 13 passed, 0 failed.
- [x] 5.5 Run `git diff --check`. Result: exit 0, no output (clean, no trailing whitespace/conflict markers).

## 6. Real-corpus validation (analyze/build only — no publish, no OCR, no Phase 1 changes)

- [x] 6.1 Run `uv run repo-jur producer analyze output/Jurisprudencia_Teses_D.P.C._ed.171.md logs/Jurisprudencia_Teses_D.P.C._ed.171.report.json --type TemaJuridico --json` and report: outcome, segment count, every segment's `segment_id`/`source_unit_label`/page range/boundary rule id/`identity_status`. Result confirmed exactly as expected per Decision 6: outcome `segments`, 16 segments (segment-0001..0016, Edições 171–?, correct page ranges), every one of the 16 `identity_status: requires_operator_metadata` with reason `jurisprudencia_em_teses_edicao_not_valid_temajuridico` (no segment resolves as `TemaJuridico`).
- [x] 6.2 Run the equivalent `analyze` against `output/STJ - Precedentes Qualificados.md` / `logs/STJ - Precedentes Qualificados.report.json` with `--type PrecedenteVinculante --json` and report the same details. Result confirmed exactly as expected: outcome `segments`, 10 segments (Temas 692, 731, 954, 978, 999, and the remaining 5), each `identity_status: resolved` with its own `repo_jur_precedente_numero`/`repo_jur_tribunal` (all resolved `STJ` via own-evidence per Decision 11)/`repo_jur_precedente_status`, no cross-contamination from `Tema em IRDR n. 11/TJSP`.
- [x] 6.3 [SUPERSEDED — Decision 9's SINGLE/candidate byte-comparison must be made at the same pipeline stage, not against the on-disk published artifact directly] Re-ran `uv run repo-jur producer analyze "output/AIRESP-1833684-2020-02-12.md" "logs/AIRESP-1833684-2020-02-12.report.json" --type Jurisprudencia --json`: confirmed segmentation outcome `single`, `identity_status: resolved` (`repo_jur_processo_numero`/`repo_jur_tribunal: STJ`), unchanged before/after this change (byte/semantically identical candidate body/metadata/path at the `build`-candidate stage). Direct byte comparison of the freshly rendered candidate against the already-published `bundle/jurisprudencia/stj_agint_no_recurso_especial_no_1833684_sc.md` confirms both are byte-identical up to the final byte: the published file carries one additional trailing newline (`...611\n\n`) that predates this change (Decision 9), caused by the writer/serialization stage, not by segmentation and not a regression introduced here.
- [x] 6.4 Do not run `producer publish` against either multi-unit real document during this task. If 6.2 confirms `PrecedenteVinculante` segments resolve cleanly, note in the final report that publishing them (as a separate, explicitly human-authorized follow-up action) would be architecturally sound; do not publish them as part of this task without further explicit authorization. Confirmed: no `producer publish` was run against either multi-unit document; publishing all 10 `PrecedenteVinculante` segments is architecturally sound (all resolved) but requires separate human authorization.
- [x] 6.5 Confirm the Section 6.1 finding matches Decision 6 exactly: report definitively whether, under the current OKF profile, "Jurisprudência em Teses" edições are valid `TemaJuridico` concepts — expected answer: **no**, because they are editorial compilations, not official numbered themes, and this must be stated plainly rather than worked around. Confirmed: answer is **no**, matching Decision 6 exactly; all 16 real segments resolve `requires_operator_metadata`, none forced or worked around.

## 7. Orchestrator-only follow-up (not part of Codex's implementation scope)

- [x] 7.1 Claude reviews the full diff line-by-line, independently re-runs every command in Sections 5-6, and confirms: no Phase 1 mutation, no silent type inference, no boundary derived from first-citation, no segment label/id ever used as identity or path fallback, correct per-segment identity/provenance, mono-concept regression-free, no multi-concept collapsed to one file, zero real publications to `bundle/` during this task. Confirmed: `git status --short` shows the pre-existing untracked `bundle/jurisprudencia/stj_agint_no_recurso_especial_no_1833684_sc.md` from an earlier, unmodified `publish` run (not written by this task); no `producer publish` was invoked against any multi-unit document; `resolve_segment_identity()`/`_base_candidate()` never read `segment_id`/`source_unit_label` into identity fields (verified by code inspection and by 3.1/2.1's tests).
- [x] 7.2 Claude prepares the final report per the user's requested structure (root cause, architecture decision, OpenSpec created, files changed, CLI/API for 1:N, test counts, three real-case results including per-segment identity_status, segment counts/pages/identities, residual risks, `git diff --check`, `git status --short`, READY/NOT READY). No commit, archive, or push without explicit human authorization.
