## 1. C7.1 — Legal Semantic Review seam: focused tests (TDD — red first)

- [ ] 1.1 Write failing tests for the input contract: the review entry point accepts the Stage 3 `Phase1Artifacts` (literal Markdown + technical conversion report) read-only plus a review profile, and evaluates without requesting or performing further conversion, without resolving evidence, and without invoking OCR; a source-inspection test asserts `src/pipeline_juridico/legal_semantic_review.py` contains no evidence-resolution, converter, engines, inspector, OCR, LLM, or semantic-model invocation path (no import of the converter, engines, inspector, evidence, OCR, or any external model client module).
- [ ] 1.2 Write failing tests for the post-gate invariant: an artifact whose technical report records `result.quality_gate` as `PASS` is eligible for review; `PASS_WITH_WARNINGS` is eligible; `FAIL` stops the review (deterministic blocked error, no review result); an unparseable report or a report without a recorded valid gate outcome is blocked; the gate outcome is read from the report, never from a caller-supplied override.
- [ ] 1.3 Write failing tests for Phase 1 immutability: `SHA256(phase1_artifacts.markdown)` before calling the review entry point equals the value after, and the serialized `phase1_artifacts.report_json` is byte-identical after review, across `OK`, `WARNING`, and `REVIEW_REQUIRED` review states; no review output (patches, warnings, state) appears in the literal Markdown body or in the report.
- [ ] 1.4 Write failing tests for the review result contract: the result exposes structured patches, extracted fields, classification suggestions, an immutable ordered tuple of warnings, and a review state that is exactly one of `OK`, `WARNING`, `REVIEW_REQUIRED`; the result is a pure function of the inputs (no execution id, timestamp, or duration in the decision-relevant output).
- [ ] 1.5 Write failing tests for word preservation on structural operations: a structural patch (separating/associating/reordering structural fields only) preserves every original word of the affected content — no word added, removed, summarized, paraphrased, translated, or invented, and no missing text completed; the preservation check is automatic and a patch that would violate it is rejected.
- [ ] 1.6 Write failing tests for structured patch provenance: every patch record carries `before`, `after`, `reason`, `confidence`, `page_refs`, and `evidence_refs` (page/evidence refs when supported); structured patches are preferred over full-document rewriting; a source-inspection test asserts the review module has no full-document overwrite path for the Phase 1 body.
- [ ] 1.7 Write failing tests for ambiguity semantics: a structural correction that cannot be made without inference yields review state `REVIEW_REQUIRED`, is not applied, and no text is invented or completed; a `REVIEW_REQUIRED` review result blocks any downstream Producer publication.
- [ ] 1.8 Write failing tests for the deterministic rule registry: rules require full provenance (`rule_id`, `rule_version`, `scope`, `source/specification`, `validation_logic_version`); a rule missing provenance is rejected at registration; duplicate rule identifiers are rejected; an empty registry is valid and review executes as a zero-rule run without structural modifications; a profile referencing an unregistered rule or duplicating an enabled rule identifier raises the dedicated configuration error; configuration errors are never converted into a successful review state.
- [ ] 1.9 Write failing tests for engine neutrality and no-LLM guarantees: the review executes through an engine seam with a deterministic default engine; a source-inspection test asserts `legal_semantic_review.py` contains no LLM client, semantic-model client, external classification service, OCR provider, or conversion engine reference; review behavior does not change based on any engine/provider/model identity.
- [ ] 1.10 Write failing tests that review never writes: executing review for any review state performs no filesystem write to `repo_jur/bundle/`, no write to judicial-process storage, and no invocation of `guard_legal_bundle_write`; a source-inspection test asserts the review module contains no bundle-write path and no guard invocation.
- [ ] 1.11 Write failing tests for bounded-context isolation: review never imports, constructs, or references a Judicial-Process schema, classifier, or enrichment model; a source-inspection test asserts no `judicial`, `process_schema`, or shared-index reference exists in the review module.

## 2. C7.1 — Legal Semantic Review seam: minimal implementation

- [ ] 2.1 Implement `src/pipeline_juridico/legal_semantic_review.py` with the engine seam (`LegalSemanticReviewEngine.review(phase1_artifacts, profile) -> ReviewResult` per tech spec §3.5), reusing `Phase1Artifacts` from the shared contracts without modification, and the review result model (patches, extracted fields, suggestions, warnings, state `OK`/`WARNING`/`REVIEW_REQUIRED`).
- [ ] 2.2 Implement the deterministic default engine: a versioned rule registry mirroring the Stage 4 provenance discipline (rule registration validated at construction, immutable registry, empty-by-default), with structural corrections limited to boundary/field-association operations that preserve every original word; automatic word-preservation validation for every structural patch.
- [ ] 2.3 Implement the gate check: parse the report read-only, read the recorded gate outcome from `result.quality_gate` (unparseable report or absent/invalid outcome → deterministic blocked error; `FAIL` → blocked error, no review result); never accept a caller-supplied gate override.
- [ ] 2.4 Implement ambiguity semantics: a structural operation that requires inference is not applied and yields review state `REVIEW_REQUIRED`; the review result never mutates `Phase1Artifacts` and never writes to the bundle; no LLM/semantic-model reference exists anywhere in the module (source-inspection enforced).

## 3. C7.2 — Legal Producer core: focused tests (TDD — red first)

- [ ] 3.1 Write failing tests for the Producer input contract: the Producer entry point accepts the Phase 1 artifacts (read-only), a routing decision, the review result, and a validated producer context; an artifact whose recorded gate outcome is `FAIL` or absent is blocked; a routing decision other than `legal_knowledge` (`judicial_process`, `review_required`, or absent) is a dedicated configuration error — Stage 7 never re-routes; the gate outcome and decision are never caller-overridden.
- [ ] 3.2 Write failing tests for the validated producer context: the context carries exactly the permitted keys — `type` (one of `Legislacao`, `Jurisprudencia`, `TemaJuridico`, `PrecedenteVinculante`) and `evidence_resource` (optional URI); an absent or invalid `type` raises the dedicated configuration error; an unknown key raises it; a structurally invalid context raises it; the error is never converted into a candidate, a publication, or a silent default.
- [ ] 3.3 Write failing tests for `type` authority: the concept candidate's `type` equals exactly the explicit `type` from the producer context; no `type` is derived from the Markdown body, the technical report, or collector candidate hints; a non-authoritative classification suggestion conflicting with the explicit `type` routes the run to `REVIEW_REQUIRED` (no publication).
- [ ] 3.4 Write failing tests for Phase 1 immutability and candidate body preservation: `SHA256(markdown)` before the Producer run equals after; the serialized report is byte-identical; the rendered candidate body preserves the Phase 1 literal content including canonical `[[Pág. N]]` markers; no Stage 7 output enters the Markdown body or the report.
- [ ] 3.5 Write failing tests for the OKF candidate render: the candidate has a valid YAML frontmatter block delimited by `---` with `type` as the first key, `generated.by` equal to `repo_jur_producer/<version>`, `sources` present for identifiable sources, and the body following the frontmatter; `verified` is absent when no real verification event exists; domain-specific profile fields are applied only for the applicable type.
- [ ] 3.6 Write failing tests for PDF cardinality provenance: exactly 1 PDF → `repo_jur_pdf_hash` with a lowercase 64-hex SHA-256 and no `repo_jur_pdf_hashes`; 2+ PDFs → `repo_jur_pdf_hashes` mapping each PDF `sources[].id` to its SHA-256 with no `repo_jur_pdf_hash`; both fields together fail validation; every PDF source in a multi-PDF concept has an `id` present in the mapping; non-PDF sources stay in `sources` but never appear in the hash mapping.
- [ ] 3.7 Write failing tests for evidence-reference provenance: a PDF-derived concept whose producer context supplies no evidence reference raises the dedicated configuration error (no invented resource, no publication); when supplied, `sources[].resource` records the reference and the report's `input.sha256` is preserved in the provenance fields; a reference that cannot be validated is a configuration error.
- [ ] 3.8 Write failing tests for lifecycle/ownership merge: regeneration loads the existing concept first; Human-Owned fields and human-curated Shared Ownership values are preserved; Producer-Owned fields are recomputed deterministically; `status` is neither inserted nor mutated (absent stays absent, existing preserved); no `_v2`/UUID/stable-id artifact is created; unknown extension keys are preserved.
- [ ] 3.9 Write failing tests for `verified`/history policy: `verified` is never auto-created; a material change to the verified object archives only a real prior event into `repo_jur_verification_history` preserving original `by`/`at` and recording `invalidated_by` and `reason`; `verified` is omitted when no active events remain; history never becomes active `verified`; a strictly technical change preserves active `verified`.
- [ ] 3.10 Write failing tests for `generated.at` semantics: a Producer run that does not meaningfully change content, ownership, or provenance does not update `generated.at`; a re-run with equivalent inputs produces no timestamp-only diff; a meaningful change updates `generated.at`.
- [ ] 3.11 Write failing tests for deterministic duplicate resolution: same physical evidence with equivalent canonical inputs/config/logical version and no meaningful change → NO-OP (no write, observability records the no-op); material change or unresolved ambiguity → HUMAN REVIEW (no write, observability records the requirement); a distinct/autonomous act → distinct concept candidate under its own positional path; SHA-256 alone is never a no-op, never a rejection, and never legal identity; `status` is not altered and no `_v2` is created.
- [ ] 3.12 Write failing tests for candidate validation and atomic publication: an invalid candidate (bad YAML, missing `type`, cardinality violation, sources-mapping mismatch, ownership violation) is never published and leaves no partial file; publication validates first, writes a temp file on the same filesystem, flushes/fsyncs, and atomically renames; the write is authorized through `guard_legal_bundle_write` with acting domain `LEGAL_KNOWLEDGE`; a non-legal acting domain is denied; no automatic Git commit/push/merge occurs.
- [ ] 3.13 Write failing tests for bundle path resolution: the concept path is positional per `concept-identity-physical-structure-v1.3` — `bundle/legislacao/`, `bundle/jurisprudencia/`, `bundle/temas/`, `bundle/precedentes/` with deterministic slugs following the lowercase/transliteration/underscore rules; no path escapes the bundle root; the canonical tree is created only by Producer publication.
- [ ] 3.14 Write failing tests for determinism and idempotency: identical artifacts + review result + producer context yield identical candidate bytes on every evaluation; no execution id, timestamp, or duration influences the candidate; an equivalent re-run against an existing concept produces no canonical diff beyond the first run.
- [ ] 3.15 Write failing tests that the Producer never behaves as retrieval or Judicial Process: source-inspection asserts no retrieval index, no shared index, no process-storage path, and no Judicial-Process schema reference in `legal_producer.py`.

## 4. C7.2 — Legal Producer core: minimal implementation

- [ ] 4.1 Implement `src/pipeline_juridico/legal_producer.py` with the Producer entry point: consume Phase 1 artifacts (read-only) + routing decision + review result + validated producer context; re-read the recorded gate outcome from the report (blocked on `FAIL`/absent); enforce the routing decision is exactly `LEGAL_KNOWLEDGE` (dedicated configuration error otherwise); render the concept candidate.
- [ ] 4.2 Implement the validated producer context carrier with the permitted vocabulary `{type, evidence_resource}` and the four-type vocabulary; dedicated configuration-error contract mirroring the Stage 4/6 patterns; never convert a malformed context into a silent default or publication.
- [ ] 4.3 Implement the OKF candidate renderer: YAML frontmatter with `type` first, `generated.by = repo_jur_producer/<version>`, `sources` when identifiable, domain-specific profile fields by type, PDF cardinality fields (singular/plural, mutually exclusive, sources-id mapping), preserved Phase 1 literal body with `[[Pág. N]]` markers; `verified` never fabricated; `status` never inserted/mutated; no `_v2`/UUID.
- [ ] 4.4 Implement lifecycle/ownership merge per `lifecycle-field-ownership-v1.4` §4: load existing concept, resolve identity/provenance, detect technical vs material change, recompute Producer-Owned, merge Shared/Human-Owned (preserving human curation), apply `verified`/history policy, apply body ownership, validate, publish.
- [ ] 4.5 Implement deterministic duplicate resolution per `decision-memo-duplicate-act-handling-v1.0` (NO-OP / REGENERATE-UPDATE / ADD SOURCE-UPDATE CARDINALITY / NEW CONCEPT / HUMAN REVIEW), write-blocked on ambiguity, hash-as-physical-identity-only.
- [ ] 4.6 Implement candidate validation (YAML parse, OKF conformance, Legal OKF Profile fields, cardinality exclusivity, sources mapping, ownership) and atomic publication through `guard_legal_bundle_write(acting_domain=RouteTarget.LEGAL_KNOWLEDGE, ...)` reusing `validator.py::write_atomic`; create the positional bundle directory tree only at publication; no automatic Git commit/push.

## 5. C7.3 — Operational producer CLI + observability: focused tests (TDD — red first)

- [ ] 5.1 Write failing tests for the `repo-jur producer build` command surface: invoking `producer build <markdown.md> <report.json> --type <Tipo> [--evidence-resource <uri>]` reads both artifact files, computes the review and the rendered candidate, writes the observability record under the configured state directory (default `var/producer/state/`, environment-overridable), and prints the candidate/outcome; `--context <producer-context.json>` supplies the same validated carrier; invalid `--type`, unknown context keys, and structurally invalid contexts exit with a configuration error; `--json` emits machine-readable output.
- [ ] 5.2 Write failing tests that `producer build` and `producer validate` never write to `bundle/`: behavioral tests confirm no file is written under `repo_jur/bundle/` for either command; a source-inspection test asserts the CLI has no direct bundle-write path outside the guarded publish path.
- [ ] 5.3 Write failing tests for the `repo-jur producer publish` command surface: invoking `producer publish <concept-candidate.md>` re-validates the candidate, publishes atomically through the write guard into `bundle/`, writes the Producer observability record (resolution outcome, materiality category, human-review requirement, publication result), and prints the publication result; a `REVIEW_REQUIRED` review state or a HUMAN REVIEW resolution exits non-zero and writes no bundle file and no publish record.
- [ ] 5.4 Write failing tests for producer CLI exit codes following the existing convention: `0` success; `1` input errors; `3` configuration/contract errors (invalid `--type`, unknown context key, missing evidence resource for PDF-derived concept, unreadable context/report, invalid report contract surface); `5` blocked (gate `FAIL`, non-legal routing decision, `REVIEW_REQUIRED`, HUMAN REVIEW — no write); `2` unexpected errors; the `repo-jur route` codes remain unchanged.
- [ ] 5.5 Write failing tests that the producer CLI never mutates the artifacts: after `producer build`/`validate`/`publish` completes (success, blocked, or error), the literal Markdown file and the technical report file are byte-identical; the CLI never invokes conversion or OCR (source-inspection: no converter, engines, inspector, OCR, or evidence module imports beyond validation utilities).
- [ ] 5.6 Write failing observability tests at the CLI level: the records written by `producer build` and `producer publish` contain the resolution outcome, materiality category, human-review requirement, publication result, evidence provenance hash, gate outcome, routing decision, patch count, and `generated.at` action — and contain no document content, no full critical identifier value, no patch body, no review patch content, and no secret or token; the state directory is rejected when it resolves inside the canonical bundle (reusing `config.py::ensure_outside_canonical_bundle`).
- [ ] 5.7 Write failing CLI non-regression tests: the existing `converter-juridico` surface (positional PDF argument, `--overwrite`, `--allow-partial`, `--no-ocr`, `--keep-temp`, `--log-level`, environment-driven `OUTPUT_DIR`/`LOGS_DIR`/`TEMP_DIR`, exit codes 0/1/2/3/4) remains unchanged; the existing `repo-jur route` surface (exit codes 0/1/2/3/5) remains unchanged; the full existing `tests/test_cli.py` and `tests/test_domain_router_cli.py` suites pass without modification.

## 6. C7.3 — Operational producer CLI + observability: minimal implementation

- [ ] 6.1 Implement `src/pipeline_juridico/legal_producer_cli.py` with argparse following the existing CLI conventions (environment-driven directories, `--log-level`, sanitized logging), exposing `repo-jur producer build|validate|publish` with the documented flags; wire the `--type`/`--evidence-resource` flags onto the validated producer context; merge `--type`/`--evidence-resource` with `--context` into the effective validated context with a dedicated configuration error on disagreement or unknown keys.
- [ ] 6.2 Wire `build` to the review seam + Producer render: load and validate the artifact files and the optional context, run the review, render the candidate, write the observability record atomically under the state directory (reusing `validator.py::write_atomic`), print the candidate/outcome, return the documented exit codes; never write to `bundle/`.
- [ ] 6.3 Wire `validate` to the candidate validator: load the candidate file, run the full validation (YAML/OKF/profile/cardinality/sources/ownership), print the outcome, return the documented exit codes; never write to `bundle/`.
- [ ] 6.4 Wire `publish` as the single write path: re-validate the candidate, authorize the target through `guard_legal_bundle_write(acting_domain=RouteTarget.LEGAL_KNOWLEDGE, ...)`, publish atomically (creating the positional bundle tree as needed), write the Producer observability record, print the publication result, return the documented exit codes; blocked/HUMAN-REVIEW runs exit non-zero without writing a bundle file or publish record; no automatic Git commit/push.
- [ ] 6.5 Register the new `repo-jur producer` subcommands through the existing `repo-jur` entry point (`domain_router_cli.py`) or a subcommand-dispatched entry module without modifying `cli.py` or the `route` surface; do not modify the `converter-juridico` entry point.

## 7. Validation

- [ ] 7.1 Run the focused Stage 7 tests (`uv run pytest tests/test_legal_semantic_review.py tests/test_legal_producer.py tests/test_legal_producer_cli.py -v`) — all green.
- [ ] 7.2 Run the full regression suite (`uv run pytest tests/`, with the gitignored `input/*.pdf` corpus fixtures present in the worktree) — all existing tests plus new Stage 7 tests pass with no behavioral change to Stages 1–6, and `tests/test_cli.py` and `tests/test_domain_router_cli.py` pass unmodified (CLI non-regression).
- [ ] 7.3 Run OpenSpec strict validation for this change (`openspec validate stage7-legal-knowledge --type change --strict`) and the full strict validation (`openspec validate --all --strict`) — all passed, 0 failed.
- [ ] 7.4 Confirm the final diff: no FROZEN baseline modification, no `bundle/` content outside the Producer's guarded publication, no Stage 2–6 code or spec modification, no `router.py` change, no converter/OCR/thresholds/cleaner/validator/report-generation change, no `contracts.py`/`config.py` change, no `cli.py` change, no `route` surface change, no dependency change, no LLM/semantic-model invocation, no Stage 8/9 implementation, no automatic `verified`/`status`/`_v2`, and no new normative rule invented without a FROZEN citation or an approved human review decision (HR-1–HR-6); the only production-code additions are `legal_semantic_review.py`, `legal_producer.py`, `legal_producer_cli.py`, the three test modules, and the `producer` subcommand registration.
