## 1. Focused Stage 4 tests (TDD — red first)

- [x] 1.1 Write failing tests for the deterministic registry lifecycle: constructing `CriticalDataValidator` with rules carrying all six provenance fields (`rule_id`, `rule_version`, `applies_to`, source/specification reference, `validation_logic_version`, `failure_status`) and unique `rule_id` values succeeds and stores them keyed by `rule_id`; supplying a rule missing any one of those fields raises `CriticalValidationConfigurationError` and the rule is never evaluated; supplying a rule whose `failure_status` is outside `WARNING` or `REVIEW_REQUIRED` (e.g. `OK`) raises `CriticalValidationConfigurationError` and the rule is never evaluated; supplying two rules with the same `rule_id` raises `CriticalValidationConfigurationError` (Rule Registry Integrity); constructing with no rules yields an empty registry.
- [x] 1.2 Write failing tests for the zero-rule default (Decision 1): `CriticalDataValidator.validate(phase1_artifacts, profile)` constructed with an empty registry returns `CriticalValidationResult(status=CriticalValidationStatus.OK, findings=[])` for representative `Phase1Artifacts` (native-text page, OCR page, hybrid page, blank page); the `OK` result asserts only that no inconsistency was detected.
- [x] 1.3 Write failing test for the non-mutation invariant: `SHA256(phase1_artifacts.markdown)` before calling `validate()` equals `SHA256(phase1_artifacts.markdown)` after, both for the zero-rule path and for a path using a test-only provenance-complete rule that produces `WARNING`/`REVIEW_REQUIRED` findings.
- [x] 1.4 Write failing test that findings never appear as literal text inside the Markdown body: using a test-only rule that produces findings, assert no finding `code` or `message` substring appears in `phase1_artifacts.markdown`.
- [x] 1.5 Write failing domain-neutrality source-inspection test mirroring `tests/test_conversion_engine.py:298-306`: reading `src/pipeline_juridico/critical_data.py` as text contains no `GateState` and no `RouteTarget` reference.
- [x] 1.6 Write failing tests for the Critical Validation Profile (Decision 7 and Rule Registry Integrity): a profile with `profile_id`, `profile_version`, and `enabled_rule_ids` selects which registered rules run (a test-only rule runs only when its `rule_id` is in `enabled_rule_ids`); a profile with no enabled rules yields `OK`/`findings=[]`; an `enabled_rule_id` that does not resolve to exactly one registered rule raises `CriticalValidationConfigurationError` and is never silently converted to `status=OK`; a profile whose `enabled_rule_ids` contains the same `rule_id` more than once raises `CriticalValidationConfigurationError` before any rule executes (no duplicate rule execution, no duplicate findings).
- [x] 1.7 Write failing tests for static severity and no downgrade (Decisions 2 and 3): a test-only rule declaring `failure_status=WARNING` yields result status `WARNING`; a rule declaring `failure_status=REVIEW_REQUIRED` yields `REVIEW_REQUIRED`; the validator never invents a severity and never returns a status lower than a reported finding's declared `failure_status`.
- [x] 1.8 Write failing tests for aggregation (Decision 4): zero findings ⇒ `OK`; only `WARNING` findings ⇒ `WARNING`; any `REVIEW_REQUIRED` finding ⇒ `REVIEW_REQUIRED`; all findings are preserved individually in the result.
- [x] 1.9 Write failing test for per-rule candidate discovery (Decision 5): a test-only rule deterministically discovers its own candidates from `Phase1Artifacts` via its `evaluate` entry point without any central extractor, and the discovery does not modify the Markdown.
- [x] 1.10 Write failing test that a `REVIEW_REQUIRED` or `WARNING` result from a test-only rule does not construct, import, or return any `GateState` or `RouteTarget` value.
- [x] 1.11 Write failing tests for rule-scoped detection (2026-08-20 human review correction): with no enabled rule whose specification authorizes detection of ambiguity or conflicting values for a field, validation performs no such detection for that field (no general discovery duty); when an enabled test-only rule does signal a conflict within its authorized scope, the signal is represented only via `WARNING`/`REVIEW_REQUIRED` with a finding — never by silently choosing a value; redundant-value comparison is never performed.

## 2. Minimal implementation

- [x] 2.1 Implement `src/pipeline_juridico/critical_data.py` with the deterministic registry lifecycle: `CriticalDataValidator` receives the full set of rules at construction, validates the six provenance fields including `failure_status` (which must be `WARNING` or `REVIEW_REQUIRED`) and unique `rule_id`, raises `CriticalValidationConfigurationError` for missing provenance, an invalid `failure_status` value, or duplicate `rule_id`, stores the validated rules in an immutable internal mapping keyed by `rule_id`, and defaults to an empty registry when constructed with no rules.
- [x] 2.2 Implement `CriticalDataValidator.validate(self, phase1_artifacts, profile) -> CriticalValidationResult`, reusing `CriticalValidationStatus`, `CriticalFinding`, and `CriticalValidationResult` from `src/pipeline_juridico/contracts.py` without modification, and reading `phase1_artifacts.markdown` / `phase1_artifacts.report_json` read-only.
- [x] 2.3 Implement the minimal rule, profile, and error representations per the design: `CriticalValidationRule` (six declared fields including `failure_status` restricted to `WARNING` or `REVIEW_REQUIRED`, read-only spec-scoped `evaluate`), `CriticalValidationProfile` (`profile_id`, `profile_version`, `enabled_rule_ids` with no duplicate rule identifiers), and `CriticalValidationConfigurationError`, per Decisions 6, 7, 12, and 13.
- [x] 2.4 Implement profile resolution and aggregation in the validator: enable exactly the registered rules named in `enabled_rule_ids`; raise `CriticalValidationConfigurationError` for an `enabled_rule_id` that does not resolve to exactly one registered rule or for duplicate `enabled_rule_ids` within the profile (before any rule executes, never `status=OK`); compute the global status as the highest severity produced (Decision 4) without ever downgrading a finding's declared `failure_status` (Decision 3); never perform rule-independent ambiguity/conflict detection or redundant-value comparison (Decision 14).
- [x] 2.5 Ensure `critical_data.py` never mutates or reconstructs `Phase1Artifacts`, never imports `GateState` or `RouteTarget`, and never writes to `repo_jur/bundle/`.

## 3. Validation

- [x] 3.1 Run focused Stage 4 tests (`uv run pytest tests/test_critical_data.py -v`) — all green.
- [x] 3.2 Run the full regression suite (`uv run pytest tests/`) — all existing tests plus new Stage 4 tests pass with no behavioral change to Stage 1-3.
- [x] 3.3 Run OpenSpec strict validation (`openspec validate stage4-critical-data-validation --type change --strict` and `openspec validate --all --strict`) — all passed, 0 failed.
- [x] 3.4 Confirm no FROZEN baseline modification, no `bundle/` write, no Stage 5+ implementation, no normative rule invented without cited provenance, no Rule Registry Integrity violation, and no dependency change in the final diff.
