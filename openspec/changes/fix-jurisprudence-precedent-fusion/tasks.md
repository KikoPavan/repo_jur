> Nota geral: toda reconversão do corpus mencionada neste arquivo DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar.

## 1. Correção da fusão indevida entre precedentes

- [ ] 1.1 Criar teste de regressão em `tests/test_cleaner.py` que reproduza exatamente o caso relatado (bloco terminado em citação de julgamento com turma/seção e `DJe`, opcionalmente seguido de "(Grifos acrescidos).", seguido de bloco em caixa alta iniciando um novo precedente) e confirmar que o teste falha antes da correção (os dois blocos são hoje indevidamente unidos).
- [ ] 1.2 Implementar em `recompose_native_paragraphs` (`src/pipeline_juridico/cleaner.py`) a menor condição adicional de não-junção que cubra o padrão geral (fechamento de referência jurisprudencial + novo bloco em caixa alta), sem alterar `formal_structure_pattern`, `bare_structure_pattern`, `qualified_structure_pattern` nem qualquer exceção já corrigida em R01 (Art. 44 §2º, Art. 593, Art. 1.458, Art. 1.368-F).
- [ ] 1.3 Executar a suíte completa (`uv run pytest tests/`) e confirmar que o novo teste passa e que os testes existentes de R01 e das demais exceções de não-junção continuam passando.
- [ ] 1.4 Reconverter os 4 PDFs do corpus fixo com `converter-juridico --no-ocr`, comparar com a saída atual (antes/depois) e confirmar: (a) os dois precedentes de `AINTARESP_1462304-PA.pdf` permanecem separados; (b) os 4 casos positivos do R01 continuam corretos; (c) os marcadores `[[Pág. N]]` permanecem sequenciais e intactos; (d) nenhuma página exige OCR sob `--no-ocr`; (e) nenhuma outra separação/junção do corpus é alterada além do caso corrigido.
