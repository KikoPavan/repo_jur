> Mudança exclusivamente diagnóstica (instrução explícita do usuário: `/goal` "SOMENTE DIAGNÓSTICO... Não implemente código. Não crie testes TDD de produção. Não arquive."). Conclusão: **A) CRITÉRIO SEGURO ENCONTRADO** — os achados C.1 e C.2 de `audit-judicial-process-pdf-support` (`100-106-DECISÃO.pdf`, páginas 1–5) compartilham a mesma causa raiz (duplicação geométrica de um bloco de texto vertical, mal interpretada pelo MarkItDown) e o mesmo mecanismo de não-correção (as duas salvaguardas de *fallback* geométrico já existentes no pipeline, ambas neutralizadas pela mesma inflação de tokens). Um único critério geométrico — bloco não horizontal com bbox quase idêntico a outro bloco não horizontal na mesma página — resolve ambos, com blast radius de 5 páginas em 270 avaliadas e zero falsos positivos medidos. Nenhuma implementação foi feita. Ver `design.md` para a evidência completa e `proposal.md` para o resumo executivo.

## 0. Diagnóstico (concluído)

- [x] 0.1 Baseline: `git status --short`, HEAD, `uv run pytest tests/`, `openspec validate --all --strict`. Resultado: repo limpo, HEAD `881d9cd`, 354/354 testes passando, 1/1 spec válida (esta mudança, sem deltas, falha `validate --type change` por design — mudança diagnóstica sem capacidades novas, mesmo padrão do precedente arquivado). `design.md`, "Contexto".
- [x] 0.2 Inspecionar `100-106-DECISÃO.pdf`, páginas 1–7, via `page.get_text("dict")`/`"blocks"`: blocos, bbox, direção de escrita, fonte, tamanho. Identificar os blocos responsáveis pelas linhas espúrias e comparar páginas 1–5 (afetadas) com 6–7 (não afetadas). `design.md`, ETAPA 1.
- [x] 0.3 Capturar a saída bruta do MarkItDown (antes de qualquer cleaner deste pipeline) para páginas afetadas e não afetadas isoladamente; determinar padrão de repetição, relação geométrica com o carimbo, e causa do espaçamento duplo. `design.md`, ETAPA 2.
- [x] 0.4 Rastrear o ruído pelas etapas do pipeline (extração, `_has_native_reading_order_defect`, `recompose_native_paragraphs`, `remove_repetitive_margins`, demais cleaners) instrumentando diretamente as funções já existentes (sem alterá-las); identificar o primeiro estágio responsável e por que as salvaguardas existentes não disparam. `design.md`, ETAPA 3.
- [x] 0.5 Avaliar critérios candidatos (rotação isolada, posição marginal, sequência de caracteres únicos, duplicação geométrica) e simular blast radius contra os 8 PDFs de referência (4 processuais + 4 canônicos, 270 páginas), sem usar nome do documento, número de página, texto de assinatura, nomes de pessoas ou número do processo. `design.md`, ETAPA 4.
- [x] 0.6 Verificar separadamente se o espaçamento duplo (C.2) desaparece ao corrigir C.1 corretamente ou exige regra própria, com evidência quantitativa (não por proximidade visual). `design.md`, ETAPA 5.
- [x] 0.7 Classificar a conclusão (A ou B) com causa raiz, critério, ponto de implementação, positivos/negativos, blast radius, relação C.1/C.2 e proposta mínima futura. `design.md`, "CONCLUSÃO".
- [x] 0.8 Confirmar não regressão: nenhum arquivo de `src/`, `tests/`, `output/`, `logs/` ou do corpus canônico alterado; nenhuma chamada de OCR/LLM. `design.md`, "Verificação de não regressão".

## 1. Encerramento do ciclo

- [x] 1.1 Claude (orquestrador) executou todo o diagnóstico diretamente (sem Codex/OpenCode), por se tratar de tarefa de auditoria/análise geométrica, não de implementação de código em `src/`/`tests/` — consistente com `AGENTS.md`.
- [x] 1.2 Nenhum código, teste, dependência ou corpus canônico foi alterado; commit local (sem push) desta mudança, sem arquivar (instrução explícita do usuário).
- [ ] 1.3 Atualizar `LOOPS.md` com o resultado deste diagnóstico e a proposta mínima futura (mudança OpenSpec de implementação candidata para C.1/C.2), a critério do orquestrador ao final do ciclo.
