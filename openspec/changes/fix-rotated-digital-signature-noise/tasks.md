> Mudança exclusivamente diagnóstica (instrução explícita do usuário: `/goal` "SOMENTE DIAGNÓSTICO... Não implemente código. Não crie testes TDD de produção. Não arquive."). Conclusão: **A) CRITÉRIO SEGURO ENCONTRADO** — os achados C.1 e C.2 de `audit-judicial-process-pdf-support` (`100-106-DECISÃO.pdf`, páginas 1–5) compartilham a mesma causa raiz (duplicação geométrica de um bloco de texto vertical, mal interpretada pelo MarkItDown) e o mesmo mecanismo de não-correção (as duas salvaguardas de *fallback* geométrico já existentes no pipeline, ambas neutralizadas pela mesma inflação de tokens). Um único critério geométrico — bloco não horizontal com bbox quase idêntico a outro bloco não horizontal na mesma página — resolve ambos, com blast radius de 5 páginas em 270 avaliadas e zero falsos positivos medidos. Nenhuma implementação foi feita. Ver `design.md` para a evidência completa e `proposal.md` para o resumo executivo.

## 0. Diagnóstico (concluído)

- [x] 0.1 Baseline: `git status --short`, HEAD, `uv run pytest tests/`, `openspec validate --all --strict`. Resultado: repo limpo, HEAD `881d9cd`, 354/354 testes passando, 1/1 spec válida (esta mudança, sem deltas, falha `validate --type change` por design — mudança diagnóstica sem capacidades novas, mesmo padrão do precedente arquivado). `design.md`, "Contexto".
- [x] 0.2 Inspecionar `100-106-DECISÃO.pdf`, páginas 1–7, via `page.get_text("dict")`/`"blocks"`: blocos, bbox, direção de escrita, fonte, tamanho. Identificar os blocos responsáveis pelas linhas espúrias e comparar páginas 1–5 (afetadas) com 6–7 (não afetadas). `design.md`, ETAPA 1.
- [x] 0.3 Capturar a saída bruta do MarkItDown (antes de qualquer cleaner deste pipeline) para páginas afetadas e não afetadas isoladamente; determinar padrão de repetição, relação geométrica com o carimbo, e causa do espaçamento duplo. `design.md`, ETAPA 2.
- [x] 0.4 Rastrear o ruído pelas etapas do pipeline (extração, `_has_native_reading_order_defect`, `recompose_native_paragraphs`, `remove_repetitive_margins`, demais cleaners) instrumentando diretamente as funções já existentes (sem alterá-las); identificar o primeiro estágio responsável e por que as salvaguardas existentes não disparam. `design.md`, ETAPA 3.
- [x] 0.5 Avaliar critérios candidatos (rotação isolada, posição marginal, sequência de caracteres únicos, duplicação geométrica) e simular blast radius contra os 8 PDFs de referência (4 processuais + 4 canônicos, 270 páginas), sem usar nome do documento, número de página, texto de assinatura, nomes de pessoas ou número do processo. `design.md`, ETAPA 4.
- [x] 0.6 Verificar separadamente se o espaçamento duplo (C.2) desaparece ao corrigir C.1 corretamente ou exige regra própria, com evidência quantitativa (não por proximidade visual). `design.md`, ETAPA 5.
- [x] 0.7 Classificar a conclusão (A ou B) com causa raiz, critério, ponto de implementação, positivos/negativos, blast radius, relação C.1/C.2 e proposta mínima futura. `design.md`, "CONCLUSÃO".
- [x] 0.8 Confirmar não regressão: nenhum arquivo de `src/`, `tests/`, `output/`, `logs/` ou do corpus canônico alterado; nenhuma chamada de OCR/LLM. `design.md`, "Verificação de não regressão".

## 1. Encerramento do ciclo de diagnóstico

- [x] 1.1 Claude (orquestrador) executou todo o diagnóstico diretamente (sem Codex/OpenCode), por se tratar de tarefa de auditoria/análise geométrica, não de implementação de código em `src/`/`tests/` — consistente com `AGENTS.md`.
- [x] 1.2 Nenhum código, teste, dependência ou corpus canônico foi alterado; commit local (sem push) desta mudança, sem arquivar (instrução explícita do usuário).
- [ ] 1.3 Atualizar `LOOPS.md` com o resultado final (diagnóstico + implementação) ao encerrar todo o ciclo desta mudança.

## 2. Implementação (TDD) — autorizada em 2026-08-12, ver `design.md` "Autorização de implementação"

> Nota geral: toda reconversão do corpus mencionada nesta seção DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar. Codex é o implementador (cria testes + código); Claude (orquestrador) revisa o diff, executa os testes e valida antes de marcar concluído — nenhum agente marca sua própria subtarefa como concluída.

- [x] 2.1 Codex criou 10 testes RED em `tests/test_converter.py` para `_has_duplicated_rotated_block` (ainda inexistente) e para dedup em `_geometric_reading_order_text`: positivos (duplicação vertical sobreposta real via `fitz.insert_text(..., rotate=90)`, mesmo texto duas vezes no mesmo ponto; dedup do texto geométrico; ausência de ruído de caractere único e ausência de chamada ao MarkItDown em `convert_document` via monkeypatch) e negativos obrigatórios (bloco vertical único; bloco horizontal duplicado ignorado; textos diferentes na mesma posição; bbox além/dentro da tolerância; ausência total de bloco vertical). `uv run pytest tests/test_converter.py -v`: 10 falharam (RED, `AttributeError`/`ImportError` esperado), 12 preexistentes continuaram passando. Nenhum arquivo de `src/` tocado nesta etapa.
- [x] 2.2 Claude revisou os testes RED: cobertura completa dos positivos/negativos exigidos, nenhum usa nome de documento/página/processo/assinatura real/pessoa real (textos de fixture genéricos). **Achado durante a revisão, incorporado antes da implementação:** o PyMuPDF pode agrupar duas cópias idênticas de um carimbo rotacionado como 2 blocos separados de bbox idêntico (caso real de `100-106-DECISÃO.pdf`, confirmado) OU como 1 bloco com 2 linhas idênticas (caso das fixtures sintéticas de teste, confirmado) — por isso a detecção/dedup foi corrigida para operar no nível de LINHA (`page.get_text("dict")[...]["lines"]`), não de bloco, exatamente como já registrado em `design.md` ("duas ou mais linhas de texto não horizontal"). Verificado byte a byte, antes de autorizar a implementação, que reconstruir o texto de um bloco a partir de suas linhas concatenadas (`"\n".join(...) + "\n"`) é idêntico ao `block[4]` já usado hoje, em ~1858 blocos de 7 PDFs do corpus de avaliação (0 mismatches) — condição necessária para garantir zero regressão em páginas sem duplicação.
- [x] 2.3 Codex implementou em `src/pipeline_juridico/converter.py`: `_line_direction`, `_line_text`, `_bbox_within_tolerance`, `_deduplicated_text_blocks_with_line_x0s` (função central: por página, remove linhas não horizontais cujo texto e bbox — tolerância 2.0pt — já apareceram antes na mesma página, mantendo a primeira ocorrência; reconstrói o texto de cada bloco a partir das linhas restantes), `_deduplicated_text_blocks` e `_has_duplicated_rotated_block` (usam a mesma lógica de comparação). `_geometric_reading_order_text` e `_sorted_native_text_blocks` passaram a consumir os blocos já deduplicados (antes usavam `page.get_text("blocks")` diretamente). Em `convert_document`, nova variável `has_duplicated_rotated_block` computada junto às demais no bloco `try` (página `texto_nativo`); quando `True`, `native_engine.convert()` (MarkItDown) NUNCA é chamado para aquela página — `content` vem direto de `reference_content` (já deduplicado); quando `False`, comportamento idêntico ao anterior. Nenhuma heurística de conteúdo (contagem de caracteres, texto de assinatura), nome de documento/página/processo, remoção genérica de texto rotacionado ou regra de espaçamento foi usada. OCR, roteamento, `config.py` e dependências não foram tocados. `uv run pytest tests/test_converter.py -v`: 22/22 passando (10 novos GREEN + 12 preexistentes). Único arquivo alterado em `src/`: `converter.py`.
- [x] 2.4 Claude revisou o diff completo de `converter.py` linha a linha: escopo confinado a funções geométricas novas + 1 condicional em `convert_document`; nenhum sinal proibido usado; nenhuma alteração em `cleaner.py`, `router.py`, `config.py`, `engines.py`, `inspector.py`, OCR ou dependências. Reexecução dos testes direcionados: 22/22 passando.
- [x] 2.5 `uv run pytest tests/`: **364/364 passando** (354 baseline + 10 novos). `openspec validate --all --strict`: 1 passed (spec), 1 failed (esta própria mudança, esperado — sem deltas, mesmo padrão do precedente diagnóstico arquivado).
- [x] 2.6 Reconversão isolada (`OUTPUT_DIR`/`LOGS_DIR` fora de `output/`/`logs/`) dos 4 PDFs de `input/processos_auditoria/` com `--no-ocr`: `Testamento Publico.pdf` corretamente bloqueado em modo estrito (comportamento preexistente, não relacionado a esta mudança); os outros 3 processaram com sucesso. `100-106-DECISÃO.md`: 2727 → 94 linhas; linhas de 1 caractere: 2130 → 0; ocorrências de espaço duplo: 502 → 0; 7 marcadores `[[Pág. N]]` preservados nos dois casos; diff de tokens (≥3 caracteres, casefold) entre antes/depois: **0 tokens perdidos**, 150 ocorrências de tokens "ganhos" — todas correspondentes a palavras do carimbo de assinatura que antes só existiam como ruído de caractere único (`documento`, `cópia`, `assinado`, `digitalmente`, `processo`, etc.), agora legíveis corretamente 1 vez. Carimbo de anexação ("Para conferir o original...") presente 7/7 páginas, carimbo de assinatura do juiz presente e não duplicado em cada uma das páginas 1–5. `001-007-Petição Inicial.md` e `086-096-CONTESTAÇÃO...md`: byte-idênticos entre antes (código sem a correção, via `git stash`) e depois.
- [x] 2.7 Reconversão isolada dos 4 PDFs canônicos com `--no-ocr`: os 4 arquivos resultantes são **byte-idênticos** a `output/AINTARESP_1462304-PA.md`, `output/REsp_1704551-SP.md`, `output/Inf0024E.md` e `output/L10.406_CC_2002.md` já commitados — logo R01, SUBTÍTULO, remoção de margens/rodapés técnicos, thin-space, índice legislativo, e a limitação conhecida Papel/Nome (nenhum desses 4 arquivos foi alterado) permanecem, por definição, intactos.
- [x] 2.8 `logs/*.report.json` das 7 reconversões bem-sucedidas: 100% das páginas com `method=texto_nativo`, `status=sucesso` — zero página roteada para OCR/erro em qualquer dos 8 PDFs. Segunda reconversão completa dos mesmos 7 PDFs (`--no-ocr`, mesmo código): **byte-idêntica** à primeira em todos os 7 arquivos — idempotência confirmada.
- [x] 2.9 Commit local (sem push) do código, testes e OpenSpec atualizado; mudança permanece não arquivada.
