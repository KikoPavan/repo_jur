> Nota geral: toda reconversão do corpus mencionada neste arquivo DEVE usar `converter-juridico --no-ocr`. Esta mudança não altera nem exercita o caminho de OCR; qualquer página que exija OCR real sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO — nunca chamar a API Gemini para contornar.

## 1. Remoção de cabeçalho/rodapé repetido fundido à continuação

- [x] 1.1 Criar testes de regressão em `tests/test_cleaner.py` para `remove_repetitive_margins` que reproduzam o caso real relatado: um documento simulado com várias páginas onde um cabeçalho de topo ("Superior Tribunal de Justiça" ou nome genérico equivalente) aparece isolado na maioria das páginas e, em uma delas, fundido ao início da continuação de um item ("agravada, pois demonstrado..."), incluindo o fim do item 6 na página anterior, o marcador `[[Pág. 5]]`, o cabeçalho repetido e a continuação. Confirmar que o teste falha antes da correção. Incluir também os testes negativos exigidos: cabeçalho isolado sem continuação permanece detectável e removível sem afetar linhas vizinhas; a mesma expressão usada legitimamente no meio de um parágrafo (não na posição de topo/rodapé de bloco) não é removida; uma continuação normal entre páginas sem cabeçalho não é afetada; um candidato abaixo do limiar de 60% de frequência não é removido; um candidato que nunca aparece isolado (sempre fundido) não é removido. Confirmar por execução da suíte que títulos, ementas, artigos, os oito cabeçalhos SUBTÍTULO, os 4 casos de R01 (Art. 44 §2º, 593, 1.458, 1.368-F) e as duas separações jurisprudenciais já corrigidas continuam passando sem alteração.
- [ ] 1.2 Implementar em `remove_repetitive_margins` (`src/pipeline_juridico/cleaner.py`) a menor correção generalizável: uma segunda etapa de detecção de margens repetitivas por texto verbatim (byte-idêntico), exigindo pelo menos duas ocorrências isoladas do mesmo texto na posição de topo/rodapé do bloco de página e frequência total (isoladas + fundidas como prefixo) igual ou acima do `minimum_occurrences` já usado pelos quatro padrões existentes. Quando fundido, remover apenas o prefixo do cabeçalho/rodapé e preservar o restante da linha. Não criar regra específica a este processo, tribunal ou frase. Não alterar `recompose_native_paragraphs`, a proteção de fechamento jurisprudencial, `build_legislative_headings`, `mark_final_index`, o extrator, o roteamento, o OCR ou a arquitetura geral.
- [ ] 1.3 Executar a suíte completa (`uv run pytest tests/`) e confirmar que todos os novos testes passam e que nenhum teste existente regrediu.
- [ ] 1.4 Reconverter os 4 PDFs do corpus fixo com `converter-juridico --no-ocr` (`AINTARESP_1462304-PA.pdf`, `REsp_1704551-SP.pdf`, `Inf0024E.pdf`, `L10.406_CC_2002.pdf`), comparar antes/depois e confirmar: (a) o trecho relatado passa a iniciar por "agravada, pois..." e "Superior Tribunal de Justiça" não permanece anexado; (b) a segunda ocorrência equivalente no mesmo arquivo ("Superior Tribunal de Justiça impossibilidade de estimá-lo...") também é corrigida; (c) as ocorrências equivalentes identificadas no diagnóstico em `Inf0024E.pdf` (cabeçalho "Informativo de Jurisprudência n. 24...") e `REsp_1704551-SP.pdf` (rodapé "Documento: 1807307...") são removidas quando isoladas, sem perda de conteúdo jurídico; (d) nenhum conteúdo jurídico legítimo foi removido em nenhum dos 4 arquivos; (e) as correções anteriores (R01, separações jurisprudenciais, cabeçalhos SUBTÍTULO) permanecem intactas; (f) os marcadores `[[Pág. N]]` permanecem sequenciais, únicos e em quantidade idêntica à anterior nos 4 arquivos; (g) nenhuma página acionou OCR sob `--no-ocr`; (h) nenhuma alteração indevida ocorreu além dos casos documentados acima.
