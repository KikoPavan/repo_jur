# Loop de implementação

Mudança ativa:

`fix-editorial-cover-structural-boundaries` — implementação e validação concluídas (todas as
subtarefas de `tasks.md` marcadas, exceto o registro do arquivamento, que exige aprovação humana).
Corrige o colapso estrutural da primeira página editorial de `output/Inf0024E.md`, onde título
estilizado, linha de edição/data, aviso editorial e cabeçalho de câmara julgadora ("CORTE
ESPECIAL") apareciam fundidos em uma única linha.

Causa raiz: `recompose_native_paragraphs` (`src/pipeline_juridico/cleaner.py`) estima a posição de
cada linha física de um bloco PyMuPDF dividindo a altura do bloco pelo número de linhas
NÃO-VAZIAS, sem ajustar para o espaço das linhas em branco descartadas. No bloco de edição/data
desta página (7 linhas em branco + 2 reais, medido via `page.get_text("dict")`), isso produzia um
gap de junção NEGATIVO entre o título e a linha seguinte — a fusão era matematicamente inevitável,
não apenas provável. Mesma classe de mecanismo já identificada na investigação de `SAIBA MAIS`,
aqui mais severa.

Um primeiro candidato de correção (interpolar sobre o total de linhas físicas, sem gate) tinha
blast radius de 3/241 páginas, mas 2 delas (`AINTARESP_1462304-PA.pdf` p.11, `REsp_1704551-SP.pdf`
p.2) pertencem ao achado pendente `Papel/Nome`, já fechado duas vezes sem critério seguro
(`openspec/changes/archive/2026-08-07-fix-role-name-list-cross-block-fusion/`). Três alternativas
foram comparadas empiricamente contra os 4 PDFs (aceitar o efeito colateral; sinal estrutural
adicional; deferir para futura investigação de `Papel/Nome`); a aprovada por decisão humana foi um
gate por página: a correção de interpolação só é permitida em páginas com pelo menos um bloco de
texto ≥20pt. Limiar justificado por evidência tipográfica, não número mágico: medição de ~13.500
spans nos 4 PDFs mostra uma lacuna real de 10.5pt entre o maior rótulo estrutural do corpus inteiro
(15.0pt — `SAIBA MAIS`, `INFORMAÇÕES DO INTEIRO TEOR`, `CORTE ESPECIAL` etc.) e o menor elemento de
masthead observado (25.5pt — banner "Superior Tribunal de Justiça" do AINTARESP, presente em 8
páginas sem o defeito, confirmando que o sinal generaliza e que sua presença sozinha não altera
nada quando não há linhas em branco intercaladas). Implementação: novo parâmetro
`page_has_large_text: bool = False` em `recompose_native_paragraphs` (fora desse modo, o cálculo
existente é preservado byte a byte) e nova função `_page_has_large_text` em `converter.py`, via
`page.get_text("dict")`. Extrator, roteamento, OCR e a interpolação para páginas sem fonte grande
não foram tocados.

Validação: suíte `339/339`, `openspec validate --all --strict` limpo, quatro PDFs reconvertidos com
`--no-ocr` (241 páginas roteadas como `texto_nativo`, zero OCR), idempotência confirmada. Único
arquivo alterado no corpus: `output/Inf0024E.md`, página 1 (4 elementos editoriais separados em
parágrafos distintos), contagem de tokens 9084 → 9084 (diferença zero). `AINTARESP_1462304-PA.md`,
`REsp_1704551-SP.md` e `L10.406_CC_2002.md` byte-idênticos ao baseline pré-mudança — `Papel/Nome`
(AINTARESP p.11 continua fundido exatamente como antes), R01 4/4, 8 SUBTÍTULO, índice do CC,
rodapés técnicos e `SAIBA MAIS` preservados por decorrência direta ou reverificação direta.
Marcadores `[[Pág. N]]` únicos e sequenciais nos 4 arquivos. Achado documentado, fora de escopo: a
correção geral da interpolação de altura de linha permanece um defeito de precisão geométrica
compartilhado; as 44 decisões de junção identificadas na investigação de `SAIBA MAIS` continuam sem
correção geral aprovada.

**Achado pendente — NÃO RESOLVIDO** (originado ao validar o corpus de `fix-colon-label-pagewide-recomposition-bypass` em 2026-08-07; diagnóstico aprofundado em duas rodadas e documentado em `openspec/changes/archive/2026-08-07-fix-role-name-list-cross-block-fusion/` — `proposal.md`/`design.md`/`tasks.md`, commit `6fba378`). Esse arquivamento é um **fechamento administrativo**, não uma correção: nenhum critério seguro foi encontrado, nenhum código de produção foi alterado, e as tarefas de TDD/implementação permanecem marcadas `BLOQUEADO`. Uma **nova mudança OpenSpec própria** precisará ser criada para qualquer tentativa futura (esta pasta arquivada é só o registro do diagnóstico, não deve ser reaberta como "mudança ativa"):

- Sintoma: o mecanismo geométrico de junção entre blocos (`gap <= previous_height * 1.2`, em `recompose_native_paragraphs`) funde indevidamente sequências de campos "Papel\nNome" (sem nenhuma linha iniciada por `:`) e listas de nomes de advogados em um único parágrafo corrido, em páginas de capa/sessão que antes ficavam inteiramente bypassadas pelo guard `:`. Exemplos reais: `AINTARESP_1462304-PA.pdf` p.11; `REsp_1704551-SP.pdf` p.3 e p.14. Nenhum token é alterado/perdido, mas a estrutura (papel/nome distintos) vira texto corrido.
- Mecanismo diferente do guard `:` e do `native_label_pattern` já corrigidos: aqui não há marcador `:` nem linha inteiramente em caixa alta envolvidos.
- **Três critérios geométricos já investigados e descartados, em duas rodadas** (validados contra o corpus real via reconversão `--no-ocr`, não apenas testes sintéticos — detalhes completos em `design.md` da mudança):
  1. Restringir junção ao mesmo bloco de origem: os 4 casos R01 nomeados sobrevivem (na geometria real estão dentro de um único bloco PyMuPDF, ao contrário do que os testes sintéticos sugerem), mas quebra outras continuações reais do Código Civil não cobertas pelos 4 nomes (`Art. 1.544`, `Art. 1.619`, `Art. 1.734`, um `Parágrafo único` antes do `Art. 1.758`).
  2. Exigir recuo (dedent) do x0 por linha (via `page.get_text("dict")`, hoje não extraído pelo pipeline) em relação à abertura do parágrafo, só em junções entre blocos diferentes: corrige `Papel/Nome` e preserva R01 e as demais continuações reais do corpo de artigos, mas quebra títulos legislativos centralizados que quebram em 2 blocos, um artefato de layout em colunas na página de rosto, e a normalização de símbolo entre páginas (`join_symbol_across_page_break`) na fronteira `Art. 2.029`/`Art. 2.030`.
  3. (Rodada 2) Versão refinada do critério 2, com bloqueio incondicional de junção entre linhas na mesma linha visual, isenção da exigência de recuo para títulos que seguem marcador bare, e colapso de linhas em branco consecutivas em `remove_repetitive_margins`: resolveu **todas** as regressões do critério 2 no Código Civil (Art. 1.544/1.619/1.734/Parágrafo único, TÍTULO IV, símbolo Art. 2.029/2.030), restando só 3 diferenças confinadas à capa/índice do CC, sem perda de token — mas validado contra o corpus INTEIRO, revelou uma regressão **mais grave que o problema original**: fragmenta parágrafos inteiros de fundamentação jurídica em `Inf0024E.pdf` e `AINTARESP_1462304-PA.pdf` (um parágrafo por frase), porque esses arquivos não usam a convenção de recuo-na-primeira-linha do Código Civil (que se mostrou uma convenção de UM gerador de PDF específico, não universal). Descartado de forma decisiva.
- Candidatos ainda não avaliados (ver `design.md`, "Próximos passos possíveis" da rodada 2): um sinal que não dependa de recuo-na-primeira-linha, ex. repetição estatística do padrão "bloco curto, N linhas, mesmo x0 do vizinho" ao longo da página inteira; reaproveitar a lógica já validada de "primeira linha do bloco" (`native_label_pattern`/`is_first`) para blocos inteiros de 1-2 linhas; um vocabulário restrito a palavras-função genéricas de capa processual (Relator(a), Presidente da Sessão, Secretário — não nomes próprios nem específicos de tribunal/processo, para não violar a regra de "não criar lista fixa").
- Requisito para a mudança futura: encontrar e validar contra o corpus INTEIRO (não só o Código Civil) um critério que preserve simultaneamente os 4 R01, as demais continuações entre blocos do Código Civil, os títulos legislativos centralizados, a normalização de símbolo entre páginas, e o texto substantivo de `Inf0024E.pdf`/`AINTARESP_1462304-PA.pdf`, antes de qualquer implementação.

Histórico:

- `establish-juridical-pdf-conversion-pipeline` foi concluída e arquivada em 2026-07-27.
- `improve-markdown-cleanup-structuring` foi concluída (grupos 0–9, incluindo o defeito R01 de recomposição determinística de parágrafos) e arquivada em 2026-07-30 (`openspec/changes/archive/2026-07-30-improve-markdown-cleanup-structuring/`). Detalhes e métricas de cada rodada de validação em `RELATORIO_FINAL.md` dentro dessa pasta.
- `fix-jurisprudence-precedent-fusion` foi concluída e arquivada em 2026-07-31 (commit de arquivamento `ff850e4`, `openspec/changes/archive/2026-07-31-fix-jurisprudence-precedent-fusion/`). Causa raiz: ausência de proteção contra fusão entre o fechamento de uma citação jurisprudencial e o início em caixa alta do precedente seguinte. Correção: verificação do parágrafo acumulado por regex antes da decisão de junção. Ocorrências corrigidas: `AINTARESP_1462304-PA.pdf` e `REsp_1704551-SP.pdf`. Validação: suíte `272/272`, quatro PDFs reconvertidos com `--no-ocr`, marcadores `[[Pág. N]]` preservados, `openspec validate --all --strict`: 1 passed, 0 failed.
- `fix-subtitulo-structural-boundaries` foi concluída e arquivada em 2026-08-06 (commit de arquivamento `26449ca`, `openspec/changes/archive/2026-08-06-fix-subtitulo-structural-boundaries/`). Causa raiz: `SUBTÍTULO`/`SUBTITULO` nunca fora incluído no vocabulário de marcadores estruturais de `cleaner.py` (`_LEGISLATIVE_MARKER_PATTERN`, `formal_structure_pattern`, `bare_structure_pattern`, `qualified_structure_pattern`, `heading_levels`), fazendo com que fosse tratado como texto comum e fundido ao artigo/parágrafo/inciso/TÍTULO anterior. Correção: SUBTÍTULO adicionado a esse vocabulário no nível Markdown `####` (mesmo de CAPÍTULO, confirmado no corpus real). Ocorrências corrigidas: as 8 do corpo de `L10.406_CC_2002.pdf`. Validação: suíte `278/278`, quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR), índice final e os 3 demais arquivos do corpus byte-idênticos, `openspec validate --all --strict`: 2 passed, 0 failed durante a mudança / 1 passed após o arquivamento.

- `fix-repeated-header-cross-page-fusion` foi concluída e arquivada em 2026-08-06 (commit de arquivamento `76dc8ab`, `openspec/changes/archive/2026-08-06-fix-repeated-header-cross-page-fusion/`). Causa raiz: `remove_repetitive_margins` só reconhecia 4 padrões fixos de margem por regex (data/hora, arquivo, URL, contador de página), sem cobrir cabeçalho/rodapé textual repetido verbatim; um cabeçalho institucional ("Superior Tribunal de Justiça") ficava por isso fundido ao início do conteúdo real da página seguinte pela junção geométrica de `recompose_native_paragraphs`. Correção: segunda etapa de detecção por texto verbatim, exigindo ≥2 ocorrências isoladas e frequência total ≥ ao mesmo limiar de 60% já usado. Ocorrências corrigidas: 2 em `AINTARESP_1462304-PA.pdf` (caso relatado + equivalente no mesmo arquivo) e 28 em `Inf0024E.pdf`; o rodapé equivalente suspeito em `REsp_1704551-SP.pdf` foi investigado e confirmado como corretamente preservado (abaixo do limiar após a remoção pré-existente do contador de página). Validação: suíte `283/283` (278 + 6 testes novos − 1 teste preexistente substituído), quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR), `openspec validate --all --strict`: 2 passed, 0 failed durante a mudança / 1 passed após o arquivamento.

- `fix-vertical-fragmented-text-recomposition` foi concluída e arquivada em 2026-08-07 (commit de arquivamento `e57d2d2`, `openspec/changes/archive/2026-08-07-fix-vertical-fragmented-text-recomposition/`). Causa raiz: em `recompose_native_paragraphs` (`src/pipeline_juridico/cleaner.py`), `native_label_pattern` bloqueava a junção de qualquer linha inteiramente em maiúsculas/sem pontuação, sem checar se essa linha era a primeira linha física do seu bloco geométrico de origem (PyMuPDF `get_text("blocks")`) — protegendo corretamente rótulos de campo reais (`PROCESSO`, `TEMA`, `RAMO DO DIREITO`, `DESTAQUE`), mas também bloqueando indevidamente palavras/fragmentos no meio de um bloco já em fluxo (ementas e campos temáticos fragmentados palavra a palavra). Correção: a proteção passou a exigir também que a linha seja a primeira linha física do bloco de origem. Ocorrências corrigidas: `AINTARESP_1462304-PA.pdf` p.7 (2 trechos) e p.9, `Inf0024E.pdf` p.4 (+ 2 ocorrências equivalentes de "Jurisprudência em Teses"), `REsp_1704551-SP.pdf` p.12; `L10.406_CC_2002.pdf` byte-idêntico (sem ocorrências). Validação: suíte `290/290` (283 + 7 itens novos), quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR, marcadores `[[Pág. N]]` únicos/sequenciais e com contagem idêntica, idempotência confirmada em segunda reconversão), `openspec validate --all --strict`: 2 passed, 0 failed durante a mudança / 1 passed após o arquivamento. Achado fora de escopo (guard de linhas iniciadas por `:` que desativa a recomposição da página inteira) retomado e corrigido em 2026-08-07 pela mudança `fix-colon-label-pagewide-recomposition-bypass` (ver entrada seguinte no histórico).

- `fix-colon-label-pagewide-recomposition-bypass` foi concluída e arquivada em 2026-08-07 (commit de arquivamento `aa8cf7c`, `openspec/changes/archive/2026-08-07-fix-colon-label-pagewide-recomposition-bypass/`). Causa raiz: em `recompose_native_paragraphs` (`src/pipeline_juridico/cleaner.py`), o early-return `if any(line.strip().startswith(":") ...): return content` varria TODOS os blocos da página e, ao achar qualquer linha `:` em qualquer bloco, abortava a recomposição da PÁGINA INTEIRA — mesmo blocos totalmente independentes (ex. a ementa) nunca chegavam a ser processados. Correção: a proteção passou a ser marcada por bloco geométrico de origem (propagada a cada linha física), impedindo a junção só quando a linha anterior ou atual pertence a um bloco `:`-marcado; blocos sem `:` na mesma página voltam a ser recompostos normalmente. Independente da causa de `fix-vertical-fragmented-text-recomposition` (aqui a função nem chegava a rodar a lógica de junção). Páginas corrigidas: `AINTARESP_1462304-PA.pdf` p.1, 4, 11; `REsp_1704551-SP.pdf` p.1, 3, 4, 6, 7, 14. `Inf0024E.pdf` e `L10.406_CC_2002.pdf` ficaram byte-idênticos (sem ocorrências). Validação: suíte `294/294` (290 + 4 novos), auditoria token a token confirmou 0 tokens adicionados e as únicas remoções (336 em AINTARESP, 132 em REsp) 100% rastreadas a um rodapé de assinatura repetido agora corretamente exposto ao mecanismo já aprovado `remove_repetitive_margins`; R01 (Art. 44 §2º, Art. 593, Art. 1.458, Art. 1.368-F), SUBTÍTULO (8/8), separação de precedentes e remoção de cabeçalhos repetitivos reverificados intactos; quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR, marcadores `[[Pág. N]]` únicos/sequenciais, idempotência confirmada), `openspec validate --all --strict`: 2 passed, 0 failed durante a mudança / 1 passed após o arquivamento. Achado fora de escopo (fusão geométrica de listas "Papel/Nome" sem `:`) descoberto ao validar esta mudança, registrado acima em "Achado pendente", ainda sem mudança OpenSpec própria.

- `fix-legislative-index-heading-hierarchy` foi concluída e arquivada em 2026-08-07 (commit de arquivamento `4a72772`, `openspec/changes/archive/2026-08-07-fix-legislative-index-heading-hierarchy/`). Causa raiz: "PARTE GERAL"/"PARTE ESPECIAL" chegam letter-spaced (`P A R T E   G E R A L`) ao pipeline tanto no corpo quanto, de forma idêntica, dentro do índice final do Código Civil — confirmado via extração bruta do PyMuPDF (`page.get_text()`, página 177 de `L10.406_CC_2002.pdf`); `build_legislative_headings` (`src/pipeline_juridico/cleaner.py`) converte esse texto em cabeçalho `#` usando a mesma tabela de níveis do corpo, sem noção de "dentro do índice", e `mark_final_index`, que roda depois e sabe onde o índice começa, nunca reexaminava o nível dos cabeçalhos já gerados ali. Resultado: `# ÍNDICE`, `# PARTE GERAL` e `# PARTE ESPECIAL` apareciam como três cabeçalhos de nível 1 irmãos. Correção: `mark_final_index` passou a demover em +1 nível qualquer cabeçalho Markdown ATX já existente estritamente após `# ÍNDICE`, com base somente na posição relativa a `# ÍNDICE` — nunca no texto "PARTE GERAL"/"PARTE ESPECIAL" — de modo que `# ÍNDICE` permanece raiz do índice e os cabeçalhos já existentes ficam subordinados a ele; o corpo legislativo (incluindo os `# PARTE GERAL`/`# PARTE ESPECIAL` legítimos antes do índice) não foi alterado. No corpus atual, as únicas entradas do índice que já eram cabeçalhos Markdown eram PARTE GERAL/PARTE ESPECIAL (as demais — LIVRO, TÍTULO, SUBTÍTULO, CAPÍTULO, Seção — não chegam letter-spaced e permanecem texto comum, inalteradas por esta mudança). Validação: suíte `302/302`, quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR, 186 marcadores `[[Pág. N]]` únicos/sequenciais, 8 SUBTÍTULO e os 4 casos R01 intactos, idempotência confirmada em segunda reconversão); único diff no corpus: `output/L10.406_CC_2002.md` (`# PARTE GERAL`/`# PARTE ESPECIAL` do índice → `## PARTE GERAL`/`## PARTE ESPECIAL`, corpo byte-idêntico antes de `# ÍNDICE`), os outros 3 arquivos do corpus byte-idênticos; extrator, roteamento, OCR e recomposição geométrica não foram tocados; `openspec validate --all --strict`: 2 passed, 0 failed durante a mudança / 1 passed após o arquivamento.

- `fix-technical-footer-paragraph-contamination` foi concluída e arquivada em 2026-08-08 (commit de arquivamento `e438bc0`, `openspec/changes/archive/2026-08-08-fix-technical-footer-paragraph-contamination/`). Defeito independente do achado pendente `Papel/Nome` acima (que permanece NÃO RESOLVIDO, não alterado incidentalmente por esta mudança). Causa raiz localizada inteiramente em `remove_repetitive_margins` (`src/pipeline_juridico/cleaner.py`), em duas camadas: (1) sua função interna `remove_verbatim_margins` só reconhecia um candidato recorrente como linha inteira ou como **prefixo** de uma linha maior (rodapé colado ao início da página seguinte, caso já corrigido por `fix-repeated-header-cross-page-fusion`) — faltava o caso simétrico de **sufixo** (rodapé colado ao final da página anterior, a geometria real de um rodapé); (2) em `AINTARESP_1462304-PA.pdf` havia duas margens recorrentes empilhadas na mesma borda — uma assinatura eletrônica legítima (externa) e o rodapé técnico GABGF09 (interno, logo abaixo dela) — e a função só examinava a última linha de conteúdo da página, calculada uma única vez, removendo a margem externa mas nunca alcançando a interna. Critério adotado: recorrência verbatim pelo mesmo limiar de quorum já existente + posição de borda (prefixo ou sufixo), com a remoção reaplicada até ponto fixo; nenhuma identificação por `GABGF09`, número de processo, `Documento`, `Página`, `DJe` ou código específico. Ocorrências corrigidas: `AINTARESP_1462304-PA.pdf` 8/8 (144 tokens técnicos removidos, 0 adicionados); `REsp_1704551-SP.pdf` 3/3 (36 tokens técnicos removidos, 0 adicionados) — 0 tokens de conteúdo jurídico removidos em ambos. Validação: suíte `311/311`, `openspec validate --all --strict` limpo, quatro PDFs reconvertidos com `--no-ocr` (nenhuma página exigiu OCR), idempotência confirmada nos 4 arquivos, `Inf0024E.pdf` e `L10.406_CC_2002.pdf` byte-idênticos, R01 4/4, 8 SUBTÍTULO e índice preservados, marcadores `[[Pág. N]]` únicos/sequenciais. Extrator, roteamento, OCR e `recompose_native_paragraphs` não foram tocados. Achado documentado, fora de escopo desta mudança: o texto interrompido entre páginas (ex. "Paulo de" / `[[Pág. N]]` / "Tarso Sanseverino") não é recomposto entre páginas — apenas o rodapé é removido, restaurando a mesma fragmentação natural já aceita em qualquer outro ponto do corpus onde uma frase atravessa uma quebra de página.

- `fix-html-thin-space-normalization` foi concluída e arquivada em 2026-08-09 (commit de arquivamento `afbdfb6`, `openspec/changes/archive/2026-08-09-fix-html-thin-space-normalization/`). Causa raiz: o literal `&#8201;` (entidade HTML decimal do caractere THIN SPACE, U+2009) já está embutido no texto nativo de `Inf0024E.pdf`, confirmado via extração bruta do PyMuPDF (`page.get_text()`, página 9) — não é produzido por MarkItDown/pdfminer nem por nenhuma função de `src/pipeline_juridico/` (nenhuma continha lógica de decodificação de entidades HTML antes desta mudança). Foram encontradas 21 ocorrências no corpus, todas em `Inf0024E.pdf`; variantes equivalentes verificadas e ausentes do corpus: `&#x2009;`/`&#X2009;` (hexadecimal), `&thinsp;` (nomeada) e o caractere Unicode real U+2009. Normalização escolhida: espaço ASCII regular (U+0020), não o caractere thin-space real, já que em todos os casos a função da entidade é puramente separar palavras/tokens coladas e o restante do pipeline já usa espaço ASCII como separador universal. Correção: nova função `normalize_thin_space_entities` em `src/pipeline_juridico/cleaner.py`, chamada em `converter.py` sobre o `raw_markdown` já composto, logo após `compose_document(blocks)` e antes de `remove_repetitive_margins`. A regra é restrita às representações de THIN SPACE (`&#8201;`, `&#x2009;`/`&#X2009;`, `&thinsp;`/`&THINSP;`, case-insensitive) — sem decodificação HTML genérica (`&amp;`, `&lt;`, `&gt;`, `&nbsp;` permanecem intocados). Ocorrências corrigidas: `output/Inf0024E.md` com zero `&#8201;` residuais (antes: 21); 21 fragmentos numéricos artificiais (`8201`) removidos da contagem de tokens (9105 → 9084), sem perda de nenhuma palavra jurídica e sem espaço duplicado. `AINTARESP_1462304-PA.md`, `REsp_1704551-SP.md` e `L10.406_CC_2002.md` byte-idênticos ao baseline pré-mudança — R01 4/4, 8 SUBTÍTULO, índice do Código Civil e rodapés técnicos já removidos permanecem preservados por decorrência direta. Validação: suíte `323/323` (311 + 12 testes novos), `openspec validate --all --strict` limpo, quatro PDFs reconvertidos com `--no-ocr` (241 páginas processadas como `texto_nativo`, zero OCR), idempotência confirmada (segunda reconversão byte-idêntica à primeira nos 4 arquivos), marcadores `[[Pág. N]]` únicos/sequenciais. Extrator, roteamento, OCR e `recompose_native_paragraphs` não foram tocados. Achados fora de escopo (fusões em "SAIBA MAIS", primeira página colapsada do Inf0024E, `RECURSO / ESPECIAL`, `Papel/Nome`) permanecem intocados, como previsto.

- `fix-saiba-mais-item-boundaries` foi concluída e arquivada em 2026-08-09 (commit de arquivamento `b03fd2d`, `openspec/changes/archive/2026-08-09-fix-saiba-mais-item-boundaries/`). Corrige a fusão indevida de itens independentes da seção editorial `SAIBA MAIS` em `output/Inf0024E.md` — 3 ocorrências reais: página 4 (precedente `CC 159976/SP, ... DJe 16/04/2019` fundido ao `Informativo de Jurisprudência n. 474` seguinte), página 14 (dois itens `Jurisprudência em Teses` e um `Informativo de Jurisprudência n. 751` fundidos em uma única linha), página 18 (`Jurisprudência em Teses` fundido ao `Informativo de Jurisprudência n. 388` seguinte). Causa raiz: `recompose_native_paragraphs` (`src/pipeline_juridico/cleaner.py`) estima a altura de cada linha física de um bloco PyMuPDF dividindo a altura total do bloco pelo número de linhas físicas (`line_height = (y1 - y0) / len(physical_lines)`); para itens de `SAIBA MAIS` que quebram em 2 linhas físicas dentro de um único bloco, essa interpolação superestima a altura real da linha (12,5pt interpolado vs. 10pt real, confirmado via `page.get_text("dict")`), inflando o limiar de junção seguinte (`gap <= previous_height * 1.2`) de ~12pt para exatamente 15pt — coincidindo, ponto a ponto, com o espaçamento real de 15pt usado entre itens distintos da lista. Duas alternativas globais foram investigadas e descartadas por alterarem 44 decisões de junção em todo o corpus: substituir a interpolação por geometria real de linha em todo o pipeline (afeta o índice do Código Civil, páginas 178–186, já endurecido por mudança arquivada anterior, e quebra uma junção legítima nunca testada em `AINTARESP_1462304-PA.pdf` página 3) e apertar o limiar de empate (`<=` para `<`, mesmo efeito colateral). Critério adotado: preservação de fronteiras entre blocos geométricos distintos dentro da seção estrutural `SAIBA MAIS` — um guard local em `recompose_native_paragraphs`, ancorado no rótulo fixo e genérico `SAIBA MAIS` (já reconhecido por `native_label_pattern`, mesma classe estrutural de `PROCESSO`/`TEMA`/`DESTAQUE`), sem nenhuma lista ou vocabulário de item (`Informativo`, `Jurisprudência em Teses`, `/`, `DJe`, números) — a união de linhas físicas dentro do mesmo bloco (mesmo item) continua normal. Implementação restrita a `recompose_native_paragraphs`; nenhuma outra função, o extrator, o roteamento, o OCR ou as dependências foram tocados. Ocorrência corrigida somente em `output/Inf0024E.md`; `AINTARESP_1462304-PA.md`, `REsp_1704551-SP.md` e `L10.406_CC_2002.md` byte-idênticos ao baseline pré-mudança (`SAIBA MAIS` não ocorre em nenhum deles) — R01 4/4, 8 SUBTÍTULO, índice do CC, rodapés técnicos já removidos e a normalização de thin-space preservados por decorrência direta; `Papel/Nome`, a primeira página do Inf0024E e `RECURSO / ESPECIAL` permanecem fora de escopo, inalterados. Validação: suíte `332/332`, `openspec validate --all --strict` limpo, quatro PDFs reconvertidos com `--no-ocr` (241 páginas roteadas como `texto_nativo`, zero OCR), idempotência confirmada (segunda reconversão byte-idêntica à primeira). Achado documentado, fora de escopo: a correção geral da interpolação de altura de linha em `recompose_native_paragraphs` permanece um defeito de precisão geométrica compartilhado, não corrigido; as 44 decisões de junção identificadas na investigação precisariam ser validadas caso a caso antes de qualquer generalização futura.

Regra geral do corpus de regressão (válida para qualquer mudança futura sobre esses 4 PDFs, não só a arquivada acima): todas as reconversões usam `converter-juridico --no-ocr`. Nenhuma mudança sobre este corpus deve alterar ou exercitar o caminho de OCR; página que exigir OCR sob `--no-ocr` é regressão de roteamento ou caso BLOQUEADO, nunca resolvido chamando a API Gemini.

Fluxo:

1. Claude identifica a primeira subtarefa não marcada.
2. Codex implementa e testa somente essa subtarefa.
3. Claude verifica (diff, testes, `openspec validate --strict`) sem que o Codex se autoaprove.
4. Claude marca a subtarefa e commita localmente (sem push) após aprovação explícita própria.
5. O ciclo recomeça na próxima subtarefa.

Nota: o OpenCode foi removido do fluxo em 2026-07-26 por instabilidade (travamentos recorrentes em execuções headless). Codex assumiu o papel de implementador; Claude assumiu a verificação final, já que o mesmo agente não deve implementar e aprovar sozinho.

Após duas tentativas sem progresso, interromper o ciclo e informar o erro.

OCR real e arquivamento exigem aprovação humana.
