# Linha de base — 2026-07-27

## Suíte de testes

Comando: `uv run pytest tests/ -q`
Resultado: 185 passed.

## Comando de conversão

`uv run converter-juridico <pdf>` → `pipeline_juridico.converter.convert_document`
(chamado via CLI em `src/pipeline_juridico/cli.py`).

## Corpus fixo de regressão (real, em `input/`)

| Arquivo | Páginas | Métodos (relatório existente) |
| --- | --- | --- |
| AINTARESP_1462304-PA.pdf | 12 | texto_nativo: 12 |
| Inf0024E.pdf | 29 | texto_nativo: 28, hibrido: 1 |
| REsp_1704551-SP.pdf | 14 | texto_nativo: 14 |

Saídas já existentes em `output/*.md` e `logs/*.report.json` foram usadas como
linha de base (não reconvertidas nesta etapa para evitar nova chamada real de
OCR na página `hibrido` de `Inf0024E.pdf` sem necessidade — reaproveita-se o
resultado já produzido anteriormente, com aprovação humana implícita de quando
foi gerado).

## Invariantes verificados na linha de base

- Marcadores `[[Pág. N]]`: sequenciais e completos nos 3 arquivos (12, 29, 14
  páginas, sem lacunas nem duplicação).
- Caractere de substituição `�`: ausente nos 3 arquivos.
- Nenhuma página com conteúdo suspeitosamente vazio (< 30 caracteres) apesar de
  método não-vazio.

## Defeito identificado (ver `iterations/0001-*.md`)

Blocos de rótulo/valor (`RELATOR` / `: MINISTRO ...`) e parágrafos de ementa
centralizados/justificados estão sendo reordenados ou convertidos em tabelas
falsas pelo motor nativo (MarkItDown → pdfplumber/pdfminer), violando os
bloqueios "frase ou coluna invertida" e "título ou ementa transformado em
tabela falsa".
